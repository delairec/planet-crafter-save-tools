# IDE
*.iml
.idea/**

# package manager
node_modules/

# generated
output/

# user input: no trailing slash, so the rule also covers the symlink a linked worktree points
# at the folder of the main checkout
input

# other
.claude/
.github/agents
next-steps.txt


# private agent context (satellite repository .do-not-commit)
/.do-not-commit/
