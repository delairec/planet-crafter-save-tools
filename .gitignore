# IDE
*.iml
.idea/**

# package manager
node_modules/
package-lock.json

# generated
output/
.output/
playwright-report/
test-results/
blob-report/

# user input: no trailing slash, so the rule also covers the symlink a linked worktree points
# at the folder of the main checkout
input

# other
.claude/
.github/agents
next-steps.txt


# private agent context (satellite repository .do-not-commit): no trailing slash, so the rule also
# covers the symlink a linked worktree points at the clone of the main checkout — the spec.awawa
# link at the root resolves through it, so linking it there is now an ordinary gesture
.do-not-commit
