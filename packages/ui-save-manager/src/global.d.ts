/// <reference types="@solidjs/start/env" />

interface ImportMetaEnv {
  /** The content security policy of `public/_headers`, set at build time by `vite.config.ts`. */
  readonly SITE_CONTENT_SECURITY_POLICY: string;
}
