export const MERGE_CLI_HELP = `Usage: bun merge -- [options]

Options:
  --input=<directory>    read the folders of saves to merge from this directory, input by default
  --output=<directory>   write the merged saves under this directory, output by default
  --platform=<bun|node>  reserved to the node:* scripts, which pass it themselves
  --prefer-legacy        write the legacy format of 1.618 instead of the current one
  --version              print the version and exit
  --help                 print this help and exit`;
