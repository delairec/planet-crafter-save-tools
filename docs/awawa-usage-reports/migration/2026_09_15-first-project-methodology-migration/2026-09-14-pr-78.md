# 2026-09-14 — PR #78 test (ui): T40 — validation errors, warnings and loading states, end to end

| Field                                          | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
|------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Author of this report**                      | Claude Fable 5.1, in a session with no memory of the work it judges, launched from the review session it measures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Model that drove the sessions under review** | Claude Opus 5 (the `Co-Authored-By` trailer of all 6 commits, one model only).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Sessions measured**                          | `739f22f1` = `session_01RQMo7ZgAYSciBGeduFs1uL` (piloting, wave launch of T40 and T43 by `/plan-wave-start` — its figures are upper bounds) with its agent `af55cd15` (T40 implementation, worktree `agent-af55cd1560b3cdcd2`); `6fd9232d` = `session_01VFt16vNFaYZWcHd9QVSrh4` (piloting, review, `/awawa-pr-review 78`) with its agent `a1c24df5` (one review pass). The trailer `session_01RQMo…` matches six transcript files (`066493d0`, `40c2de7f`, `9ce1db9f`, `5a896bb2`, `739f22f1`, `e2f52691`) and none by `sessionId`; the piloting session was told apart by the count of the trailer in its own attribution reminder and by the worktree name of its agent. Agent `a9eeedfd` (T43) and agent `aba834f5` (this report) are excluded. |
| **Delivered**                                  | Three scenario files (`validation-errors`, `warnings`, `loading-state`) and two generated fixtures in `../../../../packages/ui-save-manager/e2e`; `@TASK.T40_ScenariosDeValidationEtDEtatsDeChargement` given three anchored `SPEC` lines and promoted to `implemented`; `@DECISION.UnScenarioDUiAsserteCeQueLEcranMontrePasLaRegleQuiLAProduit` and `@DECISION.UnEtatDeChargementSObserveEnRetenantLaLectureDuFichier` written by the implementation; `@OPEN_QUESTION.LEtatDAttenteDHydratationNEstCouvertParAucunScenario` opened by the implementation and closed by the review under `@DECISION.LEtatDAttenteDHydratationResteSansScenarioEtSeGardeParLeRole`; 25 comment lines removed by the review under STD-5. No production file touched. |

## Verdict in two lines

**Ours**: the corpus carried T40 from a one-line prohibition to three anchored `SPEC` lines, two arbitrations and one
accepted limitation in 14 min 47 s of implementation time, 10 min 38 s of review time and under four minutes of owner
time; what went wrong is conduct against standing rules — `standards/coding.md` never loaded by the implementation (25
JSDoc lines removed by the review), the new question filed in `../../../tests.awawa` for the second time, no baseline
before the first write, the footer cross-check half run by both agents, the wave launched by the non-corpus command.

**The tool's**: awawa 2.7.0 kept every promise it was asked — 6 new `IMPL` anchors resolve on `952a328`, the closure
could not flip without its `CLOSES`, the three decisions carry 2 to 3 `REJECTED` each, and the `GOVERNED_BY` footer
ruled at PR #77 listed the six constraining decisions at its first use on a task — while its two accepted limits recur
for the sixth time at the cost of one command each.

## Part A — Us: method, corpus, conduct

### What went well

| What                                                                                                                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                 |
|-----------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| The wave scope was settled on the tool in 55 s and the incoming blockers read before launch                           | Pilot: `awawa status .` 16:11:09Z, `status TASK --where STATUS!=implemented` 16:11:19Z, `lint --closure` on T40 and T43 16:11:54Z, `context` on both 16:11:55Z, `status OPEN_QUESTION --where STATUS!=superseded` 16:12:04Z — the pair of `@DECISION.LePiedDePageBlockedBySeCroiseAvecStatus` 10 s apart                                                                                                 |
| The owner's time on this PR was under four minutes                                                                    | Launch: one `AskUserQuestion` at 16:13:02Z (three questions in one), agent launched 16:15:54Z, so the answer took at most 2 min 52 s. Review: one comment at 20:49:17Z (« on peut fermer la question comme une limitation acceptée/assumée »), one review submission at 20:49:48Z. Report: « Aucun retour » at launch                                                                                    |
| The implementing agent read the package, the footer and the governing decisions before any write                      | `awawa context @TASK.T40_… --skip reasoning --with-schema` 16:16:17Z, `show` on the four `GOVERNED_BY` decisions 16:16:32Z, `lint --closure` 16:16:37Z, `context @PACKAGE.ui_save_manager` 16:16:44Z, three more `show` 16:16:54Z; first `Write` at 16:20:40Z, a scratch probe                                                                                                                           |
| The task's single prohibition became three falsifiable `SPEC` lines, each anchored on the file that resolves it       | `git show origin/<base>:docs/taches.awawa`: T40 carried 1 `SPEC`, the prohibition anchored on `../../../../packages/core-mapping/src/domain/rules`; commit `2a4115f` adds three `SPEC` with six `IMPL` anchors; `awawa lint --closure @TASK.T40_…`: 4 entities, all `implemented`, `GOVERNED_BY (root, 6)`                                                                                               |
| The hold on the file read was falsified before the PR, and the falsification replays                                  | Agent: `sed -i` neutralising `releaseTheHeldFileReads` 16:24:03Z, `2 failed / 2 passed` on chromium 16:24:27Z, restored 16:24:31Z. Replayed here on `952a328`, chromium: the two end-of-state scenarios fail on `toBeVisible`, the two busy-state scenarios pass, file restored, tree clean                                                                                                              |
| Every ruling faced its alternative                                                                                    | `REJECTED` ×2 on `UnScenarioDUiAsserte…`, ×3 on `UnEtatDeChargement…`, ×3 on `LEtatDAttente…`; the question as first written (`4d75be1`) carried `RECOMMENDATION` with 2 `REJECTED`                                                                                                                                                                                                                      |
| The implementing agent flagged its own weak point as the PR's first thread, and the owner's only comment landed on it | Thread `4007314771` at 16:30:09Z, 91 s after `gh pr create` (16:28:38Z), on `loading-state.e2e.ts:63`, naming the question; the owner's reply at 20:49:17Z is on that thread                                                                                                                                                                                                                             |
| The review examined the recorded recommendation against the build before ruling on it                                 | Review agent: `routes/index.tsx` 20:52:26Z and 20:52:35Z, `Spinner.tsx`, `grep` on `Loading` 20:52:19Z, `ls .output/public/_build/assets` 20:53:23Z — the chunk names are content-hashed, so the request-interception recommendation is a `REJECTED` line of the decision, with that reason. `@DECISION.UneRecommandationEstExamineeAvantLaRatificationEstUneRegleGenerale` applied                      |
| The accepted limitation took the shape the corpus prescribes                                                          | Question `superseded`, decision `implemented` with `CLOSES`, symptom in `RATIONALE` 1, cause in `RATIONALE` 1 (`onMount`), guard as a `SPEC` anchored on `loading-state.e2e.ts`; `awawa refs` on the question: 1 incoming, `CLOSES`. `@DECISION.UneLimitationAccepteeGardeSaMatiereDansLaDecision` followed                                                                                              |
| The three decisions name what they constrain, first PR to do so from the start                                        | `APPLIES_TO_PACKAGE @PACKAGE.ui_save_manager` on all three, `APPLIES_TO_TASK` on T40 (two) and T42 (one); the schema field entered the base in `04e85bf` (PR #77), 46 minutes before this branch was cut                                                                                                                                                                                                 |
| The ruling was in the corpus and pushed before its thread was answered                                                | Commit `340414e` 20:58:48Z, push 20:59:00Z, reply 20:59:15Z                                                                                                                                                                                                                                                                                                                                              |
| The review found the STD-5 breach nobody had flagged                                                                  | Review agent 20:57:02Z: `grep` for `STD-5 \|jsdoc` in the corpus, then « The diff introduces explanatory JSDoc blocks that STD-5 forbids »; commit `952a328` removes 25 lines across the three files, 0 additions                                                                                                                                                                                        |
| The PR body described the head                                                                                        | One `gh pr edit` by the review; `updated_at` 21:00:41Z, after the last commit (20:58:57Z); the body names the review's decision and its three `REJECTED`                                                                                                                                                                                                                                                 |
| The acceptance figures hold on re-measurement                                                                         | On `952a328` in the same worktree, `bun install --frozen-lockfile` (no change), `private:sync` up to date: `bun test` 788 pass / 0 fail / 112 files, `lint:types` 0, `test:ui` 48 passed on chromium, firefox, webkit, `awawa lint --strict .` 0, `awawa fmt --check .` 0, `awawa status .` 244 sites / 0 unresolved / 0 diagnostics; `gh pr checks 78`: 7 pass, `claude` skipping; `MERGEABLE`, `CLEAN` |
| The corpus did not weigh the PR down                                                                                  | 107 corpus lines of 473 changed, 23 % (41 % at #76, 29 % at #71)                                                                                                                                                                                                                                                                                                                                         |
| No accented character reached the corpus                                                                              | 0 in the 107 added lines (`grep -P '[À-ÿ]'` on the diff)                                                                                                                                                                                                                                                                                                                                                 |
| Corpus text by heredoc with `+` continuations lints clean, third PR in a row                                          | Implementation 16:27:08Z and 16:29:31Z, review 20:55:08Z: 0 `L014` in either transcript                                                                                                                                                                                                                                                                                                                  |

### What went wrong

| What                                                                                                                     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Whose fault                                                                                    |
|--------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------|
| The implementation wrote explanatory JSDoc that STD-5 forbids, because it never loaded the file that holds STD-5         | Transcript `af55cd15`: `sed -n` on `testing/index.md`, `testing/tdd-workflow.md`, `ui/index.md`; 0 read of `standards/coding.md`; 0 occurrence of `STD-5`. `~/.claude/CLAUDE.md`, « Creating or changing code → `standards/coding.md` ». Cost: 25 lines, one review commit (`952a328`), found by the review agent and by no thread                                                                                                                                                                                                        | conduct                                                                                        |
| The new question was filed in a domain file, against the placement decision — second time, same file, same agent lineage | `4d75be1` appends the question to `../../../tests.awawa` (16:29:31Z, after `tail -20 docs/tests.awawa && wc -l`); `@DECISION.LeCorpusEstDecoupeParDomaine`, second `DESC`: « les questions ouvertes et les limitations acceptees sont groupees, pas reparties par domaine »; 48 of 57 questions are in `docs/questions.awawa`, 8 in `docs/limitations.awawa`, 1 in `docs/tests.awawa` (`OuVontLesArtefacts…`, PR #75). The review paid for it: `sed -i '417,437d'` on `tests.awawa` 20:53:38Z, re-append to `limitations.awawa` 20:55:08Z | corpus modelling, then conduct                                                                 |
| No baseline was measured before the first write, second report                                                           | First `Write` 16:20:40Z (a scratch probe), first e2e file written before 16:23:42Z; first `lint:types` 16:23:42Z, first `playwright test` 16:23:47Z, first `bun test` 16:25:04Z; `bun install` at 16:16:43Z was the only command run on the untouched tree. The launch prompt names `bun install` « avant le premier test » and no run before the first write                                                                                                                                                                             | conduct                                                                                        |
| The footer cross-check was half run by both agents, third report                                                         | Implementation: `lint --closure` 16:16:37Z, 0 `awawa status` in 31 calls. Review: `lint --closure` 20:51:45Z, `status .`, `status TASK`, `status PACKAGE`, no `status OPEN_QUESTION --where`. T40's footer reads `BLOCKED_BY (rest, 7)`, all seven absent from the live list. The launch prompt names `lint --closure` « pied de page compris » and not the pair; remediation 5 of `2026-09-14-pr-75.md` not applied                                                                                                                      | method, then conduct                                                                           |
| The wave was launched with the command for projects without a corpus                                                     | `/plan-wave-start` at 16:10:43Z; `~/.claude/commands/plan-wave-start.md` contains the word `awawa` 0 times; `awawa-plan-wave-start.md` exists. The pilot did the corpus work anyway (12 `awawa` calls), so the cost is the prompt's gaps above, not a wrong verdict                                                                                                                                                                                                                                                                       | conduct                                                                                        |
| The question was rewritten in place when the review closed it, 21 lines to 5                                             | `4d75be1`: 2 `DESC`, 2 `RATIONALE`, `RECOMMENDATION` with `REF` and 2 `REJECTED`; `340414e`: 1 `DESC`, `STATUS superseded`, `SOURCE`. The recommendation survives only as the first `REJECTED` of the decision; the first `DESC` opened on `a trancher :`, a prose prefix the `RECOMMENDATION` field was declared to replace. Consistent with `@DECISION.UneLimitationAccepteeGardeSaMatiereDansLaDecision`, whose `DESC` says the decision carries the matter and says nothing of the question's text                                    | corpus modelling                                                                               |
| The falsification is recorded without the engine it ran on                                                               | Body: « with the release neutralized, the two end-of-state scenarios fail »; second `RATIONALE` of `@DECISION.UnEtatDeChargementSObserveEnRetenantLaLectureDuFichier`: « les deux scenarios de fin d'etat echouent alors ». The run was `--project=chromium` (16:24:09Z); firefox and webkit were not run neutralised, there or here. `@OPEN_QUESTION.UnChiffreInscritDansLeCorpusPorteLaPorteeDeSaMesure` is open on exactly this                                                                                                        | conduct                                                                                        |
| The worktree was cut from the wrong base again, fifth report                                                             | Agent 16:16:07Z: `merge-base --is-ancestor` « exit=1 »; 16:16:13Z `git checkout -b t40-… origin/refactor/…`. The prompt carried the check-and-recut; the tooling still cuts from the default branch                                                                                                                                                                                                                                                                                                                                       | method                                                                                         |
| Corpus files read with `sed`, `tail`, `head` and `grep` by all three sessions, sixth report                              | Pilot: `sed -n '175,240p'` and `'295,360p'` on `taches.awawa` 16:11:47–48Z after `status TASK`. Implementation: `sed -n 170,205p docs/taches.awawa` 16:25:28Z, two `sed` ranges on `tests.awawa` 16:25:30Z, `tail -20 && wc -l` 16:26:48Z, `sed -n 425,445p` 16:29:36Z — after `context`, seven `show` and `new`. Review: `head -80 docs/limitations.awawa` 20:51:46Z, `tail -20 && wc -l` 20:53:31Z, `sed` on `tests.awawa` 20:53:36Z, `sed -n '178,196p' docs/packages.awawa` 20:57:07Z, `grep "^PACKAGE "` 20:54:50Z                   | conduct; `@OPEN_QUESTION.LaLectureDUnFichierDuCorpusPourYAjouterUneEntiteEstElleLegitime` open |
| The model entities named in `../../../../AGENTS.md` were read by neither agent                                           | Implementation: `show` on T40, the four governing decisions, three package decisions, one test decision, then `show DECISION` and `show OPEN_QUESTION` (the schema entries); review: `show` on the two decisions it applied. 0 `show` of `UneDecisionRetireeNommeSaSuccesseure`, `UneFusionProduitUneSaveOrdinaire`, `UneSaveSansJoueurMeriteUnAvertissement` or `T37_…` in either transcript; `awawa new` ran twice in each                                                                                                              | conduct                                                                                        |
| The `Claude-Session` trailer matches six transcripts, fourth report                                                      | `grep -l session_01RQMo…` over `~/.claude/projects`: six files, two of them in the `agent-applies-to` project directory; `awawa-usage-report.md:70` still says « whose `sessionId` field matches »                                                                                                                                                                                                                                                                                                                                        | method                                                                                         |
| The piloting transcript measures more than this PR, third report                                                         | `739f22f1` launched T40 and T43 in one wave (two `Agent` calls, two task notifications); its 12 min 59 s and 3.35 M cache tokens are upper bounds for T40                                                                                                                                                                                                                                                                                                                                                                                 | report                                                                                         |
| The `awawa-pr-review` command still contradicts the owner's ruling on general rules                                      | `~/.claude/commands/awawa-pr-review.md:139–144`: « A rule the review earned that may belong in the general instructions → an `OPEN_QUESTION` »; `~/.ai/AGENTS.md` Always says the opposite. Not exercised here — no general rule surfaced — so the cost this time is 0                                                                                                                                                                                                                                                                    | method                                                                                         |

### Remediations, by return

| # | Change                                                                                                                                                                                                                        | What it fixes                                                                                                                                                                                                                                                         | Recorded as                                                                                                                                                                                                                                                                                            |
|---|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | `../../../../AGENTS.md`, « Un défaut constaté est une `OPEN_QUESTION` »: name `docs/questions.awawa` as the file a new question is appended to, and `docs/limitations.awawa` as the file an accepted-limitation pair moves to | Two questions in `tests.awawa` across two PRs; a `sed -i` deletion and a re-append by the review; the next agent reads `../../../../AGENTS.md` before any command                                                                                                     | `@OPEN_QUESTION.UneQuestionNouvelleSEcritDansLeFichierDesQuestionsEtAgentsMdLeDit`                                                                                                                                                                                                                     |
| 2 | Ruling to propose: the wave pilot checks before launch that a task carries one falsifiable `SPEC` per deliverable its `DESC` names, and writes the missing ones in the launch commit                                          | T40 was launched with one `SPEC`, a prohibition, and the implementer wrote its three acceptance lines after the code (`2a4115f` at 16:27:34Z, code at 16:25:50Z), so the review checked lines the implementer wrote for itself; T41 and T42 carry 2 `SPEC` each today | `@OPEN_QUESTION.UneTacheLanceeSansSpecParLivrableEcritSesCriteresApresLeCode`                                                                                                                                                                                                                          |
| 3 | The launch prompt of the wave command names the `~/.ai` rows to load — `standards/coding.md` for any code, `testing/index.md` for any test — and the baseline run before the first write                                      | 25 JSDoc lines and one review commit; a baseline missing for the second time                                                                                                                                                                                          | No question: the commands are general, and the owner ruled on PR #71 (09:01:53Z) that a general rule goes through a pull request on `~/.ai`                                                                                                                                                            |
| 4 | The launch prompt names the footer cross-check as the pair `lint --closure` then `status OPEN_QUESTION --where STATUS!=superseded`, as the pilot runs it                                                                      | Seven `superseded` referrers read as `BLOCKED_BY` by two agents; remediation 5 of `2026-09-14-pr-75.md`, still not in the prompt                                                                                                                                      | No question: ruled by `@DECISION.LePiedDePageBlockedBySeCroiseAvecStatus`; the prompt is general                                                                                                                                                                                                       |
| 5 | Write the engine next to the falsification sentence in the decision's `RATIONALE`, and run the neutralised scenario on the three engines once                                                                                 | A corpus figure a reader cannot reproduce on firefox or webkit without knowing it was never tried there                                                                                                                                                               | No new question: `@OPEN_QUESTION.UnChiffreInscritDansLeCorpusPorteLaPorteeDeSaMesure` carries it                                                                                                                                                                                                       |
| 6 | `../../../../AGENTS.md`, « Suivi d'une PR »: name `/awawa-plan-wave-start` as the launch command, next to `/awawa-pr-review` and `/awawa-usage-report`, so the non-corpus command is not the one typed                        | A wave launched by a command that knows nothing of the corpus; the prompt gaps of rows 3 and 4 follow from it                                                                                                                                                         | No question: the sentence already lists the two other commands; a conduct gap against `../../../../AGENTS.md`, « Les commandes `/pr-review-followup` et `/pr-merge-followup` restent pour les projets sans corpus », which does not name the wave command — one line to add, offered in the PR comment |
| 7 | `awawa-usage-report.md:69–72`: drop the `sessionId` sentence, keep « `grep -l 'session_<id>'`, then the worktree name of each agent »                                                                                         | Six files for one trailer                                                                                                                                                                                                                                             | No question: general command, remediation 5 of `2026-09-14-pr-71.md`, still open                                                                                                                                                                                                                       |
| 8 | `awawa-pr-review.md:139–144`: replace the « general rule → `OPEN_QUESTION` » bullet by the `~/.ai/AGENTS.md` ruling                                                                                                           | Not exercised here; remediation 1 of `2026-09-14-pr-76.md`, still open                                                                                                                                                                                                | No question: same ruling                                                                                                                                                                                                                                                                               |

## Part B — The tool

### What went well

| Promise                                                     | Kept | Evidence                                                                                                                                                                                                                                                                                                           |
|-------------------------------------------------------------|------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Constraining decisions listed in the footer (`GOVERNED_BY`) | Yes  | `lint --closure @TASK.T40_…`: `GOVERNED_BY (root, 6)` — the four decisions in force at launch and the two written by this PR; the agent's `show` loop of 16:16:32Z read the four from that line. Schema at `docs/_schema.awawa:68–76`, 78 `APPLIES_TO_*` lines in the corpus; first report to see the field in use |
| Anchor integrity                                            | Yes  | Six `IMPL` anchors added under T40's `SPEC` lines, three under the decisions; 244 sites, 0 unresolved on `952a328`; `L016` fired on nothing because nothing moved                                                                                                                                                  |
| Closure integrity                                           | Yes  | `340414e` flips the question `superseded` and writes `CLOSES` on the decision in the same commit; `awawa refs`: 1 incoming, `CLOSES`                                                                                                                                                                               |
| Forcing the alternative                                     | Yes  | 2, 3 and 3 `REJECTED`; the question's `RECOMMENDATION` carried 2                                                                                                                                                                                                                                                   |
| Schema-correct skeleton                                     | Yes  | `awawa new DECISION Exemple` 16:25:55Z, `new OPEN_QUESTION Exemple` 16:29:15Z (implementation); both again at 20:52:57Z (review), before every append                                                                                                                                                              |
| Format as a gate                                            | Yes  | `fmt` 5 times in the implementation, 3 in the review; 0 `L014` on three heredoc appends with `+` continuations; the corpus diff of the PR touches three files and no line outside the entities written or moved                                                                                                    |
| Selection by field                                          | Yes  | `awawa status TASK --where PR==78 .` selected the task for this report's launch (21:02:44Z) and here                                                                                                                                                                                                               |
| Cheap inventory                                             | Yes  | `status` 9 times across the four sessions; the wave scope settled in 55 s (16:11:09–16:12:04Z)                                                                                                                                                                                                                     |
| A wrong name fails fast and `status` recovers it            | Yes  | Review agent 20:51:37Z: `show @TASK.T40_ScenariosDUiValidationEtChargement` (a guessed name), `status TASK \| grep -i t40` in the same call, the right name at 20:51:45Z — 8 s                                                                                                                                     |

### What went wrong

| Defect                                                                                     | Observation                                                                                                                                                           | Checked against                                                                                                                                      |
|--------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------|
| The `BLOCKED_BY` footer still counts `superseded` referrers, sixth report                  | `lint --closure @TASK.T40_…`: `BLOCKED_BY (rest, 7)`; none of the seven is in `status OPEN_QUESTION --where STATUS!=superseded` (22 live)                             | run on `952a328`; accepted limitation, `@DECISION.LePiedDePageBlockedBySeCroiseAvecStatus`; its guard applied by the pilot in 10 s, by neither agent |
| Referrers named, not expanded, sixth report                                                | Implementation: 3 `context`, 14 `show`; review: 0 `context`, 9 `show`                                                                                                 | transcripts; accepted, `@DECISION.LesReferentsDUnContexteSeLisentParShow`                                                                            |
| Nothing says where a file ends, so an append starts with `tail` and `wc -l`, second report | `tail -20 docs/tests.awawa && wc -l` 16:26:48Z; `tail -20 docs/limitations.awawa; wc -l` 20:53:31Z                                                                    | transcripts; `awawa new` prints and never writes                                                                                                     |
| Nothing holds the placement of an entity by file                                           | The question in `tests.awawa` lints clean under `--strict`; the placement rule lives in a `DESC` of `@DECISION.LeCorpusEstDecoupeParDomaine` that no command enforces | run; by construction — and ours first: part A, remediation 1                                                                                         |

### Remediations

| Proposal                                                                                                                                   | What it would have changed on this PR                                                                                      |
|--------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------|
| `awawa new TYPE Name --into FILE`, appending the skeleton after the last entity of that type in `FILE` (proposed in `2026-09-14-pr-75.md`) | The two `tail`/`wc -l` reads, and, if the target file came from a schema-declared placement, the question in `tests.awawa` |

## Recurring points

### Recurring pain points

| Point                                                                           | Times seen | First raised in                      | Seen again here                                                                                                                                                               | Remediation proposed then                                  | Done?                                                                                                                                                    |
|---------------------------------------------------------------------------------|------------|--------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------|
| The agent reads corpus files for style, orientation or placement — **flagged**  | 6          | `2026-09-12-pr-62-review.md`, part A | Two `sed` by the pilot, four reads by the implementation, five by the review, all after `context`/`show`/`new`                                                                | Model entity per type; the placement ruling asked at #75   | no — `@OPEN_QUESTION.LaLectureDUnFichierDuCorpusPourYAjouterUneEntiteEstElleLegitime` still `specified`; the model entities were not even read this time |
| The `BLOCKED_BY` footer ignores the referrers' `STATUS` (tool) — **flagged**    | 6          | `2026-09-12-pr-62-review.md`, part B | `(rest, 7)` on T40, all `superseded`                                                                                                                                          | Footer split live / suppressed                             | closed as a limitation; the guard applied by one session of four                                                                                         |
| Referrers named, not expanded (tool) — **flagged**                              | 6          | `2026-09-12-pr-62-review.md`, part B | 3 `context` then 14 `show`; 0 `context` then 9 `show`                                                                                                                         | `context --with-referrers`                                 | closed as accepted                                                                                                                                       |
| Instructions stale or against a ruling — **flagged**                            | 6          | `2026-09-12-pr-62-review.md`, part A | `awawa-usage-report.md:70` (`sessionId`, six files for one trailer); `awawa-pr-review.md:139` against `~/.ai/AGENTS.md`; the wave launched by `plan-wave-start.md`, 0 `awawa` | Strip or confront the commands                             | no — three instances, one of them new                                                                                                                    |
| The worktree base trap — **flagged**                                            | 5          | `2026-09-12-pr-62-launch.md`, part A | `exit=1` at 16:16:07Z, recut at 16:16:13Z                                                                                                                                     | Fix in `commands.md` and the launch skills                 | partly — the prompt carries the check-and-recut; the tooling still cuts from the default branch                                                          |
| An entity written in a PR rewritten or removed inside the same PR — **flagged** | 4          | `2026-09-13-pr-72.md`, part A        | The question of `4d75be1` rewritten from 21 lines to 5 and moved by `340414e`; the name and the `superseded` status kept                                                      | Name the temporary reason or hold the decision             | no — here the rewrite follows an accepted-limitation ruling, and no rule says what the closed question keeps                                             |
| A figure stated wider than, or without, its scope — **flagged**                 | 4          | `2026-09-13-pr-72.md`, part A        | « les deux scenarios de fin d'etat echouent alors », run on chromium only, in a `RATIONALE` and in the body                                                                   | State the scope next to every figure                       | no — `@OPEN_QUESTION.UnChiffreInscritDansLeCorpusPorteLaPorteeDeSaMesure` still `specified`                                                              |
| The `Claude-Session` trailer identifies no single session — **flagged**         | 4          | `2026-09-13-pr-72.md`, part A        | Six files for `session_01RQMo…`                                                                                                                                               | Locate by project directory, drop the `sessionId` sentence | partly — `grep -l` plus the worktree name found the files; the sentence remains                                                                          |
| The footer cross-check run by the pilot and not by the agents — **flagged**     | 3          | `2026-09-14-pr-75.md`, part A        | Neither agent ran `status OPEN_QUESTION --where`; the prompt names half the pair                                                                                              | The prompt names the pair (#75, remediation 5)             | no                                                                                                                                                       |
| The piloting transcript measures more than this PR — **flagged**                | 3          | `2026-09-14-pr-71.md`, part A        | Two agents launched from `739f22f1`                                                                                                                                           | none                                                       | no — inherent to a wave; the figures are marked as upper bounds                                                                                          |
| The new question sits in a domain file, against the placement decision          | 2          | `2026-09-14-pr-75.md`, part A        | `../../../tests.awawa` again, moved by the review                                                                                                                             | none recorded then (the `--into` proposal of part B only)  | no — this report opens `@OPEN_QUESTION.UneQuestionNouvelleSEcritDansLeFichierDesQuestionsEtAgentsMdLeDit`                                                |
| The baseline not measured before the first write                                | 2          | `2026-09-14-pr-75.md`, part A        | First run 16:23:42Z, first `Write` 16:20:40Z                                                                                                                                  | none recorded                                              | no — part A, remediation 3                                                                                                                               |
| The journey started with the wrong command                                      | 2          | `2026-09-14-pr-75.md`, part A        | `/plan-wave-start` for a corpus project                                                                                                                                       | The command headers name the corpus root                   | no — part A, remediation 6                                                                                                                               |

Raised before and not seen again, closed by observation: the corpus is a list, not a graph — `GOVERNED_BY (root, 6)` on
T40 and 78 `APPLIES_TO_*` lines answer the defect as stated at #62 (« nothing retrieves the decisions constraining X »);
84 of 129 decisions still have no incoming edge (65 %, 70 % at #76), which is the retrofit
`@OPEN_QUESTION.UneDecisionNommeCeQuElleGouverneParUnChampAppliesTo` (`superseded`, PR #77) left to the configuration
review; a rule bound for `~/.ai` recorded as a corpus entity first — no such rule here; corpus text by heredoc with `+`
continuations — 0 `L014` on three appends; the PR body describing an abandoned state — rewritten after the last commit;
the private clone read unsynced — synced by the review at 20:57:58Z, read by nobody; the review pilot answering without
a call to the PR — one owner message, one agent pass; a decision reversed by the owner inside the PR — the only owner
ruling closed a question the implementation had opened for that purpose.

### Recurring strengths

| Mechanism                                                                                 | Times seen | First noted in                       | Held again here                                                                                                                                                         |
|-------------------------------------------------------------------------------------------|------------|--------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Incoming blockers known before code, from the footer — **established**                    | 7          | `2026-09-12-pr-62-launch.md`, part A | Pilot 16:11:54Z, agent 16:16:37Z, first `Write` 16:20:40Z                                                                                                               |
| Required `REJECTED` forcing the alternative — **established**                             | 7          | `2026-09-12-pr-62-launch.md`, part A | 2 to 3 per decision; the full rule message, the multi-failure fixture and the request interception named as what lost                                                   |
| `L026`: no closure without naming what closed it — **established**                        | 7          | `2026-09-12-pr-62-launch.md`, part A | One `CLOSES`, one `superseded`, one incoming edge                                                                                                                       |
| `L016` anchor integrity — **established**                                                 | 7          | `2026-09-12-pr-62-launch.md`, part A | Nine anchors added, 0 unresolved; not exercised on a move                                                                                                               |
| The agent measures rather than asserts — **established**                                  | 7          | `2026-09-12-pr-62-launch.md`, part A | The hold neutralised and restored in 28 s with `2 failed`; the full suite after each commit (16:25:04Z, 16:27:39Z, 16:27:47Z) — held, minus the baseline and the engine |
| Cheap inventory through `status` — **established**                                        | 6          | `2026-09-12-pr-62-review.md`, part B | 9 calls; the wave scope in 55 s                                                                                                                                         |
| Task `SPEC` lines handed as acceptance criteria — **established**                         | 6          | `2026-09-12-pr-62-launch.md`, part A | Prompt: « Ses lignes `SPEC` sont tes critères d'acceptation » — held on one line; the other three were written by the agent, part A remediation 2                       |
| The ruling recorded and pushed before it is used — **established**                        | 5          | `2026-09-12-pr-65.md`, part A        | Push 20:59:00Z, reply 20:59:15Z                                                                                                                                         |
| The footer cross-check within seconds — **established**                                   | 5          | `2026-09-13-pr-72.md`, part A        | 10 s for the pilot; not run by the agents                                                                                                                               |
| The corpus does not weigh the PR down — **established**                                   | 5          | `2026-09-12-pr-62-review.md`, part A | 23 %                                                                                                                                                                    |
| The agent flags its own weak point as a review thread — **established**                   | 4          | `2026-09-13-pr-72.md`, part A        | Thread `4007314771`, 91 s after the PR; the owner's only comment is on it                                                                                               |
| The PR body described the head — **established**                                          | 4          | `2026-09-12-pr-65.md`, part A        | `gh pr edit` after the last commit                                                                                                                                      |
| The acceptance figures hold on re-measurement — **established**                           | 4          | `2026-09-13-pr-72.md`, part A        | 788 / 0 / 112, 48 on three engines, 244 sites, 7 CI pass                                                                                                                |
| The private clone refreshed before it is read — **established**                           | 4          | `2026-09-13-pr-72.md`, part A        | `private:sync` 20:57:58Z, before the checks; nothing was read from it                                                                                                   |
| The ruling faced the code before it was asked or taken, findings stated — **established** | 3          | `2026-09-12-pr-65.md`, part A        | `routes/index.tsx`, `Spinner.tsx`, the build assets read 20:52–20:53Z; the findings in thread `4007314771` under « Two things checked against the code »                |
| No accented character in the corpus diff                                                  | 2          | `2026-09-14-pr-75.md`, part A        | 0 in 107 lines                                                                                                                                                          |
| The three decisions name what they constrain, `GOVERNED_BY` read from the footer          | 1          | this report                          | `APPLIES_TO_*` on all three; `(root, 6)`                                                                                                                                |
| The recorded recommendation examined against the build and rejected with its reason       | 1          | this report                          | `ls .output/public/_build/assets` 20:53:23Z → « dont les noms sont hashes » in the decision's first `REJECTED`                                                          |

Noted before and not seen holding here: the model entity read by `show` — 0 `show` of the four entities
`../../../../AGENTS.md` names, in either agent; the reviewer retracts or corrects in plain terms — nothing to retract; a
rule bound for `~/.ai` leaves the corpus — none surfaced; entities composed in files and spliced — heredocs again, at 0
`L014`; RED by the agent, GREEN by a sub-agent — no behaviour changed; `show FILE:LINE` — not used; the same agent
resumed by `SendMessage` — one pass; `fmt` then `git diff --stat` on the corpus, no cross-branch conflict —
`git diff --stat docs/` at 16:29:44Z, and T43 (`a9eeedfd`) ran in the same wave: `MERGEABLE`/`CLEAN` at the head, but
T43's branch was not checked here.

## Follow-up on the previous report's remediations

| Remediation                                                                                                  | Done? | Evidence                                                                                                                                                                  |
|--------------------------------------------------------------------------------------------------------------|-------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1. `awawa-pr-review.md:139–144` replaced by the `~/.ai/AGENTS.md` ruling                                     | no    | The bullet stands; not exercised on this PR                                                                                                                               |
| 2. A tool question handed to the reporter as the extra instruction, never as a thread                        | n/a   | No tool question on this PR; the launch carried no extra instruction                                                                                                      |
| 3. The review pilot re-lists the threads on every owner message                                              | n/a   | One owner message, `/awawa-pr-review 78` 50 s after it                                                                                                                    |
| 4. Every corpus figure with its scope — `@OPEN_QUESTION.UnChiffreInscritDansLeCorpusPorteLaPorteeDeSaMesure` | no    | Question `specified`; one new figure without its engine (part A)                                                                                                          |
| 5. `APPLIES_TO` with `CONVERSE GOVERNED_BY` on `DECISION`                                                    | yes   | `04e85bf` (PR #77), `docs/_schema.awawa:68–76`; `@OPEN_QUESTION.UneDecisionNommeCeQuElleGouverneParUnChampAppliesTo` `superseded`; used by all three decisions of this PR |
| 6. `awawa-usage-report.md`: drop the `sessionId` sentence                                                    | no    | Lines 70 and 72 unchanged; six files for one trailer                                                                                                                      |

## Appendix

### Measurements

| Session                                                                                                               | Active time                      | User turns | Tool calls                                          | awawa calls by subcommand                    | Tokens read from cache | Tokens written |
|-----------------------------------------------------------------------------------------------------------------------|----------------------------------|------------|-----------------------------------------------------|----------------------------------------------|------------------------|----------------|
| piloting `739f22f1` = `session_01RQMo7ZgAYSciBGeduFs1uL` (wave launch, T40 and T43; upper bound)                      | 12 min 59 s (16:10:35–16:35:32Z) | 7          | 23 (Bash 19, Agent 2, Skill 1, AskUserQuestion 1)   | 12: status 4, context 3, show 3, lint 2      | 3,348,419              | 69,984         |
| agent `af55cd15` (T40 implementation)                                                                                 | 14 min 47 s (16:15:57–16:30:44Z) | 2          | 106 (Bash 92, Write 10, Edit 2, Read 1, Skill 1)    | 31: show 14, lint 7, fmt 5, context 3, new 2 | 18,626,615             | 56,234         |
| piloting `6fd9232d` = `session_01VFt16vNFaYZWcHd9QVSrh4` (review; includes the launch of this report at 21:02–21:03Z) | 13 min 16 s (20:50:07–21:03:23Z) | 5          | 10 (Bash 7, Agent 2, AskUserQuestion 1)             | 1: status 1                                  | 1,089,687              | 9,632          |
| agent `a1c24df5` (review)                                                                                             | 10 min 38 s (20:50:30–21:01:08Z) | 2          | 71 (Bash 62, Edit 4, Read 3, Skill 1, ToolSearch 1) | 23: show 9, lint 5, status 4, fmt 3, new 2   | 10,337,331             | 34,815         |
| total                                                                                                                 | 51 min 40 s                      | 16         | 210                                                 | 67                                           | 33,402,052             | 170,665        |

| Corpus lines changed                                              | PR lines changed | Share |
|-------------------------------------------------------------------|------------------|-------|
| 107 (`limitations.awawa` 41, `tests.awawa` 52, `taches.awawa` 14) | 473 in 9 files   | 23 %  |

Owner's time: one `AskUserQuestion` at launch (16:13:02Z, answered by 16:15:54Z), one review comment (20:49:17Z), one
review submission (20:49:48Z), one « Aucun retour » at the report's launch. PR created 16:28:38Z, signalled ready 21:00:
41Z: 4 h 32 min wall clock, of which 4 h 19 min waiting for the owner's comment.

### Claims checked

| Claim                                                                                                                                                         | Check                                                                                                                                                                          | Verdict                                                                |
|---------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------|
| Body: « T40 carried a single acceptance line, and it was a prohibition »                                                                                      | `git show origin/<base>:docs/taches.awawa`, T40 block: 1 `SPEC`, « aucun scenario ne rejoue une regle… »                                                                       | confirmed                                                              |
| Body: « Three `SPEC` lines now say what the task has always asked, each anchored on the scenario file that resolves it, and the task moves to `implemented` » | Corpus diff: 3 `SPEC`, 6 `IMPL`; `awawa status TASK --where PR==78`: `implemented`                                                                                             | confirmed                                                              |
| Body: « Three decisions … all `implemented`, all naming the package and the task they constrain »                                                             | `APPLIES_TO_PACKAGE` and `APPLIES_TO_TASK` on all three; `STATUS implemented` on all three                                                                                     | confirmed                                                              |
| Body: « `routes/index.tsx` raises `isReady` in `onMount` »                                                                                                    | `routes/index.tsx:29–30`: `createSignal(false)`, `onMount(() => setIsReady(true))`; line 53 `fallback={<p …>{displayRouteLoadingLabel}</p>}`                                   | confirmed                                                              |
| Body and thread: « `displayRouteLoadingLabel` and `spinnerLoadingLabel` are both `Loading...`, in two message modules »                                       | `messages/displayRouteMessages.js:1` and `messages/spinnerMessages.js:1`, both `'Loading...'`                                                                                  | confirmed                                                              |
| Thread: « the fallback is a `p` with no role, the busy indicator a `span role="status"` »                                                                     | `routes/index.tsx:53`; `Spinner.tsx:9`                                                                                                                                         | confirmed                                                              |
| Thread: « the delivered scenarios already locate it by `getByRole('status')` »                                                                                | 5 occurrences in `e2e/*.e2e.ts`, 0 `getByText('Loading`                                                                                                                        | confirmed                                                              |
| Body: « with the release neutralized, the two end-of-state scenarios fail »                                                                                   | Transcript 16:24:27Z `2 failed / 2 passed`, chromium; replayed here, chromium: same two fail on `toBeVisible`                                                                  | confirmed on chromium; unchecked on firefox and webkit, there and here |
| Body: `bun test` 788, `lint:types` 0, `audit:quality` 0, `test:ui` 48, `awawa fmt`/`lint --strict` 0                                                          | Re-run on `952a328`: 788 / 0 / 112; 0; 48 on three engines; 0 / 0. `audit:quality` not re-run here; the review agent's run at 20:58:13Z and the `fallow`/`guards` CI jobs pass | confirmed, `audit:quality` by CI                                       |
| Body: « Both fixtures are generated by `createFakeSaveContent` »                                                                                              | `fixtures/README.md:61–72` carries the recipe; `negative-gauge_invalid.json` line 5 `"playerGaugeToxic":-1.0`; recipes not replayed here                                       | unchecked                                                              |
| Body: « No production file is touched by this branch »                                                                                                        | `git diff --stat`: 9 files, all under `../../../../packages/ui-save-manager/e2e` or `docs/`                                                                                    | confirmed                                                              |
| Thread: « The pair moved from `../../../tests.awawa` to `docs/limitations.awawa` »                                                                            | `340414e` diff: 21 lines removed from `tests.awawa`, 41 added to `limitations.awawa`                                                                                           | confirmed                                                              |
| Thread: « the production build names its chunks by content hash »                                                                                             | Review agent 20:53:23Z listed `.output/public/_build/assets`; not re-listed here                                                                                               | unchecked                                                              |
| Readiness comment: « checks green »                                                                                                                           | `gh pr checks 78`: 7 pass, `claude` skipping; `MERGEABLE`, `CLEAN`                                                                                                             | confirmed                                                              |
| `2026-09-14-pr-76.md`, remediation 5: `APPLIES_TO` field to declare                                                                                           | `docs/_schema.awawa:68–76`; PR #77                                                                                                                                             | done, confirmed                                                        |
