# 2026-09-12 — PR #65 T37 (part B) — the section controllers take the validated text

| Field                                          | Value                                                                                                                                                                                                                                                                                                      |
|------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Author of this report**                      | Claude Fable 5.1, in a session with no memory of the work it judges.                                                                                                                                                                                                                                       |
| **Model that drove the sessions under review** | Claude Fable 5.1 (the `Co-Authored-By` trailer of the four commits of #65 and of the one commit of #66).                                                                                                                                                                                                   |
| **Sessions measured**                          | `session_01JFnUY7rRLaxKhW7EMMZpV8` (piloting, cut at 22:42:00Z, the launch of the first reporter), its background agents `ada79f01` (implementation), `a1a6c8f8` (review follow-up), `ae42623c` (close-out, PR #66) and `aba4495d` (first reporter, aborted).                                              |
| **Delivered**                                  | the five section controllers of `core-mapping` take the validated save text through `createSaveSectionsReader`, `LoadSaveFileViewModel` loses its `sections` field, the UI keeps the text in a signal; one decision recorded and one question closed (#65), both entities promoted to `implemented` (#66). |

## Verdict in two lines

**Ours**: the method ran the whole journey — ruling, implementation, review, promotion — in 41 minutes of wall time with
100 seconds of user arbitration, and the ruling faced the code before it was asked; but every structural remediation of
the previous report is still undone, so the closed question keeps its prose recommendation, the task keeps its five
closed blockers in its footer, the new decision adds a directory anchor, and an `implemented` decision now carries a
sentence that #65 made false.

**The tool's**: awawa 2.7.0 kept every integrity promise it was asked (19 anchors, one `CLOSES`, one required
`REJECTED`, `lint --strict` at 0 on every step) and repeated its two retrieval defects: the `BLOCKED_BY` footer lists
five `superseded` questions as if live on T37 and one on T32, and `diff` had to be fed a hand-copied directory to
compare two revisions.

## Part A — Us: method, corpus, conduct

### What went well

| What                                                                 | Evidence                                                                                                                                                                                                                                                                                                                             |
|----------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| The ruling faced the code before it was asked                        | Piloting transcript: `awawa status DECISION` at 22:00:32Z, four `cat`/`grep` reads of `compositionRoot.ts`, the five controllers, `useLoadSaveFile.ts`, `useSectionViewModels.ts` and the presenter between 22:01:07Z and 22:01:52Z, then one `AskUserQuestion` at 22:02:29Z — remediation 5 of `2026-09-12-pr-62-review.md` applied |
| Arbitration was no longer the bottleneck                             | One question, answered at 22:04:09Z: 100 seconds of user time, against 32 minutes and 8 questions on #62                                                                                                                                                                                                                             |
| The ruling was recorded before the code                              | Commit `c9fd4516` (22:08:51Z) writes `@DECISION.LesControleursDeSectionRecoiventLeTexteValide` and flips `@OPEN_QUESTION.FormeDuContratControleurVersUi` to `superseded`; the first code commit `eafa45a1` follows at 22:10:47Z                                                                                                      |
| The decision names the decision it partly replaces                   | A nested `REF @DECISION.UneFusionProduitUneSaveOrdinaire` under its fourth `DESC` — the writing discipline `2026-09-12-pr-62-launch.md` asked for                                                                                                                                                                                    |
| A stale figure in the corpus was caught by measurement, not repeated | The question's `RATIONALE` carried "2 ms"; the agent measured 11.5 ms per parse on a 2.02 MB save (20 runs) and recorded it in the decision's `RATIONALE`; reproduced here at 11.55 ms                                                                                                                                               |
| The agent measured before it changed anything                        | `bun merge` baseline at 22:07:16Z, before the first `Write` at 22:09:23Z; two md5 sums identical to the reference before and after                                                                                                                                                                                                   |
| The PR body described the head                                       | The review agent reread the body against the full diff and found nothing to rewrite; the `commands.md` rule born on PR #61 held                                                                                                                                                                                                      |
| The pilot answered the user in artefact terms                        | 22:21:49Z, to « tu peux me rappeler ce que cette PR résout ? »: the ViewModel, the signal, the five controllers, the parse cost — one attempt, against three on #62                                                                                                                                                                  |
| The corpus did not weigh the PR down                                 | 39 corpus lines out of 212 changed in #65, 18 %; 4 out of 4 in #66                                                                                                                                                                                                                                                                   |
| The whole journey fits one evening                                   | Wave launch 22:00:17Z, PR #65 created 22:13:16Z, merged 22:26:21Z, PR #66 created 22:29:28Z, merged 22:41:15Z: 41 minutes, four sessions, 21.3 M tokens read from cache, 112 k written                                                                                                                                               |

### What went wrong

| What                                                                                    | Evidence                                                                                                                                                                                                                                                                                                                                          | Whose fault                                                                                   |
|-----------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------|
| An `implemented` decision now states a fact #65 made false, and nothing marks it        | The third `DESC` of `@DECISION.UneFusionProduitUneSaveOrdinaire` says the load ViewModel and the section controllers carry a `SaveSections`; since `f2e34eb` they carry none. The new decision's `REF` is an outgoing edge from the new entity; a `context` that reaches the old one reads the sentence with no flag, and `lint --strict` exits 0 | corpus modelling, then schema: `SUPERSEDES` is whole-entity, there is no partial supersession |
| One task for two pull requests, third time                                              | `awawa lint --closure @TASK.T37_…` prints `BLOCKED_BY (root, 5)` — all five `superseded`; `PR "65"` appended next to `PR "62"`; the launch prompt had to list by hand « the two lines still open » because the entity could not say which of its 14 `SPEC` lines part B owed                                                                      | corpus modelling                                                                              |
| The closed question still carries its recommendation as prose                           | `@OPEN_QUESTION.FormeDuContratControleurVersUi`, second `DESC` starts with `recommandation :`; 14 such prefixes remain in `../../../questions.awawa`; `docs/_schema.awawa` declares no `RECOMMENDATION` field                                                                                                                                     | schema                                                                                        |
| The new decision adds a directory anchor                                                | `IMPL "packages/core-mapping/src/controllers"` under its first `SPEC`; 44 of the 180 `IMPL` anchors of the corpus point at a directory (measured with `os.path.isdir` on the merged base), on which `L016` can never fire                                                                                                                         | schema                                                                                        |
| The examination happened but its findings were not stated                               | Between the last code read (22:01:52Z) and the question (22:02:29Z) the pilot wrote no text; the findings live only in the two option descriptions, and the first option is still labelled « (Recommended) ». `~/.claude/CLAUDE.md` asks to « state what the examination found »                                                                  | conduct                                                                                       |
| The review step was a 2 min 33 s agent establishing that there was nothing to do        | `gh api …/pulls/65/reviews` returns 0; 0 threads; the `claude-review` CI job ended with « No buffered inline comments »; the review agent ran 26 `Bash` calls and 2.78 M cache tokens to confirm this, then posted « review addressed » on a PR that had no review                                                                                | method                                                                                        |
| The close-out attributes the closure to a review that wrote nothing                     | PR #66 body: « already `superseded` by the review of #65 ». The flip is in `c9fd4516`, the launch commit, 22:08:51Z; the review agent reports « Entities written or promoted: none »                                                                                                                                                              | report                                                                                        |
| The corpus stays a list rather than a graph                                             | 74 of 91 decisions have no incoming edge (77 of 84 on #62); 13 `REF` lines in 2,535 corpus lines (7 in 2,282); the new decision has 0 incoming                                                                                                                                                                                                    | corpus modelling                                                                              |
| A stale instruction survived the corpus move                                            | `~/.claude/commands/awawa-pr-merged.md`, line 99: « Push the private context repository. A promotion that is not pushed is a promotion the next worktree does not see » — the promotion went through PR #66 in the public repository; the agent wrote « The private repository was not touched » and lost nothing this time                       | method                                                                                        |
| The worktree base trap fired twice more                                                 | The implementing agent: « the worktree had been cut from `master` … I recreated the branch »; this reporter's worktree was at `576001d` on `master` before it re-cut its branch. The launch prompts now carry the check-and-recut instruction, the tooling still defaults to the default branch                                                   | method                                                                                        |
| The agent read corpus files for style                                                   | `sed -n 100,160p docs/packages.awawa`, `sed -n 155,175p docs/questions.awawa`, `sed -n 140,215p docs/fusion.awawa`, `sed -n 30,60p docs/limitations.awawa`, then `head -12` on all twelve files — after one `awawa context` and one `awawa new`                                                                                                   | method                                                                                        |
| The first usage-report launch died on a session rate limit and left a branch in the way | Agent `aba4495d`: 3 min 02 s, 31 `Bash` calls, 2.48 M cache tokens, last message « You've hit your session limit »; its empty worktree kept the branch `docs/usage-report-pr-65` checked out, so the relaunch had to take another name                                                                                                            | method                                                                                        |

### Remediations, by return

| # | Change                                                                                                                                                                                                                                                                                 | What it fixes                                                                                                       | Recorded as                                                                                                                                                                                                                                                    |
|---|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | Ruling to propose: when a decision replaces one `DESC` of an `implemented` decision, that `DESC` is deleted in the same commit and the new decision's `REF` becomes the trace; otherwise the old decision is superseded whole by a `:v2`                                               | The false sentence in `@DECISION.UneFusionProduitUneSaveOrdinaire`, and every next partial replacement              | `@OPEN_QUESTION.UnRemplacementPartielDUneDecisionImplementeeNEstPasMarque`                                                                                                                                                                                     |
| 2 | `/awawa-pr-review`: one `gh` call in the piloting session — reviews, threads, comments — and no agent when all three are zero; the readiness comment then says « no review feedback » rather than « review addressed »                                                                 | 2 min 33 s, 2.78 M tokens and one wrong sentence on this PR                                                         | `@OPEN_QUESTION.LaRevueSansRetourLanceQuandMemeUnAgent`                                                                                                                                                                                                        |
| 3 | Ruling to propose, already proposed twice: a task that ships in several pull requests is one entity per pull request (`T37_…:partB` with `AFTER` the first). It contradicts the `DESC` of `SCHEMA TASK`, « fusionne en une ou plusieurs pull requests », which the ruling would revise | The hand-written « two lines still open » in the launch prompt and the `(root, 5)` footer; flagged at three reports | `@OPEN_QUESTION.UneTacheLivreeEnPlusieursPrEstUneEntiteParPr`                                                                                                                                                                                                  |
| 4 | `FIELD RECOMMENDATION` with a nested required `REJECTED` on `OPEN_QUESTION`, and a one-off conversion of the 14 prose prefixes                                                                                                                                                         | Proposed by both previous reports; the question closed here would have carried its alternative as a field           | `@OPEN_QUESTION.LaRecommandationDUneQuestionEstUnChampAvecSonRejet`                                                                                                                                                                                            |
| 5 | Forbid a directory anchor under `SPEC` in `../../../_schema.awawa`                                                                                                                                                                                                                     | The 45th directory anchor; 44 unfalsifiable ones                                                                    | `@OPEN_QUESTION.UneAncreDeRepertoireSousSpecEstInterdite`                                                                                                                                                                                                      |
| 6 | Before an `AskUserQuestion` that ratifies, one sentence per file opened, saying what it showed; drop the « (Recommended) » label when the description already says why                                                                                                                 | The silent examination of 22:01–22:02                                                                               | No question: already ruled by `@DECISION.UneRecommandationEstExamineeAvantLaRatificationEstUneRegleGenerale` and by the « state what the examination found » clause of `~/.claude/CLAUDE.md`; this is a conduct gap against a standing rule, not an open point |
| 7 | Delete line 99 of `awawa-pr-merged.md` (or rewrite it as « push the promotion PR »)                                                                                                                                                                                                    | The last known project fact in the command that the corpus move made false                                          | `@OPEN_QUESTION.LaCommandeDeFusionOrdonneEncoreUnPushDuDepotPrive`                                                                                                                                                                                             |
| 8 | `/awawa-usage-report`: `git branch -D` an existing `docs/usage-report-pr-<N>` that has no commit over the base before cutting, or suffix the name                                                                                                                                      | The relaunch collision                                                                                              | `@OPEN_QUESTION.LeRelancementDUnRapportDUsageButeSurSaBranche`                                                                                                                                                                                                 |
| 9 | A `docs/_example.awawa` or one model entity per type                                                                                                                                                                                                                                   | Five `sed`/`head` reads of corpus files for style, second report in a row                                           | `@OPEN_QUESTION.UnModeleDEntiteEviteLaLectureDesFichiersDuCorpus`                                                                                                                                                                                              |

All questions are appended to `../../../questions.awawa`, not to a domain file: `@DECISION.LeCorpusEstDecoupeParDomaine`
groups the open questions in one file, and a project decision outranks the command's per-domain placement.

## Part B — The tool

### What went well

| Promise                            | Kept | Evidence                                                                                                                              |
|------------------------------------|------|---------------------------------------------------------------------------------------------------------------------------------------|
| Anchor integrity                   | Yes  | 19 `IMPL` anchors on the two promoted entities, `lint --strict` 0 diagnostics on `a5f3bd7`; `L016` ran on every commit of the journey |
| Closure integrity                  | Yes  | The question could not go `superseded` without the decision's `CLOSES`; `refs` shows exactly one incoming edge on it                  |
| Forcing the alternative            | Yes  | The required `REJECTED` made the decision name the opaque-identifier registry and why it loses (state in a stateless package)         |
| Finding incoming blockers          | Yes  | The pilot and the agent both read the footer before writing; `lint --closure` ran 3 times in the piloting session                     |
| Cheap inventory                    | Yes  | `awawa status DECISION` before the question (pilot) and before writing (agent); `status` called 15 times across the four sessions     |
| Schema-correct skeleton            | Yes  | `awawa new` once, by the agent, before appending the decision                                                                         |
| Entity-level review of a promotion | Yes  | `awawa diff` at close-out: « 2 changed, 0 added, 0 removed »                                                                          |
| Format as a gate                   | Yes  | `fmt --check` exit 0 at review; `fmt` then `lint --strict` exit 0 at every hand-off                                                   |

### What went wrong

| Defect                                                                 | Observation                                                                                                                                                                                                                                                                                        | Checked against                                                                                     |
|------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------|
| The `BLOCKED_BY` footer ignores the referrers' `STATUS`, second report | `lint --closure @TASK.T37_…`: `BLOCKED_BY (root, 5)`, all five `superseded`; `lint --closure @TASK.T32_…`: `BLOCKED_BY (root, 1): @OPEN_QUESTION.FormeDuContratControleurVersUi`, `superseded`, while the pilot told the user « T32 is also freed by the now-closed controller contract question » | run on the merged base; manual §closure: reached suppressed entities are listed, nothing marks them |
| Referrers are named, not expanded                                      | The agent chained six `awawa show` after its one `awawa context` to read the question and the neighbouring decisions                                                                                                                                                                               | agent transcript, 22:06:03Z–22:06:16Z                                                               |
| `diff` compares two paths, never two revisions                         | The close-out agent ran `cp -r docs …/docs-before` before editing to be able to run `awawa diff` afterwards                                                                                                                                                                                        | agent transcript, 22:28:58Z; manual: `awawa diff OLD NEW`                                           |
| A partial contradiction between two entities is invisible              | `UneFusionProduitUneSaveOrdinaire` (implemented) and `LesControleursDeSectionRecoiventLeTexteValide` (implemented) disagree on what the ViewModel carries; 0 diagnostics                                                                                                                           | run; the schema has no partial supersession, so this defect is sent back to part A, remediation 1   |

### Remediations

| Proposal                                                                                               | What it would have changed on this PR                           | Recorded as                                                          |
|--------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------|----------------------------------------------------------------------|
| Footer split by gating: `BLOCKED_BY (root, 0 live, 5 suppressed)`, or omit `GATE suppressed` referrers | T37 would read as unblocked and T32 as freed, as the pilot said | `@OPEN_QUESTION.LePiedDePageBlockedByIgnoreLeStatutDesReferents`     |
| `awawa diff --git <rev> [PATH]`, reading the old side from the repository                              | No `cp -r docs` at close-out                                    | `@OPEN_QUESTION.AwawaDiffNeCompareQueDeuxRepertoires`                |
| `context --with-referrers` to expand the entities named in the footer                                  | One call instead of seven for the agent                         | `@OPEN_QUESTION.LesReferentsDUnContexteSontNommesSansEtreDeveloppes` |

## Recurring points

### Recurring pain points

| Point                                                                    | Times seen | First raised in                               | Seen again here                                                                                                | Remediation proposed then                              | Done?                                                                         |
|--------------------------------------------------------------------------|------------|-----------------------------------------------|----------------------------------------------------------------------------------------------------------------|--------------------------------------------------------|-------------------------------------------------------------------------------|
| One task for two pull requests, stale block count — **flagged**          | 3          | `2026-09-12-pr-62-launch.md`, part A          | `BLOCKED_BY (root, 5)` all `superseded`; two `PR` lines; open `SPEC` lines listed by hand in the launch prompt | Split into one entity per PR (`:v2` for part B)        | no — T37 is now `implemented`, the rule for the next split task is unrecorded |
| Recommendation hidden in `DESC` prose, no `REJECTED` on it — **flagged** | 3          | `2026-09-12-pr-62-launch.md`, part A (schema) | The question closed here still opens on `recommandation :`; 14 prefixes in `questions.awawa`; schema unchanged | `FIELD RECOMMENDATION` with nested required `REJECTED` | no                                                                            |
| Directory anchors on which `L016` cannot fire                            | 2          | `2026-09-12-pr-62-review.md`, part A          | One added by the new decision; 44 of 180                                                                       | Forbid by schema under `SPEC`                          | no                                                                            |
| The corpus is a list, not a graph                                        | 2          | `2026-09-12-pr-62-review.md`, part A          | 74 of 91 decisions without incoming edge; the new decision has none                                            | `APPLIES_TO` with `CONVERSE GOVERNED_BY` on `DECISION` | no                                                                            |
| The agent reads corpus files for style                                   | 2          | `2026-09-12-pr-62-review.md`, part A          | Four `sed -n` ranges and `head -12` on twelve files after one `context`                                        | A model entity per type or `docs/_example.awawa`       | no                                                                            |
| Instructions stale since the corpus moved                                | 2          | `2026-09-12-pr-62-review.md`, part A          | `awawa-pr-merged.md` line 99 still orders a push of the private repository                                     | Strip project facts from the commands                  | no — cost was zero this time                                                  |
| Worktree base trap                                                       | 2          | `2026-09-12-pr-62-launch.md`, part A          | Fired for the implementing agent and for this reporter                                                         | Fix in `commands.md` and the launch skills             | partly — the prompts carry the check-and-recut; the tooling does not          |
| Footer ignores referrers' `STATUS` (tool)                                | 2          | `2026-09-12-pr-62-review.md`, part B          | `(root, 5)` on T37, `(root, 1)` on T32, all `superseded`                                                       | Footer split live / suppressed                         | no — awawa still 2.7.0                                                        |
| Referrers named, not expanded (tool)                                     | 2          | `2026-09-12-pr-62-review.md`, part B          | Six `show` after one `context`                                                                                 | `context --with-referrers`                             | no                                                                            |

Raised before and not seen again, closed by observation: bulk ratification offered first — one question, code read
first, 100 seconds; the standing contradiction not read — the agent measured the stale 2 ms figure instead of repeating
it; arbitration as the bottleneck — 1 question against 8; explaining in corpus vocabulary — one attempt in artefact
terms at 22:21:49Z; the usage report grading its own author —
`@DECISION.UnRapportDUsageEstEcritApresChaqueFusionParUneSessionIndependante` and this file; `context` empty for a
`PACKAGE` — not exercised, the agent asked for the `TASK`; `awawa lint --help` as a usage error — not attempted; the
permission classifier refusing a read-only sizing command — not observed in the four sessions measured.

### Recurring strengths

| Mechanism                                                                              | Times seen | First noted in                       | Held again here                                                                                                    |
|----------------------------------------------------------------------------------------|------------|--------------------------------------|--------------------------------------------------------------------------------------------------------------------|
| Incoming blockers known before code, from the footer — **established**                 | 3          | `2026-09-12-pr-62-launch.md`, part A | Pilot: 3 `lint --closure`; agent: footer read at 22:06:03Z before the first `Write`                                |
| Required `REJECTED` forcing the alternative — **established**                          | 3          | `2026-09-12-pr-62-launch.md`, part A | The decision rejects the opaque identifier with its cost named                                                     |
| `L026`: no closure without naming what closed it — **established**                     | 3          | `2026-09-12-pr-62-launch.md`, part A | `CLOSES` on the decision, one incoming edge on the question                                                        |
| `L016` anchor integrity — **established**                                              | 3          | `2026-09-12-pr-62-launch.md`, part A | 19 anchors resolve on `a5f3bd7`; no move this time, so not exercised on a move                                     |
| The agent measures rather than asserts — **established**                               | 3          | `2026-09-12-pr-62-launch.md`, part A | 11.5 ms over 20 runs against a 2 ms claim; baseline md5 before the first edit                                      |
| `fmt` then `git diff --stat` on the corpus, no cross-branch conflict — **established** | 3          | `2026-09-12-pr-62-launch.md`, part A | 22:12:25Z: `awawa fmt . && awawa lint --strict . ; git diff --stat`; three corpus files touched, no reformat noise |
| Task `SPEC` lines handed verbatim as acceptance criteria                               | 2          | `2026-09-12-pr-62-launch.md`, part A | The launch prompt quotes the two open `SPEC` lines; the agent's report answers them one by one                     |
| Cheap inventory through `status DECISION`                                              | 2          | `2026-09-12-pr-62-review.md`, part B | Run by the pilot before its question and by the agent before writing                                               |
| The corpus does not weigh the PR down                                                  | 2          | `2026-09-12-pr-62-review.md`, part A | 18 % of the lines of #65                                                                                           |

Noted before and not seen holding here: a standing decision superseded rather than contradicted — this time the
contradiction is partial and only `REF`ed (part A, first row); gating by `STATUS` cheapening a retrofit — no retrofit;
the user refusing a bulk ratification — no bulk offered; `--overlay` for a dry-run edit — not used.

## Follow-up on the previous report's remediations

The previous report is `2026-09-12-pr-62-review.md`; its nine remediations, in its order.

| Remediation                                                    | Done? | Evidence                                                                                                                                    |
|----------------------------------------------------------------|-------|---------------------------------------------------------------------------------------------------------------------------------------------|
| 1. `APPLIES_TO` / `GOVERNED_BY` on `DECISION`                  | no    | `grep APPLIES_TO docs/_schema.awawa` empty                                                                                                  |
| 2. `RECOMMENDATION` field with required `REJECTED`             | no    | 14 `recommandation :` prefixes, schema unchanged                                                                                            |
| 3. Forbid directory anchors under `SPEC`                       | no    | 44 directory anchors, one added by #65                                                                                                      |
| 4. Split T37 into a `:v2` for part B                           | no    | One entity, `PR "62"` and `PR "65"`, five closed blockers in its footer                                                                     |
| 5. Open the anchors and `status DECISION` before each question | yes   | Four code reads and one `status DECISION` before the single question                                                                        |
| 6. Ratify on the diff with a `draft` decision                  | no    | The decision was recorded `specified` before the code; with one question the cost was 100 seconds, so the case for it is weaker than on #62 |
| 7. Strip project facts from the `awawa-*` commands             | no    | `awawa-pr-merged.md` line 99                                                                                                                |
| 8. A model entity per type                                     | no    | Five style reads by the agent                                                                                                               |
| 9. Usage report with transcript figures, by another session    | yes   | `@DECISION.UnRapportDUsageEstEcritApresChaqueFusionParUneSessionIndependante`, `../../_template.md`, this report                            |

## Appendix

### Measurements

| Session                                                                         | Active time | User turns                                        | Tool calls                                                | awawa calls by subcommand                                 | Tokens read from cache | Tokens written |
|---------------------------------------------------------------------------------|-------------|---------------------------------------------------|-----------------------------------------------------------|-----------------------------------------------------------|------------------------|----------------|
| piloting `session_01JFnUY7rRLaxKhW7EMMZpV8` (22:00:17Z–22:42:00Z)               | 29 min 56 s | 14 (6 typed by the user: 4 commands, 2 questions) | 26 (21 `Bash`, 3 `Agent`, 1 `AskUserQuestion`, 1 `Skill`) | status 6, show 8, lint 3                                  | 5,621,350              | 56,065         |
| agent `ada79f01` (implementation, 22:05:09Z–22:13:56Z)                          | 8 min 47 s  | 1                                                 | 53 (39 `Bash`, 13 `Write`, 1 `Read`)                      | context 1, lint 5, show 6, status 2, new 1, fmt 4, refs 1 | 11,040,754             | 36,693         |
| agent `a1a6c8f8` (review follow-up, 22:23:30Z–22:26:03Z)                        | 2 min 33 s  | 1                                                 | 28 (26 `Bash`, 2 `Read`)                                  | status 3, fmt 1, lint 2                                   | 2,778,382              | 9,163          |
| agent `ae42623c` (close-out, 22:27:09Z–22:30:00Z)                               | 2 min 51 s  | 1                                                 | 19 `Bash`                                                 | status 4, show 6, lint 4, refs 1, fmt 2, diff 2           | 1,831,367              | 10,466         |
| agent `aba4495d` (first reporter, aborted on a rate limit, 22:42:21Z–22:45:23Z) | 3 min 02 s  | 1                                                 | 34 (31 `Bash`, 3 `Read`)                                  | status 2, lint 2, fmt 1, show 4, refs 2                   | 2,476,952              | 14,675         |

Wall time of the journey: 22:00:17Z to 22:41:15Z, 41 minutes. User arbitration: 22:02:29Z to 22:04:09Z, 100 seconds.

| Corpus lines changed                                                   | PR lines changed                                  | Share |
|------------------------------------------------------------------------|---------------------------------------------------|-------|
| 39 (#65: `packages.awawa` +36, `questions.awawa` 1, `taches.awawa` +1) | 212 (#65: 113 insertions, 99 deletions, 24 files) | 18 %  |
| 4 (#66: two `STATUS` lines)                                            | 4                                                 | 100 % |

### Claims checked

| Claim                                                                                                                           | Check                                                                                                                                            | Verdict                               |
|---------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------|
| #65: the decision closes the question, now `superseded`, and states what was rejected                                           | `awawa refs @OPEN_QUESTION.FormeDuContratControleurVersUi .`: one incoming `CLOSES`; `show`: `STATUS superseded`; one `REJECTED`                 | confirmed                             |
| #65: 11.5 ms per parse on `../../../../input/Test/Standard-3.json`, 2.02 MB, 20 runs, Bun                                       | Re-measured with `createSaveSectionsParser().parse` on 2,118,395 bytes: 11.55 ms average, 9.93–14.81                                             | confirmed                             |
| #65: the task stays `specified`, `/awawa-pr-merged` promotes it                                                                 | Corpus diff of #65 changes no `STATUS` on the task; #66 flips it                                                                                 | confirmed                             |
| #65 and #66: `bun test` 748 pass, 0 fail                                                                                        | Run on `a5f3bd7`: 748 pass, 0 fail, 114 files                                                                                                    | confirmed                             |
| #65: `lint:types`, `check:guards`, `build:ui`, `audit:quality` exit 0                                                           | Run on `a5f3bd7`: all four exit 0                                                                                                                | confirmed                             |
| #65: `test:ui` 12 passed on chromium, firefox and webkit                                                                        | Not run here; no UI-test check in the PR's status rollup                                                                                         | unchecked                             |
| #65 and #66: merge md5 `b58ca254…` and `b1762398…` identical to the reference                                                   | `bun merge --input=input`: both sums match `plans/plan.md` of the private clone                                                                  | confirmed                             |
| #65: stderr shape identical to the baseline                                                                                     | One stderr line produced; the baseline shape was not compared                                                                                    | unchecked                             |
| #65 and #66: acceptance greps empty                                                                                             | Four greps on the merged base, all empty; `createSaveSectionsReader` exported; `validatedContent` signal feeds `useSectionViewModels`            | confirmed                             |
| #65: tests and implementation together under the exception `@DECISION.TestsEtImplementationNeChangentPasDansLeMemeCommit` names | The decision's `DESC` names « un changement de signature a comportement inchange, prouve par une verification de bout en bout » as the exception | confirmed                             |
| #65 and #66: `awawa fmt .` then `awawa lint --strict .` exit 0                                                                  | Both exit 0 on `a5f3bd7`; `status`: 0 errors, 0 warnings, 0 unresolved                                                                           | confirmed                             |
| #66: T37 stays in the corpus, `BEFORE` edges from T15, T32 and T38                                                              | Footer: `BEFORE (root, 3)` naming the three                                                                                                      | confirmed                             |
| #66: no open question names PR #65 as its source, no rule pending                                                               | `grep '#65' docs/*.awawa`: only the decision's `SOURCE` and the task's `PR`                                                                      | confirmed                             |
| #66: the question « was already `superseded` by the review of #65 »                                                             | The flip is in `c9fd4516` (launch commit, 22:08:51Z); the review agent wrote no entity                                                           | wrong                                 |
| #66: 19 anchors, all present                                                                                                    | 14 on the task, 5 on the decision; `lint --strict` 0 diagnostics                                                                                 | confirmed                             |
| #66: `awawa diff` two entities changed, `STATUS specified -> implemented` each                                                  | `git diff f2e34eb..a5f3bd7 -- docs`: two `STATUS` lines, nothing else                                                                            | confirmed                             |
| Readiness comment: « review addressed »                                                                                         | 0 reviews, 0 threads, 1 comment (the readiness comment itself); CI review posted nothing                                                         | overstated                            |
| Pilot relay: « T32 is also freed by the now-closed controller contract question »                                               | True by `STATUS`; `lint --closure @TASK.T32_…` still prints it under `BLOCKED_BY (root, 1)`                                                      | confirmed, with the footer against it |
| `2026-09-12-pr-62-review.md`: part B « inherits the entity and a false block count »                                            | `BLOCKED_BY (root, 5)`, all `superseded`, on the merged base                                                                                     | confirmed                             |
| `2026-09-12-pr-62-launch.md`: the worktree base trap needs a tooling fix                                                        | Fired for the implementing agent and for this reporter                                                                                           | confirmed                             |
