<!-- awawa-usage-report -->

# 2026-09-17 — Defects of the corpus starter, found walking Flow S

**The tool's**: thirty-two defects of `awawa-specification-starter.md` — four in the closing formula repeated by the
eight
steps of Flow S, observed on the S1 to S2 boundary, two in what a step carries and in the form it answers in,
observed inside S3, three in S4 and in the principles it invokes, P8 and P10, three in S5 and in the archive principle
P4, two on the S5 to S6 boundary, three in S6 and in the principles it invokes, P7 and P10, three in S7 and in the two
measures it
takes, P1 and P2, one in the closing formula of every step, raised by the owner at the confirmation of S7, one in what a
step carries
from one step and one session to the next, measured on the four sessions that walked S4 to S7, one in the coverage
of the two
flows, read from the document's own structure rather than hit in a step, two in S8 and in the principle it
invokes, P9, and three in S7b, the audit run after the walk closed — how a step counts the material it covers, how it
proves it covered prose, and a type whose `DESC` admits none of the values of its own discriminating field —
and one raised by the owner at S7b's confirmation, the walk writing its own step numbers into the corpus that
outlives it —
what becomes of the documents the corpus replaced, and what guards their deletion —
and three in S7c, the audit of the domains run after S7b: a declared value no entity ever carries, a document S1 kept
whose prose no step ever reads, and what a `SOURCE` has no field for. All of them found walking the
product
specification area of planet-crafter-save-tools.

| Field                             | Value                                                                                                                                                            |
|-----------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Document under review**         | `docs/awawa-usage-reports/artifact-assets/awawa-specification-starter.md`, as served to the awawa team                                                           |
| **Walk**                          | Flow S, product specification area, planet-crafter-save-tools                                                                                                    |
| **Steps observed**                | S1 (inventory), S2 (types), S3 (reading loop) and S4 (skeleton), 2026-09-17; S5 (pivot and archive), S6 (sources and shapes), S7 (first entities) and S8 (cleanup), 2026-09-18 — the whole of Flow S, plus S7b and S7c, the merge-typing audit, run after S8 |
| **Agent that walked the starter** | `Claude Opus 5 (1M context)`, effort `medium` — every defect below is measured under that model and that effort, and under no other                              |
| **Author of this report**         | the same agent                                                                                                                                                   |

## Defects

| #  | Defect                                                                                                                                                                                                    | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Remediation proposed                                                                                                                                                                                                                                                                                                                                                                 |
|----|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1  | A step's result is written nowhere, so the next session cannot read it                                                                                                                                    | S1 ends on « Respond with the inventory table (place, kinds of fact, example) and the five questions », naming no file. Measured: the S2 session could re-read neither the inventory nor the five questions; `grep -rln S1 docs/` returns nothing, `git status` is clean, no work file exists. S2 re-derived the inventory from the repository and stated it had done so                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Every step writes its result to a named work file, and the next step's prompt carries that file's path among its entry answers                                                                                                                                                                                                                                                       |
| 2  | Questions meant for the user are printed, never asked                                                                                                                                                     | S1 says « List the five questions a working session most often asks about the object »; the step lists them and nothing requires that they be put to the user, while the starter's entry point does say « Ask the user » for Question 1. Measured: the five questions of S1 were never presented, so none was arbitrated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A step that produces a question puts it to the user and waits for the answer; what is only printed is not arbitrated                                                                                                                                                                                                                                                                 |
| 3  | The next step's prompt is proposed together with the result, before any arbitration                                                                                                                       | All eight steps of Flow S end on « then propose the S<n+1> prompt ». Measured: the S3 prompt was proposed at the end of S2, then the user's refinement changed four of the five open points and added a type, so the prompt was stale when it was written                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | The next prompt is emitted only once the step's work is done and arbitrated, after the user's last answer                                                                                                                                                                                                                                                                            |
| 4  | Nothing says when a step is over: no step asks the user to confirm that every point is settled                                                                                                            | The eight steps end on « Respond with … then propose the S<n+1> prompt », which makes the answer the end of the step. Measured: S2 was answered three times — the first answer carried five open points, the second changed four of them and added the `DATATABLE` type, the third corrected the table inventory — with no confirmation asked at any of the three                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | A step ends only when every point it opened is settled and the user, asked the question, confirms moving to the next step; the answer alone never ends it                                                                                                                                                                                                                            |
| 5  | An entry answer taken once at the entry point is carried by no step, so the obligation it creates is forgotten                                                                                            | The entry point asks « [tool defects: recorded\|ignored] » once. The eight step prompts carry the entry answers as a bracketed list, but no closing formula of the eight names the defects file or requires writing to it. Measured on S3: the defects found inside the step were recorded only because the user asked for it mid-step, not because the step required it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | An entry answer that creates a standing obligation is restated as an instruction in every step prompt — « at the end, record the starter's defects in the dedicated file » — not only as a bracketed entry answer                                                                                                                                                                    |
| 6  | A step puts its questions to the user in prose, so arbitrating costs the user the work of reconstructing the alternatives                                                                                 | Every step of Flow S ends on « Respond with … », and S3 on « Respond with the question → commands table » — a table for the result, nothing for the questions. Measured on S3: the first arbitration of question 1 was refused by the user with « fais moi des tableaux pour que ce soit plus facile de piger ce que je dois répondre », one turn spent before any question was answered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A step that puts a question to the user presents its alternatives as a table — one row per alternative, the columns being what distinguishes them — and never as prose                                                                                                                                                                                                               |
| 7  | S4 has no branch for an area that joins an existing workspace: it prescribes creating a `SCHEMA *` that would be a second one                                                                             | The premise is stated in « Out of Scope » line 524 — « a second corpus (e.g. a product specification beside a methodology) runs its own flow; each corpus is independent » — which is false of the tool: the unit of resolution is the workspace, and two areas of one workspace share one `SCHEMA *` and may reference each other. S4 line 276: « Create _schema.awawa: SCHEMA * (DESC field, RATIONALE with CATEGORY reasoning), then one SCHEMA entry per S2 type ». Measured here: the workspace already carries one `SCHEMA *` (ruling 2 of 2026-09-17), so a second is illegal; S4 in fact extended the existing schema by 108 lines and created no `SCHEMA *`, and the divergence had to be declared by the owner's prompt, not by the starter                                                                                                                                                                                                                                                                                | The entry point asks whether the object joins an existing workspace; S4 branches on the answer — extend the workspace's `SCHEMA *` and its ladder, or create one                                                                                                                                                                                                                     |
| 8  | P10's throwaway corpus is « the schema » copied alone, which cannot see a collision with the names the workspace already reserves                                                                         | P10 line 194: « write it in a throwaway corpus outside the project (a temporary directory): the declaration, one entity, one reference ». Measured here: the reserved namespace is 41 names; the check that matters for the 18 names S3 introduced is `L013`, and it fires only against a corpus holding the existing declarations. The probe had to copy the whole area — 8 files, 219 entities — not the schema lines being added                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P10 copies the corpus the schema will enter, not the lines being written, whenever one exists; a from-scratch corpus is the special case, not the rule                                                                                                                                                                                                                               |
| 9  | P10's probe writes conforming entities only, so a conditional obligation that never fires is accepted in silence                                                                                          | P10 line 194 and S4 line 280 both say « one entity per type with one reference ». Measured here on 2.7.0: the conforming probe — 7 entities, one per type, each with one reference — linted 0 error whether or not `WHEN DOMAIN merge` fired. The three `WHEN` blocks were proven only by a second file of deliberately non-conforming entities, which produced 8 findings (6 `L006`, 1 `L003`, 1 `L006` nested under `SPEC`); and two things the schema cannot forbid — an `INDEX` on a `HOLDS_FOR legacy` section, `CONFLICT` on a non-merge rule — were found only that way, `lint` staying silent on both                                                                                                                                                                                                                                                                                                                                                                                                                        | P10 writes two entities per conditional declaration: one that satisfies it and one that violates it, and records the three outcomes — imposed, refused, accepted in silence — per schema line, the third being a finding, not a pass                                                                                                                                                 |
| 10 | No flow covers a project with no implementation yet: the two flows branch on whether a corpus exists, and both assume the code does                                                                       | Question 1 line 22 branches on « Does an awawa corpus already exist? » alone. Flow S, the branch for « no corpus », is written throughout for an existing implementation: S1 inventories « README, architecture decision records, agent instructions, issue tracker, wiki, code comments »; S6 probes « an anchor to a missing symbol (expect L016) », which presupposes files to anchor in; S7 writes « the S1 examples » as the first entities and takes both measures on them; the readiness criteria ask that « `awawa status` shows every type with entities ». A project with no files has no S1 examples, so S7 writes nothing and neither P1 nor P2 can be measured. Deeper: the anchor-backed idiom the flows install — a `SPEC` whose `IMPL` is REQUIRED, an `ATTESTED_BY` REQUIRED under each obligation — cannot hold before the code exists, since every anchor would be `L016`                                                                                                                                         | A third flow, for a corpus written before the implementation, whose schema makes the anchor REQUIRED only at the implemented rung of the STATUS ladder — the shape awawa's own `spec.awawa` uses (`WHEN STATUS implemented` → `SPEC` REQUIRED → `IMPL` REQUIRED) and this repository's `TASK` type with it. Question 1 then branches on two axes, corpus and implementation, not one |
| 11 | S5 has no branch for an area joining a workspace whose schema already carries the archive mechanism, so the step prescribes creating what exists                                                          | S5 line 292: « On SCHEMA \*: FIELD STATUS active\|archived, DEFAULT active; WHEN STATUS archived with GATE suppressed and FIELD ARCHIVED_ON date REQUIRED; a SHAPE date ». Measured: all four already stood in `docs/_schema.awawa` before S5 — `STATUS active\|archived DEFAULT active` (line 24), `WHEN STATUS archived` with `GATE suppressed` (29–30), `ARCHIVED_ON date REQUIRED` (31–32) and `SHAPE date` (52–54) — and S5 added **0 schema line**. The reduction of the step to a probe came from the owner's prompt, not from the starter. Same shape as defect 7, one step later                                                                                                                                                                                                                                                                                                                                                                                                                                            | S5 branches on the entry answer of defect 7: where the workspace already declares the mechanism, the step probes it on the new types and completes only what the probe shows missing                                                                                                                                                                                                 |
| 12 | P4 declares `GATE suppressed` without saying that archiving silences every check on the entity, so an entity can be archived unclean and nothing reports it again                                         | P4 line 174. Measured on 2.7.0, probe area, each violation written beside its active twin: an archived `SECTION` with `HOLDS_FOR current` and no `INDEX` lints clean while its active twin raises `L006`; an archived `RULE` with `DOMAIN merge` and none of `CONFLICT`, `RESOLUTION`, `APPLIES_TO_SECTION`, and a `SPEC` with no `ATTESTED_BY`, lints clean — four `L006` extinguished; an archived rule pointing at `@SECTION.ProbeDoesNotExist` raises no `L004`, and one anchored on a missing file no `L016` (377 before, 377 after); an archived `INDEX "not-a-number"` is silent while its active twin raises `L007`. Only the block's own fields stay checked: `ARCHIVED_ON` absent → `L006`, `ARCHIVED_ON "18/09/2026"` → `L007`                                                                                                                                                                                                                                                                                            | P4 states that archiving suppresses validation of the entity, and that the change which archives runs `lint --strict` **before** writing `STATUS archived`: afterwards no command reports what was broken                                                                                                                                                                            |
| 13 | Neither P3 nor P4 says what archiving costs the entities that still point at the archived one: `context` drops its body silently                                                                          | Measured: `@SECTION.ProbeArchivedTarget` archived, cited by an active `@RULE.ProbePointsAtArchived`. `awawa context @RULE.ProbePointsAtArchived` returns 1 entity, 7 lines, and the single line `// suppressed: @SECTION.ProbeArchivedTarget` — the section's `LABEL`, `INDEX` and `LEGACY_INDEX` are absent from the package a session reads before working on that rule. The reverse direction still reports: `context` and `status` on the archived section print `CONSTRAINED_BY (root, 1)` and `incoming 1`. So the outgoing loss is silent and the incoming one is not                                                                                                                                                                                                                                                                                                                                                                                                                                                         | P4 names the precondition for archiving — nothing active still points at the entity — and P9's cleanup pass reads the incoming count `status TYPE` prints, which is the only side the tool reports                                                                                                                                                                                   |
| 14 | No step says where the walk stands, so the position is rebuilt by hand in every prompt                                                                                                                    | The eight step prompts of Flow S carry the entry answers as a bracketed list and no position: neither « step n of N » nor what is already closed. Measured on the S5 prompt: 3 of its 24 lines are the owner retyping the position — « S2, S3 et S4 sont clos : … Ne rien y rouvrir sans le dire » — and the same 3 lines were retyped in S3 and S4. Nothing reads them back from the work file defect 1 asks for, so the cost is paid at every step and grows with the number of closed steps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Every step prompt opens with its rank (« S5, step 5 of 8 ») and the step reads the closed steps from the work file rather than from the prompt; the owner states only what diverges                                                                                                                                                                                                  |
| 15 | No step reads the schema back as a whole: eight steps each add to it and none reviews it                                                                                                                  | S4 added 108 lines to `docs/_schema.awawa`, S5 none, and the five types had never been read together when S5 closed. Measured: the first such reading — asked for by the owner, not by the starter, on the S5 to S6 boundary — raised four points no single step could see, three of them probed as real gaps: `SECTION.INDEX`, the most falsifiable number of the corpus, is attested by nothing while `RULE.SPEC.ATTESTED_BY` is REQUIRED; `DATATABLE` names no JSON Schema although « one JSON Schema per table » is a settled entry answer; no `INCOMING` is declared anywhere, so a section the game writes today that no rule constrains is silent                                                                                                                                                                                                                                                                                                                                                                             | A review step between the schema steps and the first entities: re-read every declaration against the entry answers and the rulings already taken, probe what the review would change, and rule before any entity depends on it                                                                                                                                                       |
| 16 | S6 prescribes the `SOURCE` mechanism as a creation, so a step whose whole subject already stands in the workspace has no work left to describe                                                            | S6 line 307: « On SCHEMA \*: FIELD SOURCE string REPEATABLE, nested FIELD REF reference REQUIRED ». Measured: the declaration stood at `docs/_schema.awawa:15-23` before S6 opened, together with four source types (`PROJECT`, `PULL_REQUEST`, `USAGE_REPORT`, `URL`) and nine `SHAPE`. Of the five places S1 lists without a source type, three needed no new type — the wiki and the mod label file are `URL`, the private context is `@PROJECT.DNC` — and one type was added, `GAME_RELEASE`. Third occurrence of the shape of defects 7 and 11, which makes it structural rather than a miss in one step                                                                                                                                                                                                                                                                                                                                                                                                                        | The entry point's « does the object join an existing workspace » answer (defect 7) is carried by every step, and each step that declares a mechanism opens by reading what the workspace already declares, then completes only the difference                                                                                                                                        |
| 17 | P7 is invoked by S6 alone, two steps after S4 writes the type declarations, so the schema is knowingly written untyped and a step draws durable conclusions from it                                       | P7 line 154 carries « A field legal in one state only is declared inside that state's `WHEN` block »; S4 (line 276) invokes P8 and P10 and not P7. Measured: S4 declared `INDEX`, `LEGACY_INDEX`, `CONFLICT` and `RESOLUTION` at type level with `REQUIRED` added in their `WHEN` blocks, recorded two of its fourteen probe lines as « accepted in silence » — a `HOLDS_FOR legacy` section carrying `INDEX 9` lints clean, a `DOMAIN format` rule carrying `CONFLICT` and `RESOLUTION` lints clean — and concluded « not a defect of the tool … not worth a second discriminant field », a conclusion written into the work file. S6 applying P7 refutes it: the same four fields declared inside their blocks alone raise `L003 field INDEX is not declared on SECTION` and two more on the rule, and repeating `INDEX` in `WHEN HOLDS_FOR current` and `WHEN HOLDS_FOR both` raises no `L013`. Here the cost was a wrong conclusion carried for one day; in a walk that wrote entities between S4 and S6 it would be a migration | S4 invokes P7 with P8 and P10: a declaration is typed when it is written, not two steps later. S6 then probes and completes, as it does for the sources                                                                                                                                                                                                                              |
| 18 | P10's probes are named by the principle, generically, so a step re-proves the tool instead of its own declarations                                                                                        | S6 line 311 names three probes — a `SOURCE` with no `REF`, an anchor to a missing symbol, a value missing its shape. Measured on 2.7.0: all three behaved exactly as documented (`L006 required field REF is absent from RULE.SOURCE`; ``L016 field ATTESTED_BY: `NoSuchLiteralAnywhere` does not occur in ./docs/energy-levels.md``; `L007 field KEY expects json_key`) and **none of the three changed a schema line**. The probe rows that did change the schema are the ones the area's own declarations dictate: `INDEX` on a `legacy` section and `CONFLICT` on a `format` rule, refused only once the fields were moved, and a new `SHAPE` tried on its boundary values — `"g"` and `"gId2"` accepted, `"world_object_name"` and `"WorldObjectName"` refused                                                                                                                                                                                                                                                                  | P10 keeps its generic probes for the first walk of a project and requires, from every step after the first, one probe per declaration the step writes or moves, each with its conforming and non-conforming entity — the rule of defect 9, extended from « per conditional declaration » to « per declaration the step touches »                                                     |
| 19 | S7 opens on writing entities without asking whether the REQUIRED anchors of each type resolve today, so a type declared in S4 can be unwritable and the flow finds out at the moment of writing           | `SCHEMA DATATABLE` requires `TABLE` and `JSON_SCHEMA`, two anchors, and `@DECISION.AnAnchorNamesAFileTrackedByGit` requires both to name files git tracks. Measured: the five planned tables are TypeScript modules, neither JSON file nor JSON Schema exists, and **0 of the 5 `DATATABLE` could be written in S7** — the ordering had to be ruled by the owner's prompt before the step opened, not by the step. The comparison is in the same step: `COMMAND.IMPL` resolves against `merge-cli.js` and `validate-cli.js`, so both commands were written unblocked. The cost of finding out late is a wave of entities whose anchors are placeholders, or a step that stops                                                                                                                                                                                                                                                                                                                                                        | S4, which declares the types, asks of every REQUIRED anchor whether a tracked file satisfies it today; where none does, the answer is a task, recorded then, and S7 reads it instead of discovering it                                                                                                                                                                               |
| 20 | A retyping the walk decides is owned by no step, so it is carried by hand from the step that rules it to the step that can perform it                                                                     | S2 ruled that the eight `FACT` become four `RULE` and four `HYPOTHESIS`; S4 could not perform it, the targets not existing yet, and recorded the deferral in the work file; S7 performed it. Measured on the real corpus: 8 entities retyped, 6 incoming edges repointed (5 `RESTS_ON`, 1 `RATIONALE.REF`), 1 schema field retargeted (`DECISION.RESTS_ON @FACT` → `@RULE`), 1 `SCHEMA` entry deleted, 1 file deleted, and 2 mentions outside the corpus — `AGENTS.md` and a code comment in `worldObjectLabels.ts`. The flow names none of those five kinds of site. Had the walk stopped at S6, the corpus would carry a temporary type and a schema field pointing at it, and `lint --strict` would have reported nothing                                                                                                                                                                                                                                                                                                         | A step that rules a retyping opens a migration ledger in the work file — entities, incoming edges, schema fields, files, mentions outside the corpus — the flow makes the ledger a precondition for closing the walk, and `refs` is run per entity before the type is deleted                                                                                                        |
| 21 | S7's two measures say nothing about coverage: P1 counts entities the threshold should have refused and P2 counts calls per question, neither asking whether the entities cover the documents they replace | Measured here: the coverage check was run only because the S2 plan happened to carry a « where each document empties » table, and it found two statements of `docs/save-format.md` that no planned entity covered — the numeric planet ids are stable across saves, and `liPlanet` is carried only by an interplanetary exchange platform — which became a 19th `RULE` and a 5th `HYPOTHESIS`. P1 scored 0 and P2 scored the predicted call counts with both statements still missing, so neither measure would have caught them; S8 deletes the document, and both would have been lost there in silence                                                                                                                                                                                                                                                                                                                                                                                                                            | S7 requires a coverage table, one row per section of each source document, each row naming the entity that holds it, the ruling that drops it, or the task that will carry it; S8 refuses to delete a document whose rows are not all filled                                                                                                                                         |
| 22 | A step's closing formula asks for results and never for the files the step touched, so the owner is asked to confirm work they cannot locate                                                              | The eight closing formulas of Flow S all read « Respond with <the step's results> », and name counts, tables and measures only. Measured, and raised by the owner at the confirmation of S7: « il ne m'a pas été précisé lors des premières étapes quels fichiers avaient été créés et où, j'ai dû les trouver par moi-même afin de les relire avant confirmation ». The cost is paid at every step and by the owner, not by the agent: S4 created six files and moved one, S6 changed three, S7 changed eleven tracked files and deleted one, and in none of the three did the answer name them. Defect 4 made the confirmation a step of the walk; this defect is that the confirmation has nothing to stand on                                                                                                                                                                                                                                                                                                                    | The closing formula requires, before the confirmation question, the list of files created, modified and deleted, each with its path, and names which of them the owner should re-read to confirm — the basis of the confirmation, not an appendix to the result                                                                                                                      |
| 23 | A step carries its conclusions to the next and nothing else — no ledger of what it read, no cost — so the sources are re-read from scratch at every step and no step can be seen to be worth its price    | The eight closing formulas ask for the step's results; the work file of defect 1 records what a step concluded, never what it read, at which revision, nor what it cost. Measured on the four sessions that walked S4 to S7, one session per step, counting API turns: **43, 51, 58 and 54 turns; 4.45, 5.80, 7.23 and 8.02 M weighted tokens** (cache read plus cache creation). The turn count moves with nothing the step produces — S5 spent 51 turns and 5.80 M to add **0 schema line**, S7 spent 54 turns and 8.02 M to write 38 entities, retype 8, delete a type and run 19 probes — and the two steps read alike in the walk's own records, which carry no cost. Inside S7, **13 turns and 1.42 M** went to re-reading `save-format.md`, `game-rules.md`, the merge specs, the index module and the root JSON Schema, all of them already read by S1 and S2; and recording the step cost as much as performing it, 12 turns and 2.54 M against 16 turns and 2.55 M                                                         | Two halves. A step records in the work file the sources it read and at which revision, so a later step re-reads only what changed and states what it re-read; and a step ends on its cost — turns and weighted tokens — beside its result, which is the only measure by which a step that produced nothing shows up as such                                                          |
| 24 | Flow S never disposes of the source material the corpus replaced: S8 designs a cleanup pass over *entities*, and no step and no readiness criterion asks whether the document a type emptied still stands | S8's prompt reads « If the corpus will grow or change often: design the cleanup script… Otherwise: record that it is skipped », and names nothing but archived entities and a retention delay. The ten readiness criteria check the lint, the `status`, two `context` calls, the thresholds, the instructions and the deletion of the probe directories; none names the sources S1 inventoried. Measured on 2026-09-18, at the last step of the walk: `docs/game-rules.md`, 219 lines, whose thirty `GR-*` rules had each become a `SPEC` of one of the fourteen `RULE DOMAIN merge` written in S7, was still in `docs/` and still opened on « This is the **authoritative reference** for every merge decision ». Nothing in the starter would ever have removed it | S8 asks, one row per document the walk emptied, whether it can be deleted and what is still owed before it can — the answer being per document, not one for all: on this walk `game-rules.md` went and `save-format.md` and `energy-levels.md` stayed, their value tables waiting on a task. A readiness criterion states that no source the corpus replaced still stands, or names the task that will remove it |
| 25 | The deletion of a source is guarded inside the corpus alone, and the starter never says so: `lint` resolves the anchors the corpus carries and reads no code comment, no README link and no other document | S8's only validation is `awawa fmt --check` and `awawa lint --strict`. Measured on 2026-09-18 at the deletion of `docs/game-rules.md`: the gate raised **6 `L016`** — 5 `IMPL` and 1 `SEEN_IN` — and said nothing of **24 code comments in 21 files** (`@see GR-ID-5 in docs/game-rules.md`), **13 README lines** and **2 links** from `docs/save-format.md` and `docs/energy-levels.md`. 45 citation sites, 6 of them seen by the tool; the 39 the tool does not see were found by one `grep -rn` and repointed in the same change | The step counts the citations outside the corpus with a `grep` on the document's name *before* deleting it, and repoints them in the same change; a code comment then cites the entity — `@see @RULE.TheSaveOnPrimeBecomesSaveA` — rather than a file, so the next rename is an `awawa fmt --rename` and not a second `grep` |
| 26 | A step asserts the size of the material it covers instead of computing it, and the number is then carried by every later step | `docs/game-rules.md` holds **37** rules — `git show HEAD:docs/game-rules.md \| grep -c '^\*\*Rule GR-'` → 37, and 37 distinct identifiers. S7's coverage table wrote « 30 rules », and the figure was copied unchanged into the S7b prompt, into S8's document table and into the bilan of the walk. Seven statements were extracted, audited and are now in the corpus that no step knew it had written; had the miscount gone the other way, seven would have been lost in a deletion S8 called complete | The step that maps a document to entities prints the command that counts its units beside the count, as the gate prints its lint line; a number no command produced is not a coverage figure |
| 27 | S7's coverage table proves nothing about prose: a source line is reported as covered when the step names a home for its *section*, not for the line | Measured on §1 of `game-rules.md`, reported as covered by three `DECISION`: those three state none of its four bullets. « The two input files are never modified » lives in `README.md` alone, attested by nothing and cited by no entity; « all merge decisions are deterministic and stateless » is nowhere; « section rules are applied independently » is nowhere and is false besides, GR-WO-3 and GR-ID-2 contradicting it. A fourth line, GR-PLAYER-2's « a save that flags no host is rejected as invalid before the merge », was dropped when the sentence became a merge `SPEC`. Four statements, none of which any measure of S7 or S8 could see | The coverage table takes one row per statement of the source, not per section, and a row whose home is a document rather than an entity is reported as uncovered; the step that deletes the source reads that table rather than the section headings |
| 28 | A type's `DESC` and the values of its own discriminating field can contradict each other, and no step reads them together | `SCHEMA RULE` says « a statement about the save or the game that a real save, or a game source, can contradict; a ruling of ours that could have been made otherwise is a DECISION », and declares `DOMAIN format\|game\|merge`. **No `DOMAIN merge` statement satisfies the `DESC`**: no save contradicts « the higher value wins », and all fourteen could have been ruled otherwise. The contradiction was written in S4, survived the four schema reviews R1 to R8 — which read declarations against the entry answers, not a `DESC` against an enum of the same block — and was found only by an audit of the entities, two steps after the last review | A schema review reads each type's `DESC` as the admission test of that type and runs it against every value of its discriminating fields, one row per value; a value no `DESC` admits is either a second type or a `DESC` that is wrong |
| 29 | The starter's own steps are written into the corpus, which outlives them: a step names itself in the `SOURCE` of every entity it writes, and the walk is a temporary process no later session may read | **56 entities of 274** cite a step of Flow S — 55 in a `SOURCE`, 2 in a `REJECTED`, 1 in an `UNTIL`. The whole product specification area is concerned: 21 `RULE` of 21, 12 `SECTION` of 12, 5 `HYPOTHESIS` of 5, 2 `COMMAND` of 2; plus 12 `DECISION`, 2 `TASK`, 1 `LIMITATION`, 1 `PROCESS` of the methodology area. Wordings: « step S7 of the specification walk », « retyped from FACT in step S7 … », « Review of the target schema, S5 to S6 boundary … ». The breakdown is in `2026-09-18-flow-s-walk.md`, « The starter's own steps, written into the corpus ». Raised by the owner at S7b's confirmation, not by any measure of any step | S6 says what a `SOURCE` carries — the date, the source document, the identifier inside it, the pull request — and adds that **the rank of the step is not part of it**: the walk is scaffolding, and a corpus that names it makes every reader of the corpus ask what S7 was. The closing formula of each step checks the `SOURCE` it wrote against that list, as it already checks the lint |
| 30 | A value of an enumerated field is declared once and never measured, so a value no entity ever carries reads exactly like a value in use | `DOMAIN format\|game\|merge` was declared on `SCHEMA RULE` in S4. `DOMAIN game` carried **0 of 21 `RULE`** through S7, S7b and the four schema reviews R1 to R8, and the zero was named only by an audit the owner asked for, two steps after the last review. No measure of the starter looks at the distribution of a declared enum: P1 counts entities the threshold should have refused, P2 counts calls per question, and a review reads declarations against the entry answers. The audit confirmed the zero rather than lifting it — under the criterion the owner ruled, a `RULE` is `game` only when a change to its statement would change how the game plays, and the corpus, extracted from a format document and a merge document, holds none | The step that declares an enumerated field records the criterion that decides between its values as an entity of the corpus, not as prose of the `DESC`; and the gate of every later step prints one line per declared value — `awawa status TYPE --where FIELD==VALUE` — a value at zero being reported beside the lint line rather than found by an audit |
| 31 | S1 sorts the documents into replaced and kept, and no step of the walk ever reads the content of a kept one | `docs/energy-levels.md` was inventoried at S1 and kept, its value tables deferred to `@TASK.CHORE52`. S7's coverage table covers only the documents entities replace; the eight reviews read the schema. Measured at S7c on 2026-09-18, classifying the two kept documents statement by statement: **44 statements**, of which **11 to 13 are `RULE` candidates** — EN-BASE-1, EN-OPT-1 to EN-OPT-3, EN-FUSE-1 to EN-FUSE-4, EN-PLANET-1 and the power cost of a machine — that no step of the walk has ever seen. Deferring the *tables* of a document was read by every later step as deferring the *document*, and `@TASK.CHORE52` was written on that reading: its `SPEC` requires that « every rule with authority over those values names its table through `VALUES_FROM` » while no such rule exists, and its `SPEC` deleting the value tables of `energy-levels.md` would drop §3.1, §3.2 and §4, which are prose and not tables | S1's inventory carries, per kept document, what of it is deferred and what is not; a document kept for its tables alone has its prose classified in the step that defers the tables, or the deferral names the prose it leaves behind, so no later step can read « the tables wait » as « the document waits » |
| 32 | Applying defect 29's remediation shows what `SOURCE` lacks: the process that produced an entity has no field, which is why it was written into prose in the first place | The cleanup rewrote **58 fields in 56 entities** — 55 `SOURCE`, 2 `REJECTED`, 1 `UNTIL` — and left **11 `SOURCE` carrying a bare date and a `REF`**, `SOURCE "2026-09-18"` with `REF @PROJECT.PCST`, because for those eleven the only source was the walk itself and the walk is exactly what must not be named. The date, the source document, the `GR-*` identifier and the pull request all survived where they existed; where none of them existed, the prose had been carrying the process alone | `SOURCE` declares a nested field for the date, so a date is data rather than a sentence, and the starter states that an entity whose only origin is the process that wrote it carries that date alone — rather than leaving each step to invent a sentence, which is what put the step rank into 56 entities |

## Kept for the migration-3 report: a defect of the binary, not of the starter

Found in the same walk, on awawa 2.7.0, and recorded here only so it is not lost; its place is the report on the
third migration.

| #  | Defect                                                                                                              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Remediation proposed                                                                                                                                                   |
|----|---------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| B1 | `awawa new` omits the `INCOMING` obligation of a `WHEN` block, so the skeleton understates what the schema requires | `WHEN HOLDS_FOR current` on `SCHEMA SECTION` carries `INCOMING CONSTRAINED_BY` and `FIELD INDEX REQUIRED`. `awawa new SECTION Probe .` prints `// WHEN HOLDS_FOR current: INDEX (required)` and says nothing of the reverse edge, although `L026` fires on the entity as soon as it is written alone. The skeleton is the documented way of reading what a declaration requires, so the one obligation an author cannot see by reading the entity is the one the skeleton drops | The `WHEN` footer line names the incoming obligation beside the fields, e.g. `// WHEN HOLDS_FOR current: INDEX (required), referenced under CONSTRAINED_BY (required)` |

## Scope of the claim

The whole walk was run by `Claude Opus 5 (1M context)` at effort `medium`, on awawa 2.7.0: what a step cost, what it
left unasked and what it concluded from a probe are measured under that agent and that effort alone, and another
model, or the same one at another effort, may pay a defect differently or not hit it at all. Twenty-two of the first twenty-three are
read from the starter's text; defect 23 is read from the session transcripts of the four steps S4 to S7, and its
figures hold for this agent, this effort and this walk alone. Defects 1 to 4 are observed on one step boundary of one
walk,
S1 to S2; defects 5 and 6 inside S3 of the same walk; defects 7 to 9 inside S4 of the same walk, on awawa 2.7.0;
defects 11 to 13 inside S5, on the same version; defects 14 and 15 on the S5 to S6 boundary, the second one
measured by the review the owner asked for there; defects 16 to 18 inside S6 and defects 19 to 21 inside S7, on the same
version; defect 22 at the confirmation of S7, raised by the owner and read from the closing formula the eight steps
share;
defect 23 measured after that confirmation, over the four sessions of S4 to S7 rather than inside one step; defects 24
and 25 inside S8, the last step, on the same version, and both measured on the deletion of one document,
`docs/game-rules.md`, in one repository; defects 26 to 28 in S7b, the audit S7 drafted and that ran after S8
closed, and all three measured on the same document and the same corpus, defect 28 on a declaration of
`docs/_schema.awawa` rather than on the starter's text; defect 29 raised by the owner at S7b's confirmation and
measured across both areas of the workspace, S7b being itself the author of six of the fifty-six sites it reports. No figure here is measured over the eight steps as a whole: the cost table of
defect 23 covers S4 to S7 and was not extended to S8. Defects 8 and 9 are P10's, a principle
S4 invokes; S5 applied their two
remediations — the whole area copied, a conforming and a non-conforming entity per conditional declaration — and the
three findings of defect 12 come from the non-conforming half alone, which is what the remediation was for. Defect 10 is
of another kind:
it was not hit by this walk, which is on an implemented project and is served correctly by Flow S; it is read from
the coverage of the two flows, and its cost would be paid by another project, not by this one.
