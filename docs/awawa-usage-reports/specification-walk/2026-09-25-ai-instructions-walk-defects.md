<!-- awawa-usage-report -->

# 2026-09-25 — Defects of awawa 2.7.0 and of the corpus starter (version 2), found walking Flow S

The walk built an awawa corpus from scratch for the global instructions given to AI agents (`delairec/.ai`): 23
instruction files, 478 statements, replaced by 237 entities. Addressed to the authors of the tool and of the starter;
the `Step` column names the starter's step the defect was found in.

| Field                      | Value                                                                    |
|----------------------------|--------------------------------------------------------------------------|
| **Documents under review** | `awawa-specification-starter-v2.md` (version 2); awawa 2.7.0             |
| **Walk**                   | Flow S, global AI agent instructions, `delairec/.ai`                     |
| **Steps observed**         | S1 to S8, in walk order S1–S6, S9, S7, S10, S8; 2026-09-24 to 2026-09-25 |
| **Author of this report**  | `Claude Opus 5.5`, effort `medium`                                       |

| Step | Subject | Defect | Evidence |
|------|---------|--------|----------|
| S1 | starter | W4 and W5 ask for a count of « statements » and a coverage row per statement, but the starter defines no statement: list item, sentence, paragraph and table row each give a different figure, and each walk invents its own | this walk had to define it (list item + paragraph + table row → 478 for the replaced documents, against 111 rule identifiers) |
| S1 | starter | The `[implementation: none]` branch of S1 assumes the knowledge is in the user, a brief, sketches and issues; it has no case for an object that is itself a set of documents, so the interview has little to ask | object « instructions given to AI agents »: 23 instruction files hold the knowledge, the user added no place (Q1 A) |
| S2 | starter | The entry questions do not ask the language of the exchange; the step prompts are in one language and the user answers in another, so the session oscillates between the two. The language should be an entry question, kept for the whole walk | this walk: S1–S2 prompts in English, the user answering in French; ruled at S2 (French, corpus in English) |
| S3 | awawa | No command prints the field values of every entity of a type: `status TYPE` (and `--json`) gives identity, `file:line` and incoming counts, `show FILE --json` token positions without values. A session that must read the conditions of all entities of a type (an index) has to read the file, or the corpus has to carry a hub entity whose only purpose is an outgoing edge to each of them | `status TRIGGER --json`, `context TRIGGER`, `show data.awawa --json`, `refs TRIGGER --json` on a probe corpus: none prints `CONDITION` |
| S3 | starter | P2 and the S3 body list `context @TYPE.X [--skip CATEGORY]` without `--depth`; at the default depth 3 a package from a well-connected entity spans the corpus, so the reading loop measured without `--depth` is wrong from the first question | probe: `awawa context @RULE.no_cd` → `8 entities, 29 lines, depth 3` on a 3-rule corpus; `--depth 1` → the rule and its direct targets |
| S3 | starter | The tool-behaviours table omits that a reference slot takes one target type (L020), so S2 can sketch a field « an area or a rule » that no schema accepts; S3 then reopens S2 | `FIELD LOADS @RULE\|@PACKAGE_KIND` → L020; `OPEN_QUESTION.ABOUT` re-ruled at S3 |
| S4 | starter | S4 states « [implementation: none] No anchor is REQUIRED outside WHEN STATUS implemented » as if every anchored type had a lifecycle; a type whose anchor names a file that exists before its entity (a tracked procedure) has no implemented rung, and the rule cannot be applied as written | `PROCEDURE.PATH`: `git ls-files procedures` → 2 files, REQUIRED at type level, probe clean |
| S4 | starter | The reference skeleton's `SHAPE date` (`\d{4}-\d{2}-\d{2}`) accepts `2026-13-45`, although P10 asks for a shape to be tried on its boundary values | probe: `DATE 2026-13-45` accepted in silence; `\d{4}-(0[1-9]\|1[0-2])-(0[1-9]\|[12]\d\|3[01])` → L007 |
| S4 | starter | P7 and the tool-behaviours table record that an empty required string passes `--strict`, but offer no remedy; a shape `[^\s].*` on every required text refuses `""` and `" x"` (L007) and still accepts `+` continuations | probe: `SPEC ""` silent as `string`, L007 as `prose` |
| S4 | awawa | A type-level `FIELD` redeclaring a field of `SCHEMA *` (`AREA.DESC`, to make it REQUIRED) is accepted without L013 and shadows the wildcard's declaration; the schema-authoring guide states that « a field arriving twice, however it arrives, is L013 » and documents shadowing for `GATE` and `NAME` only | probe: `SCHEMA AREA` › `FIELD DESC prose REQUIRED` → `lint --strict` clean; an `AREA` without `DESC` → L006 |
| S5 | awawa | An entity whose `STATUS` comes from the `DEFAULT` is counted under `(none)` by `status` and matched by no `--where STATUS==<default>`, although its `WHEN STATUS <default>` block fires on it (`L026` raised on the default). The same field reads as two different values depending on the command | probe: `DEFAULT active`, 16 rules, 5 archived → `awawa status RULE --where STATUS==active .` → `RULE (0 of 16)`; `--where STATUS!=archived` → `11 of 16`; `awawa status .` → `active 0`, `(none) 10` |
| S5 | starter | P3 and S5 ask to « name the pivot » without saying what the choice commits to; when no type has a lifecycle, the choice changes no schema line and only confirms the edges of S3, yet it is put to the user as a decision. The user could not tell what they were deciding. S5 should state the commitments (the type carrying a ladder, the direction of later edges, the first entities written) and say when the choice reduces to a confirmation | this walk: pivot `RULE`, no ladder; the user asked what the question engaged before answering |
| S5 | starter | P4 lists two archive conditions but not their interaction with P1: a `REQUIRED` reference whose only target is archived still satisfies the requirement, so a threshold field (P1) is met by a suppressed entity in silence. The archiving change must also check that no active entity is left with only archived targets in a `REQUIRED` field | probe: a `TRIGGER` whose only `LOADS` (REQUIRED) is an archived `RULE` → `lint --strict` clean; `context` → `1 entities`, `// suppressed:` |
| S6 | starter | P6 and S6 make a `SOURCE` on `SCHEMA *` and source types mandatory (« from the first schema ») and give no branch for a project where provenance serves nothing — no unit of change, replaced documents deleted, history in version control. The step can only reopen the rulings that refused it; it should ask first whether provenance is worth recording, and what it costs per entity | this walk: S2 ruling 9 (« no `SOURCE` on `RULE` ») against S6's `[corpus: none]` line; ruled C2, no `SOURCE` and no measurement type |
| S6 | starter | The reference skeleton declares `SCHEMA DOCUMENT` with `PATH anchor` `REQUIRED`, while S8 deletes the documents the corpus replaces: every `SOURCE` citing a replaced document then fails L016, or has to be rewritten at disposal. P6 does not say that the documents a corpus replaces cannot be anchored sources | this walk: 478 statements in replaced documents (S1), each a candidate `SOURCE` whose anchor S8 would break |
| S9 | awawa | A type that narrows the wildcard's `STATUS` enum to exclude the suppressing value still accepts that value in silence: the wildcard's `WHEN STATUS archived` block fires on it, and its `GATE suppressed` silences the L007 on `STATUS` itself. Narrowing an enum (allowed by `WHEN` monotonicity) is thus ineffective for exactly the value it matters most to refuse | probe: `SCHEMA SESSION` › `FIELD STATUS active`; `STATUS foo` → L007; `STATUS archived` with `ARCHIVED_ON` → `lint --strict` clean |
| S7 | starter | Step identifiers and walk order diverge: S9 is walked at rank 7, before S7 (rank 8), S10 (rank 9) and S8 (rank 10). The rank table states « walk order is the order of this table » but not why the identifiers do not follow it, so a reader of the prompts sees S7 arrive after S9 and takes it for a numbering error. Either renumber the steps in walk order, or state beside the table that identifiers are stable and ranks are not | Flow S rank table: `S1 … S6, S9, S7, S10, S8`; bodies in the same order (`grep -n '^### S' awawa-specification-starter-v2.md` → S9 at line 568, S7 at 587, S10 at 613, S8 at 636) |
| S7 | awawa | At `--depth 1`, the `// not expanded (depth):` footer of `context` names again every reference the body has already printed: the entry package of this corpus repeated its 155 rule and 5 package names on one line, a cost paid on every read and carrying no fact the body lacks | `awawa context @SESSION.start --depth 1 --skip reasoning .` → 39 entities, 390 lines, 22 518 bytes (`wc -c`) |
| S7 | awawa | `--skip CATEGORY` on a reference field removes the field lines but the `// skipped (<category>):` footer lists every skipped target by name, so skipping references saves only part of what it drops (24 % here) | `CATEGORY loading` on `TRIGGER.LOADS*`: `awawa context @SESSION.start --depth 1 --skip reasoning --skip loading .` → 17 049 bytes, footer still naming the 160 targets |
| S7 | starter | S7 asks to measure P2 in calls only; the calls stayed as planned while the entry package doubled in lines (181 → 390) as types were filled: a call count does not see goal « reduce the context consumed ». Remeasure P2 in lines or bytes as well | this walk, « Measures (full corpus) » |
| S10 | starter | W5 closes a coverage row once it « names an entity », and S7 fills the ledger on that criterion; S10 then finds the entity often states only part of the statement. The loss is systematic, not random: reasons condensed into a `RATIONALE`, list elements and examples dropped, a clause lost when a sentence became a field. S7 should check each row against the entity text as it writes it, and S10 should say that a sample is not enough when its partial rate is high | this walk: sample 14 of 72 partial (19 %); full audit 85 of 465 rows with an entity, about 55 of them reasons |
| S10 | starter | S10 asks for each type's `DESC` and each `REQUIRED` field to be read against the entities, but the tool prints no field values for a whole type (defect S3), so the audit of 220 entities needs one `show --json` per entity from a script; the starter should name that cost or the command form | `dump.sh`: `awawa status --json` then `awawa show @TYPE.X --json` × 220 |
| S8 | starter | S8 asks for « a search of the whole project for its name » before a document is deleted; a document is also cited by the identifiers of its rules and by the names of its sections, which that search misses. S8 should search for the name, every rule identifier the document defines, and the section titles of a document partly kept | this walk: 8 citations outside the corpus, 4 of them by identifier only (`CFG-2`, `PROC-2`, `PROC-3`, `TEST-1`), 1 by a section name (`AGENTS.md` *Always*) |
| S8 | awawa | L025 refuses an entity mention inside any string field, an example included, and offers no way to mark a string as literal text: an `EXAMPLE` that shows how to cite an entity (a commit subject naming `@RULE.x`) must fall back to a placeholder or a schema field made for that one case | `EXAMPLE "docs(testing): @RULE.test_aaa_structure — …"` → L025 « mentioned in prose and indexed by nothing »; `@RULE.<name>` → clean |
| S8 | starter | The readiness criterion « `awawa refs @SOURCE_TYPE.X` on a source entity lists what cites it » has no branch for a corpus ruled to carry no source type (P6), so the check can only be marked not applicable by hand | this walk: S6 C2, no `SOURCE`, no source type |
| S8 | starter | The walk is too long for its owner: ten steps, several spanning more than one session, each opening by re-reading the protocol, the work file and the ledgers, and each closing on a seven-part ritual. The starter gives no estimate of its length against the size of the corpus, and no way to shorten it: no merging of steps for a small schema, no statement of which audits a sample may replace. The owner had to add « the user tires on long sessions: propose a stopping point » to the prompts | this walk: 2026-09-24 → 2026-09-25; S7 over three sessions; S10 about 45 tool calls, 11 sub-agents, about 560 k sub-agent tokens; S8 about 45 tool calls; 478 coverage rows filled, then audited again one by one |
