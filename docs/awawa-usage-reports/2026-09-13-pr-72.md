# 2026-09-13 — PR #72 T15: the five domain entities of core-mapping become classes

| Field                                          | Value                                                                                                                                                                                                                                                                                                                                                                                                                                      |
|------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Author of this report**                      | GitHub Copilot, in a JetBrains IDE session with no memory of the work it judges. `@DECISION.UnRapportDUsageEstCommiteDansLaPrDeTacheParUneSessionIndependante` names Claude Fable 5.1 at medium effort; the owner ruled otherwise for this pull request, and the deviation is stated here rather than hidden.                                                                                                                              |
| **Model that drove the sessions under review** | Claude Opus 5 (the `Co-Authored-By` trailer of all 18 commits, one model only).                                                                                                                                                                                                                                                                                                                                                            |
| **Sessions measured**                          | `f3cb6453` (piloting, wave launch) with its agent `ad83e66f` (implementation), `67924570` (piloting, review) with its agent `a7c51ee3` (review follow-up). The `Claude-Session` trailer of the 18 commits reads `session_01JFnUY7rRLaxKhW7EMMZpV8` for all of them, and for 11 commits already in the base: it does not identify a session — part A, third row of "What went wrong".                                                       |
| **Delivered**                                  | the five domain entities of `core-mapping` become classes with private fields, getters and constructor validation; the behaviour that follows from an entity's own state moves onto it; `PlanetEnergyGrid` becomes the planet's aggregate; no domain entity reaches `presentation/`, held by a fourth guard. Eight decisions written, six questions opened, three closed, `@TASK.T15_EntitesDuDomaineEnClasses` promoted to `implemented`. |

## Verdict in two lines

**Ours**: the method ran the whole journey in 6 h 08 of wall time and 2 h 35 of session activity, and every guard the
previous reports' remediations produced was applied at least once — the `BLOCKED_BY` cross-check within one second, the
model entity read by `show`, the `diff`-against-a-copy workaround — but the two design changes that make this pull
request worth its size came from the owner's push-back, and each reversed a decision an agent had recorded hours
earlier, which nothing in the corpus flagged.

**The tool's**: awawa 2.7.0 kept its integrity promises again (every anchor resolving, three `CLOSES` required,
`REJECTED` on each of the eight decisions, `lint --strict` at 0 on every hand-off) and charged 54 `L014` errors for
corpus text written by heredoc, costing two hand-written repair passes; its one retrieval defect on this PR is a ruled
and accepted limitation, not a new finding.

## Part A — Us: method, corpus, conduct

### What went well

| What                                                                                       | Evidence                                                                                                                                                                                                                                                    |
|--------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| The footer cross-check ruled two days ago was applied one second after the footer was read | `awawa lint --closure` on T15 and T32 at 09:55:34, `awawa status OPEN_QUESTION --where STATUS!=superseded .` at 09:55:35 — `@DECISION.LePiedDePageBlockedBySeCroiseAvecStatus` in force                                                                     |
| The implementing agent read the package, not the files                                     | One `awawa context @TASK.T15_EntitesDuDomaineEnClasses --skip reasoning --with-schema` at 09:59:13, then eight `show` — the shape `@DECISION.LesReferentsDUnContexteSeLisentParShow` prescribes                                                             |
| The model entity was read through the tool, as ruled                                       | `awawa show @DECISION.UneFusionProduitUneSaveOrdinaire` at 10:07:22 and `awawa show @OPEN_QUESTION.UneSaveSansJoueurMeriteUnAvertissement` at 14:33:11 — `@DECISION.AgentsMdNommeUneEntiteModeleParType` applied by both agents                             |
| The implementing agent flagged its own weak point as a review thread                       | 10:19:11, one minute before it ended, on `InventoryEntity.ts:25`, naming `@OPEN_QUESTION.UneEntiteRendSonTableauInterneSansLeCopier` — the owner's first comment, 3 h 45 later, is on that exact line                                                       |
| Every ruling faced its alternative                                                         | The eight decisions carry 1 to 2 `REJECTED` each; five of the six new questions carry a `RECOMMENDATION` with its own `REJECTED`, the sixth being a limitation with nothing to propose                                                                      |
| The reviewer retracted an unsupported claim in plain terms                                 | 15:28:24 the owner pushes back on the presenter thread; 15:54:10 the answer opens with « Correction first: I wrote above that "one of the two is wrong", and I cannot support that », then states the narrower fact that settles it — one port out of eight |
| A convention was given a red gate instead of a sentence                                    | `scripts/check-presentation-entities.ts` and its spec, chained as `check:presentation`; re-run here: four guards, exit 0                                                                                                                                    |
| The acceptance figures hold on re-measurement                                              | Re-run on `b3923d2` in a worktree with `bun install --frozen-lockfile`: `bun test` 771 pass / 0 fail / 110 files, `lint:types` 0, `check:guards` 0, `awawa lint --strict .` 0, `awawa status .` 0 errors / 0 warnings / 114 reference sites / 0 unresolved  |
| The corpus did not weigh the pull request down                                             | 412 corpus lines changed out of 2,945 lines in the PR, 14 % — 18 % on #65, 11 % on #62                                                                                                                                                                      |
| The private clone was refreshed before the corpus was touched                              | `bun run private:sync` at 14:21:12, first command of the review agent                                                                                                                                                                                       |

### What went wrong

| What                                                                                                                             | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Whose fault                   |
|----------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------|
| The two design changes that carry this PR came from the owner, and each reversed a decision written hours earlier in the same PR | `@DECISION.LesDeuxVuesDUnObjetDuMondeRestentDesClassesDistinctes` written 10:08:01, superseded by `@DECISION.UnObjetPlaceEstUnObjetDuMonde` in `24e14ea`; `@DECISION.LeCasDUtilisationDeSectionAssembleSesValueObjectsChampParChamp` written 10:16:37, superseded by `@DECISION.LaGrilleEnergetiqueDUnePlaneteEstUnAgregat` after « y a pas une manière plus orientée objet de régler ça ? » at 14:50:00. Both rested on a reason the same PR expired; no `lint`, no `context` footer, no `status` could say so | method, then corpus modelling |
| A ratification question was rejected by the owner and had to be re-presented                                                     | `AskUserQuestion` at 14:38:27, rejected at 14:41:57, followed by « redonne le contexte de la question avec un tableau si ça aide » at 14:42:22. 3 min 30 s spent on a question whose two options were both refused — the third, the aggregate, came from the owner eight minutes later                                                                                                                                                                                                                          | conduct                       |
| The `Claude-Session` trailer identifies no session, and the command's transcript recipe rests on it                              | The same `session_01JFnUY7rRLaxKhW7EMMZpV8` on all 18 commits of this PR and on 11 commits already in the base; it comes from a `remote_session_change` attachment present in three unrelated piloting sessions and matches no `~/.claude/projects/*/<uuid>.jsonl` name. Section 1 of `awawa-usage-report.md` says « Each id is a file … whose `sessionId` field matches »; the four transcripts had to be found by project directory instead                                                                   | method, then report           |
| Two accented characters reached the corpus and only a human caught them                                                          | `grep` for accented characters at 15:01:19 and `sed -i` at 15:01:22, then commit `9407c24`; `awawa lint --strict .` exits 0 with them. `grep -rn accent docs/*.awawa` returns nothing: the unaccented convention is written in no entity, no schema rule, no guard                                                                                                                                                                                                                                              | corpus modelling              |
| Writing corpus text by heredoc cost 54 lint errors and two repair scripts                                                        | 54 `L014` occurrences in the implementation transcript (`unterminated string`, `continuation line follows no string it can continue`), then hand-written `python3` passes at 10:12:10 and 10:12:17 to re-balance the `+ "…"` continuations                                                                                                                                                                                                                                                                      | method                        |
| The agent still read corpus files for style after reading the model entity                                                       | 10:07:27: `head -40 docs/packages.awawa`, `tail -30 docs/packages.awawa`, then `head -1` on all twelve `.awawa` files — five seconds after the `show` that was supposed to replace them                                                                                                                                                                                                                                                                                                                         | method                        |
| The worktree was cut from the wrong base again                                                                                   | 09:58:57 `merge-base --is-ancestor` on the base, 09:59:05 `git checkout -b t15-domain-entities-as-classes origin/refactor/…` — the agent recreated its branch before writing a line. The prompt carried the check; the tooling still defaults elsewhere                                                                                                                                                                                                                                                         | method                        |
| The readiness follow-up overstates the CI result by one check                                                                    | « eight CI checks pass on `b3923d2` »; `gh pr checks 72`: seven pass, `claude` reports `skipping`                                                                                                                                                                                                                                                                                                                                                                                                               | report                        |
| The corpus is still a list more than a graph                                                                                     | 84 of the 113 decisions have no incoming edge, 74 % (74 of 91, 81 %, at #65). This PR added 10 `REF` lines, bringing the corpus to 42; `@DECISION.UnObjetPlaceEstUnObjetDuMonde` has 0 incoming                                                                                                                                                                                                                                                                                                                 | corpus modelling              |

### Remediations, by return

| # | Change                                                                                                                                                                                                                    | What it fixes                                                                                                                |
|---|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------|
| 1 | Before recording a decision whose reason is a state of the branch in progress — a commit order, a type not yet converted — name that reason as temporary in the `DESC`, or hold the decision until the branch is complete | The two intra-PR reversals: two entities written, superseded and kept forever, and a review pass spent undoing them          |
| 2 | When a ratification question opposes two designs, put the comparison in a table in the message before the question, and name a third option « something else, say what »                                                  | The rejected question of 14:38:27 and the 3 min 30 s it cost; the winning option was not among the two offered               |
| 3 | Ruling to propose: the corpus is written without accented characters, and one grep in `check:guards` holds it                                                                                                             | Two characters that reached a pushed branch, a hand fix and a dedicated commit; the convention is held by memory alone today |
| 4 | `/awawa-usage-report` section 1: locate transcripts by the project directory of the worktree (`~/.claude/projects/<slugified path>/`), and keep the `Claude-Session` trailer as a date hint only                          | A recipe that no longer works: the same id on 29 commits across three pull requests                                          |
| 5 | Write a corpus entity from a file composed in one go, or through `awawa new` plus an editor pass, never by an interactive heredoc carrying `+ "…"` continuations                                                          | 54 `L014` errors and two hand-written repair passes                                                                          |
| 6 | Drop the `head`/`tail` reads of `docs/*.awawa` from the implementing prompt now that `AGENTS.md` names a model entity per type                                                                                            | Three file reads the ruling of #69 was meant to replace, third report in a row                                               |

No `OPEN_QUESTION` was opened for these six remediations: the owner instructed this session to write the report and
nothing else. They therefore live in this file only — which is exactly the failure mode section 4 of the command names,
the next session reading the corpus and not this folder.

## Part B — The tool

### What went well

| Promise                           | Kept | Evidence                                                                                                                                                                                                                                    |
|-----------------------------------|------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Anchor integrity                  | Yes  | 18 `IMPL` anchors added and every anchor of the corpus resolving on `b3923d2`; `L016` seen 4 times in the implementation transcript and 17 times in the review one, on a PR that deleted eight rule files and moved `energyOptimizerConfig` |
| Closure integrity                 | Yes  | Three questions closed, each by a `CLOSES` on the decision that settles it; `awawa refs @OPEN_QUESTION.UnPresenterPortRecoitUneEntiteOuUnValueObject` shows exactly one incoming edge                                                       |
| Forcing the alternative           | Yes  | `REJECTED` on all eight decisions; the `RECOMMENDATION` field ruled at #69 carries its required `REJECTED` on five of the six new questions                                                                                                 |
| Supersession rather than deletion | Yes  | Two whole-entity supersessions inside the PR, both with `SUPERSEDES`; `L026` seen five times in each transcript                                                                                                                             |
| Cheap inventory                   | Yes  | `awawa status` 12 times across the four sessions; the wave scope settled on `status` plus `lint --closure` in 90 seconds                                                                                                                    |
| Schema-correct skeleton           | Yes  | `awawa new` once by the implementing agent, twice by the review agent, before every append                                                                                                                                                  |
| Format as a gate                  | Yes  | `awawa fmt` 9 times in the implementation, 11 in the review; the corpus diff touches exactly three files, no reformat noise across two branches in flight                                                                                   |

### What went wrong

| Defect                                                                         | Observation                                                                                                                                                                                                               | Checked against                                                                                                                                                                                            |
|--------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| The `BLOCKED_BY` footer still counts `superseded` referrers with the live ones | `awawa lint --closure @TASK.T15_EntitesDuDomaineEnClasses .` prints `BLOCKED_BY (rest, 6)`; all six checked one by one, all six `superseded`                                                                              | run on `b3923d2`; ruled an accepted limitation by `@DECISION.LePiedDePageBlockedBySeCroiseAvecStatus`, whose guard was applied at 09:55:35 — the cost this time was one extra command, not a wrong verdict |
| `L014` cannot say which string it means                                        | 54 occurrences, all `unterminated string` / `continuation line follows no string it can continue`, pointing at a line inside a `DESC` spanning four; the agent answered with two `python3` scripts rather than with `fmt` | implementation transcript, 10:11:58–10:12:22                                                                                                                                                               |
| Nothing in the tool holds a project's writing conventions                      | The unaccented spelling of the corpus is a convention the schema cannot express: no `SHAPE` applies to the free text of a `DESC`, and `lint --strict` exits 0 on accented entries                                         | run; `docs/_schema.awawa` declares no constraint on `DESC` content                                                                                                                                         |

The third defect is half ours: the convention is recorded nowhere in the corpus either, so the tool was never asked.
Remediation 3 of part A is where it belongs.

## Recurring points

### Recurring pain points

| Point                                                          | Times seen | First raised in                      | Seen again here                                                                                                                                 | Remediation proposed then                                               | Done?                                                                                                                                   |
|----------------------------------------------------------------|------------|--------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------|
| The corpus is a list, not a graph — **flagged**                | 3          | `2026-09-12-pr-62-review.md`, part A | 84 of 113 decisions with no incoming edge (74 %), one of the eight new decisions among them; 10 `REF` added, 42 in the corpus against 13 at #65 | `APPLIES_TO` with `CONVERSE GOVERNED_BY` on `DECISION`                  | no — the density improves by hand, the edge type does not exist                                                                         |
| The agent reads corpus files for style — **flagged**           | 3          | `2026-09-12-pr-62-review.md`, part A | `head -40`, `tail -30`, `head -1` on twelve files at 10:07:27, five seconds after the model entity was read by `show`                           | A model entity per type or `docs/_example.awawa`                        | yes, and insufficient — `@DECISION.AgentsMdNommeUneEntiteModeleParType` is applied and the file reads continue                          |
| The worktree base trap — **flagged**                           | 3          | `2026-09-12-pr-62-launch.md`, part A | `git checkout -b … origin/<base>` at 09:59:05, after the ancestor check failed at 09:58:57                                                      | Fix in `commands.md` and the launch skills                              | partly, third report running — the prompt carries the check, the tooling still cuts from the default branch                             |
| Instructions stale since the corpus moved — **flagged**        | 3          | `2026-09-12-pr-62-review.md`, part A | `awawa-usage-report.md` section 1 locates transcripts by a `Claude-Session` trailer identical on 29 commits across three pull requests          | Strip project facts from the commands, or confront them with the corpus | no — a new instance, in the command this report itself follows                                                                          |
| The `BLOCKED_BY` footer ignores the referrers' `STATUS` (tool) | 3          | `2026-09-12-pr-62-review.md`, part B | `(rest, 6)` on T15, all six `superseded`                                                                                                        | Footer split live / suppressed                                          | closed as a limitation, not as a fix — `@DECISION.LePiedDePageBlockedBySeCroiseAvecStatus` records the guard, and the guard was applied |
| Referrers named, not expanded (tool)                           | 3          | `2026-09-12-pr-62-review.md`, part B | One `context` then eight `show` in the implementation, seven in the review                                                                      | `context --with-referrers`                                              | closed as accepted — `@DECISION.LesReferentsDUnContexteSeLisentParShow` makes the chain the prescribed shape                            |

Raised before and not seen again, closed by observation: one task for several pull requests —
`@DECISION.UneTacheEstUneEntiteParPullRequest` is ruled and T15 carries a single `PR "72"`; the recommendation hidden in
`DESC` prose — `FIELD RECOMMENDATION` with its required `REJECTED` exists in `docs/_schema.awawa` and five of the six
new questions use it, two prose prefixes remaining out of fourteen; directory anchors on which `L016` cannot fire — the
two added here fall under the exception `@DECISION.UneAncreSousSpecNommeUnFichierOuUnSymbole` states, both on `SPEC`
lines asserting an absence and naming the directory in their text; a review without feedback launching an agent anyway —
this review had six threads; the usage report grading its own author — this session took no part in the work; the
first-option bulk ratification — none offered, the single ratification question was refused on its content, not on its
form; `awawa diff` needing a hand-copied directory — the copy is the ruled shape now and the review agent used it at 14:
35:01; an `implemented` decision left carrying a sentence made false —
`@DECISION.UnDescRenduFauxEstSupprimeDansLeMemeCommit` is ruled and the two reversals here were whole-entity
supersessions, so it was not exercised.

### Recurring strengths

| Mechanism                                                                              | Times seen | First noted in                       | Held again here                                                                                                                                                      |
|----------------------------------------------------------------------------------------|------------|--------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Incoming blockers known before code, from the footer — **established**                 | 4          | `2026-09-12-pr-62-launch.md`, part A | `lint --closure` by the pilot at 09:55:34 and by the agent at 09:59:21, before the first `Write` at 10:04:19                                                         |
| Required `REJECTED` forcing the alternative — **established**                          | 4          | `2026-09-12-pr-62-launch.md`, part A | 1 to 2 `REJECTED` on each of the eight decisions; the `RECOMMENDATION` of five questions carries its own                                                             |
| `L026`: no closure without naming what closed it — **established**                     | 4          | `2026-09-12-pr-62-launch.md`, part A | Three `CLOSES`, three questions `superseded`, `L026` seen five times per transcript                                                                                  |
| `L016` anchor integrity — **established**                                              | 4          | `2026-09-12-pr-62-launch.md`, part A | Eight rule files deleted and one moved; the anchors that pointed at them leave the corpus in the same commits, `lint --strict` 0                                     |
| The agent measures rather than asserts — **established**                               | 4          | `2026-09-12-pr-62-launch.md`, part A | `bun test` / `lint:types` / `check:guards` / `awawa lint --strict` run at 10:02:57 before the first entity was written, and again at 10:19:15, 14:34:37 and 15:02:28 |
| `fmt` then `git diff --stat` on the corpus, no cross-branch conflict — **established** | 4          | `2026-09-12-pr-62-launch.md`, part A | 20 `fmt` calls across the two agents; T15 and T32 appended to `docs/packages.awawa` in the same wave without conflict                                                |
| Cheap inventory through `status`                                                       | 3          | `2026-09-12-pr-62-review.md`, part B | 12 calls; the wave scope settled in 90 seconds                                                                                                                       |
| The corpus does not weigh the PR down                                                  | 3          | `2026-09-12-pr-62-review.md`, part A | 14 % of the lines of #72                                                                                                                                             |
| Task `SPEC` lines handed as acceptance criteria                                        | 3          | `2026-09-12-pr-62-launch.md`, part A | The PR body answers the task's `SPEC` lines one by one, and the review re-anchored every `SPEC` of the six deleted rules on `PlanetEnergyGrid.levels()`              |

Noted before and not seen holding here: the ruling facing the code before it is asked — the review's first ratification
question opposed two designs the owner refused both of, and the examination that would have found the aggregate happened
only after the push-back; arbitration no longer being the bottleneck — 19 min 59 s of owner time in the terminal across
two questions, plus six review threads, against 100 seconds on #65.

## Follow-up on the previous report's remediations

The previous report is `2026-09-12-pr-65.md`: nine remediations in part A, three in part B. Every one of the twelve
questions it opened is now `superseded`, each closed by a named decision — that loop closed in full between #65 and #72.

| Remediation                                                      | Done?                      | Evidence                                                                                                                              |
|------------------------------------------------------------------|----------------------------|---------------------------------------------------------------------------------------------------------------------------------------|
| A1. Partial replacement of an `implemented` decision             | yes                        | `@DECISION.UnDescRenduFauxEstSupprimeDansLeMemeCommit`; not exercised here, both reversals being whole-entity                         |
| A2. No agent for a review with no feedback                       | yes                        | `@DECISION.LaRevueSansRetourNeLanceAucunAgent`; not exercised, six threads to answer                                                  |
| A3. One task entity per pull request                             | yes                        | `@DECISION.UneTacheEstUneEntiteParPullRequest`; T15 carries one `PR "72"`                                                             |
| A4. `RECOMMENDATION` field with required `REJECTED`              | yes                        | `docs/_schema.awawa:76`; used by five of the six new questions; two prose prefixes left in `docs/questions.awawa`, down from fourteen |
| A5. Forbid a directory anchor under `SPEC`                       | ruled differently          | `@DECISION.UneAncreSousSpecNommeUnFichierOuUnSymbole` allows a directory for a layer-wide obligation; the two added here comply       |
| A6. State what the examination found before a ratifying question | partly                     | The question carried its two options and their examination; the owner refused both and asked for a table                              |
| A7. Strip the stale line from `awawa-pr-merged.md`               | yes                        | `@DECISION.LaCommandeDeFusionPousseLeDepotDuCorpusParSonFluxDeTravail`                                                                |
| A8. Clean an orphan report branch before cutting one             | yes                        | `@DECISION.LeRapportDUsageNettoieUneBrancheOrphelineAvantDeCouperLaSienne`; moot here, the report is committed on the PR branch       |
| A9. A model entity per type                                      | yes, insufficient          | `@DECISION.AgentsMdNommeUneEntiteModeleParType` applied by both agents, and three corpus files still read for style                   |
| B1. Footer split by gating                                       | no, closed as a limitation | `@DECISION.LePiedDePageBlockedBySeCroiseAvecStatus`; awawa still 2.7.0                                                                |
| B2. `awawa diff --git <rev>`                                     | no, closed by a workaround | `@DECISION.AwawaDiffSeLanceContreUneCopie`; the review agent copied `docs/questions.awawa` to its scratchpad at 14:35:01              |
| B3. `context --with-referrers`                                   | no, closed as accepted     | `@DECISION.LesReferentsDUnContexteSeLisentParShow`                                                                                    |

## Appendix

### Measurements

| Session                                                  | Active time | User turns | Tool calls                                                                                  | awawa calls by subcommand                          | Tokens read from cache | Tokens written |
|----------------------------------------------------------|-------------|------------|---------------------------------------------------------------------------------------------|----------------------------------------------------|------------------------|----------------|
| piloting `f3cb6453` (09:53:07Z–10:20:50Z)                | 15 min 21 s | 10         | 28 (24 `Bash`, 2 `Agent`, 1 `AskUserQuestion`, 1 `Skill`)                                   | status 7, lint 6, show 6                           | 4,372,792              | 76,298         |
| agent `ad83e66f` (implementation, 09:58:47Z–10:19:49Z)   | 21 min 02 s | 2          | 125 (109 `Bash`, 15 `Write`, 1 `Skill`)                                                     | lint 15, fmt 9, show 8, context 1, status 1, new 1 | 29,308,816             | 90,921         |
| piloting `67924570` (review, 14:20:03Z–16:01:31Z)        | 59 min 30 s | 13         | 26 (20 `Bash`, 2 `AskUserQuestion`, 2 `SendMessage`, 1 `Agent`, 1 `ToolSearch`)             | none                                               | 4,345,692              | 53,957         |
| agent `a7c51ee3` (review follow-up, 14:20:49Z–16:01:12Z) | 58 min 52 s | 8          | 177 (166 `Bash`, 3 `Monitor`, 3 `TaskStop`, 2 `Write`, 1 `Read`, 1 `Skill`, 1 `ToolSearch`) | lint 19, fmt 11, show 7, status 4, new 2, refs 2   | 56,045,968             | 149,858        |

Wall time of the journey: 09:53:07Z to 16:01:31Z, 6 h 08 min, of which 3 h 44 min of waiting between the pull request
(10:18:38Z) and the owner's first review comment (14:04:42Z). Session activity: 2 h 34 min 45 s. Owner arbitration in
the terminal: 35 s on the wave question (09:56:58Z–09:57:33Z), 14 min 38 s on the energy ruling (14:38:27Z–14:53:05Z, a
rejected question included), 4 min 46 s on the presentation reading (15:44:06Z–15:48:52Z) — 19 min 59 s, plus six review
threads written between 14:04:42Z and 14:18:24Z.

| Corpus lines changed                                                   | PR lines changed                                    | Share |
|------------------------------------------------------------------------|-----------------------------------------------------|-------|
| 412 (`packages.awawa` +294, `questions.awawa` +103, `taches.awawa` 15) | 2,945 (1,877 insertions, 1,068 deletions, 50 files) | 14 %  |

### Claims checked

| Claim                                                                                                                           | Check                                                                                                                                                                                                     | Verdict                                        |
|---------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------|
| PR body: `bun test` 771 pass, 0 fail                                                                                            | Re-run on `b3923d2` after `bun install --frozen-lockfile` in the worktree: 771 pass, 0 fail, 110 files                                                                                                    | confirmed                                      |
| PR body: `lint:types` 0, `check:guards` 0 (four guards), `awawa lint --strict .` 0, `build:ui` 0                                | The first three re-run here, all exit 0, `check:presentation` among the four; `build:ui` not re-run, the `build` check passes in CI                                                                       | confirmed, `build:ui` by CI only               |
| Readiness follow-up: « eight CI checks pass on `b3923d2` »                                                                      | `gh pr checks 72`: audit, build, claude-review, fallow, guards, lint, test pass; `claude` reports `skipping`                                                                                              | overstated — seven pass, one skipped           |
| PR body: `PlayersPresenterPort` was the only one of the eight presenter ports declaring an entity                               | On the base, `Entity` appears twice in `PlayersPresenterPort.ts` and zero times in the other seven                                                                                                        | confirmed                                      |
| PR body: `computeOptimizerBoosts` ran three times per planet                                                                    | On the base: `computeEnergyProductionLevel` → `computeEnergyFuseCountsByProducerId` → boosts; `computeOptimizers` → `computeEnergyFuseCountsByProducerId` → boosts; `computeOptimizers` → boosts directly | confirmed                                      |
| PR body: no spread of an entity or a value object left in `domain/rules` or `application`                                       | Eight spreads remain, all on save records (`worldObject`, `inventory`, `messageA`) or on `Set` / `Map` iteration                                                                                          | confirmed                                      |
| PR body: `@TASK.T15_EntitesDuDomaineEnClasses` moves to `implemented`                                                           | `awawa show`: `STATUS implemented`, `PR "72"`, `AFTER @TASK.T37_…`                                                                                                                                        | confirmed                                      |
| PR body: six decisions and three open questions listed                                                                          | Eight decisions written in fact — two superseded inside the same PR — and six questions opened, three closed by a `CLOSES`                                                                                | confirmed, the body listing the survivors only |
| Review thread: « the reason it rested on had expired », about `@DECISION.LesDeuxVuesDUnObjetDuMondeRestentDesClassesDistinctes` | The decision was written at 10:08:01, when `PlacedWorldObjectEntity` was still an interface; `bd902d6` converted it 40 minutes later, in the same branch                                                  | confirmed                                      |
| Review thread: « one port out of eight » settles the presenter question                                                         | Counted on the base: 1 of 8                                                                                                                                                                               | confirmed                                      |
| Readiness comment: « every thread carries an answer »                                                                           | Six threads opened by the owner between 14:04:42Z and 14:18:24Z, each answered by `Clauche-bot` between 14:36:14Z and 14:37:10Z; the seventh exchange (15:28:24Z) answered at 15:54:10Z                   | confirmed                                      |
| `2026-09-12-pr-65.md`: « the `BLOCKED_BY` footer lists `superseded` questions as if live »                                      | `lint --closure` on T15: `(rest, 6)`, six checked, six `superseded`                                                                                                                                       | confirmed                                      |
| `2026-09-12-pr-65.md`: « 14 `recommandation :` prefixes remain »                                                                | Two remain in `docs/questions.awawa`                                                                                                                                                                      | superseded by the schema change                |
| `2026-09-12-pr-65.md`: « 44 of the 180 `IMPL` anchors point at a directory »                                                    | Not recounted corpus-wide; of the 18 anchors added here, 2 are directories, both under a `SPEC` asserting an absence                                                                                      | unchecked corpus-wide                          |
| Command section 1: each `Claude-Session` id names a transcript file whose `sessionId` matches                                   | `session_01JFnUY7rRLaxKhW7EMMZpV8` matches no file name and appears in three unrelated piloting sessions and on 11 base commits                                                                           | wrong                                          |
| `test:ui` on chromium, firefox and webkit                                                                                       | Not run here; no UI-test check in the PR's status rollup                                                                                                                                                  | unchecked                                      |
| Merge output byte-identical to the reference saves                                                                              | Not run here; this PR changes no merge rule                                                                                                                                                               | unchecked                                      |

