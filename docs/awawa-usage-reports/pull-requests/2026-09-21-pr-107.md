<!-- awawa-usage-report -->

# 2026-09-21 — PR #107 docs(tasks): split FEAT55 into wave 5; ratify FEAT55, FEAT65, TEST67

A refinement pull request, not a delivery: no `TASK` names 107 in `DELIVERED_BY` (`corpus-tasks-of-pr.json`: 0 entities on the head tree; the same on the base). The owner ruled that the report is warranted because awawa carried the refinement, so the tasks the PR refines, splits and ratifies take the place of the delivered task: `@TASK.FEAT55` (narrowed, `draft` → `todo`), `@TASK.FEAT65` and `@TASK.TEST67` (new, `todo`), `@TASK.FEAT66` and `@TASK.FEAT68` (new, `draft`). No review round ran on GitHub (`pr-rendered.md`: 0 reviews, 0 threads, one netlify comment); the review of this PR was the owner's own reading in the working session — the two turns of 12:21:53Z and 12:34:14Z and the free-text answer of 12:40:28Z (`timeline-01`, `timeline-02`) — and no readiness comment exists.

| Field                                          | Value |
|------------------------------------------------|-------|
| **Author of this report**                      | `Claude Fable 5.1` (`claude-fable-5-1`), effort `high` (from `~/.claude/agents/awawa-usage-reporter.md`) |
| **Models that drove the sessions under review** | `Claude Opus 5` (`claude-opus-5`), effort `medium` — the 3 commits of the range (`Co-Authored-By` trailers, `git-log.txt`); `"effort":"medium"` on 126 of 126 request records of session `146a20c2` and 79 of 79 of `f0befb44`<br>`Claude Sonnet 5` (`claude-sonnet-5`), effort `medium` — the 3 discovery agents `agent-a2f08876` (102 of 102 records), `agent-aaded4e0` (31 of 31), `agent-ae3b54bc` (33 of 33), launched with `model: sonnet`<br>`Claude Haiku 4.5` (`claude-haiku-4-5-20251001`), effort `unchecked` (no `effort` field on its 71 records) — the `Explore` agent `agent-a79d74f2`, launched with `model: haiku` |
| **Sessions measured**                          | Commit trailer `session_01Sy8BVna2L9N7qUS9y88GEu` (the 3 commits)<br>`146a20c2` (interactive, `/awawa-refine-task 55` first run: discovery and refinement, 11:57–12:35Z, cwd the main worktree then `EnterWorktree`) and its 4 discovery agents `agent-aaded4e0` (wiki), `agent-a2f08876` (PCEdit and akarnokd repositories), `agent-ae3b54bc` (Steam news), `agent-a79d74f2` (repository impact) — the four measured by hand with `measure.py`, `transcripts.md` listing them as read-only mentions<br>`f0befb44` (interactive, `/awawa-refine-task 55` second run: the split and the ratifications, 12:35–12:45Z, in the `refine-FEAT55` worktree) |
| **Delivered**                                  | Corpus only, 5 `.awawa` files, +238 −11 lines (`git-diff-stat.txt`): `@TASK.FEAT55` rewritten to the `logisticsPaused` format work and ratified; `@TASK.FEAT65`, `@TASK.TEST67` new `todo`; `@TASK.FEAT66`, `@TASK.FEAT68` new `draft`, each behind its own `OPEN_QUESTION`; `@GAME_RELEASE.2.102`; 15 `URL` entities pinned by revision or commit; `@HYPOTHESIS.TerraTokensStayWithinTheExactIntegerRangeOfADouble`; the `RATIONALE` of `@RULE.APlanetNumericIdIsStableAcrossSaves` corrected (`corpus-diff.txt`: 23 added, 2 changed, 0 removed); no file outside the corpus (`git diff … -- . ':(exclude)*.awawa'`: empty) |

## Verdict in two lines

**Ours**: a one-`SPEC` draft written from memory became five tasks of wave 5 with 16 sources pinned to a revision in 48 min 20 s of two sessions (11:57:14Z to 12:45:34Z), the discovery delegated to four read-only agents on Sonnet and Haiku (6.33 M of the 16.86 M cache-read tokens), the false `RATIONALE` of a live `RULE` corrected by a recomputation this report reproduces (6 of 6 planet ids), 0 `DECISION` written and the conflict of `@TASK.FEAT66` with `@DECISION.AnUnprintedEnergyValueIsNeitherInferredNorZero` stated in its `RATIONALE`; but a finding of the discovery (`deprecated("2.102", "Phytoplankton2", "Phytoplankton3")`, hand-back 12:06:16Z) missed the ruling questions of 12:07:46Z and entered the corpus 28 minutes later on the owner's question, the recommendation on those ids rested on an untracked save named « Test merge » until the owner asked where it came from, the figure « the reference saves reach 735473 » written into a `RATIONALE` is one save's value where the seven reference saves reach 3,024,414, the split came in a second session opened on the owner's initiative after the first run had recommended keeping six `SPEC` draft behind one, and the worktree-isolation check refused 10 commands in 215 answers, a point flagged for the fourth report in a row.

**The tool's**: awawa 2.7.0 rendered the split at field level — `diff` lists 25 entities with `STATUS draft -> todo`, the three field rewrites and 17 new `REF` lines in 0.0 s, `status --where WAVE==5` returns the five tasks, `context --skip reasoning --skip provenance` on `@TASK.FEAT66` prints 6 lines and the one `BLOCKED_BY` edge no traversal finds, L006 refuses an `OPEN_QUESTION` without `BLOCKS` (probe), and the three commits lint and format clean — and still costs the same three things: `lint --closure` has no `--skip`, so the footer of `@TASK.FEAT55` lists 185 unrelated names, `status` counts the 106 `DECISION` at their default `STATUS` as `(none)`, and `fmt` wrapped 54 of the 243 added lines with no option; plus one small one, `status TASK` has no numeric order, so « the last number of the sequence » that `agents/corpus-ecrire.md` sends there was read through `grep | sort -n` at 12:36:35Z.

## Part A — Us: method, corpus, conduct

### What went well

| What | Evidence |
|------|----------|
| The discovery was delegated to four read-only agents on cheaper models, in parallel, within the owner's cap of four | `timeline-01`: `Agent` calls at 11:59:21Z, 11:59:32Z, 11:59:38Z (`general-purpose`, `model: sonnet`) and 11:59:48Z (`Explore`, `model: haiku`); briefs of 1.1 to 2.1 k characters naming the source to read, the fetch that works (MediaWiki `api.php` with a browser user agent, `gh api` on pinned shas) and « read-only, no repository writes »; hand-backs 12:01:22Z, 12:01:32Z, 12:01:42Z, 12:06:16Z; 116 answers, 6,333,856 cache-read, 49,441 written (this report's `measure.py` runs) — 37.6 % of the journey's cache-read, on models below the session's |
| Every source became an entity pinned to a revision, with the reading date in the prose | `corpus-diff.txt`: 15 `URL` — 7 fandom pages by `oldid` (`@URL.WikiSkeoMoon` 19068 … `@URL.WikiGameUpdates` 19232), 5 PCEdit files and 2 akarnokd addresses by commit sha (`ca7760bd…`, `3f10f457…`, `16fa2a63…`), the Steam news API — and `@GAME_RELEASE.2.102`; every `SOURCE` of the five tasks ends on « read on 2026-09-21 » (`corpus-entities-of-diff.txt`), as `agents/corpus-ecrire.md` asks; 0 `URL` at base (`awawa status URL .` on `trees/base`) |
| The task's premises were checked against the code before the questions, and they hold | `awawa-refine-task-FEAT55.md` step 3 (12:01Z): section 0 schema `additionalProperties false`, `mergeGlobalMetadata.ts` returns the five known fields, `planetNamesByNumericId.json` 5 rows; this report: `packages/shared-save-processing/schemas/section0-player-progression.schema.json` lists 5 properties and `"additionalProperties": false`; `bun validate` on the private 2.103 sample prints exactly `[Global metadata (section 0), entry 0] must NOT have additional properties`; `EnergyLevelsPresenter.ts:44` falls back to `resolveEnergyLevelsSectionUnnamedPlanetName` — the `AFTER @TASK.FEAT65` of `@TASK.TEST67` |
| A false `RATIONALE` of a live `RULE` was corrected by a recomputation, not by a citation alone | `corpus-diff.txt`: `@RULE.APlanetNumericIdIsStableAcrossSaves` `RATIONALE` « No known deterministic hash reproduces the five identifiers » → « a two-accumulator djb2 over its UTF-16 code units, which reproduces the six known ids »; step 6 « verified locally »; this report's `probe.py` on `PlanetHash.cs` at `ca7760bd`: Prime, Selenea, Aqualis, Humble, Toxicity, Skeo — 6 of 6 equal to the table and to `-440810600` |
| Questions went to the owner in one call each, a recommendation first, and two recommendations were overruled on the merits | `transcripts-measure.txt`: 4 questions at 12:07:46Z (merge of `logisticsPaused`, planet id, `terraTokens` uint64, energy), 3 at 12:37:49Z (split, fate of FEAT55, deprecated ids), 3 ratifications at 12:43:41Z; the owner chose « Mesure en jeu requise » over the recommended « Hors tâche, absentes » (12:13:01Z) and answered the deprecated-ids question with a question (12:40:28Z) |
| The owner's free-text answer was treated as the question it was, and re-asked with its context | 12:40:28Z « la save 2.103 c'est quoi? elle vient d'où? » → five commands in 26 s (12:40:32–12:40:54Z: `git -C .do-not-commit show`, `ls`, `grep` on `input/Skeo/skeo.json`) establishing « untracked, 2.8 MB, `saveDisplayName` "Test merge", not in the private repository » → the question of 12:41:12Z opens on that provenance (REP-4); answer 12:42:23Z « Non / incertain : laisser ouverte » → `@TASK.FEAT68` `draft`, `@OPEN_QUESTION.DeprecatedGroupIdsOfTheSkeoUpdateAreHandled` `BLOCKS` it |
| Each open question blocks the task it holds up, and the three unblocked tasks were ratified one by one | `awawa context @TASK.FEAT66 --skip reasoning --skip provenance .`: `// BLOCKED_BY (root, 1): @OPEN_QUESTION.TheEnergyOfTheSkeoMachinesIsMeasuredInGame`; `awawa refs @TASK.FEAT68 .`: `BLOCKS` from `@OPEN_QUESTION.DeprecatedGroupIdsOfTheSkeoUpdateAreHandled`; `awawa status TASK --where WAVE==5 .`: FEAT55, FEAT65, TEST67 `todo` with no `BLOCKED_BY`, FEAT66, FEAT68 `draft` with one each; at `cd68eb3` both questions blocked FEAT55 |
| The conflict with a live decision was stated, and no `DECISION` was written or rewritten | `@TASK.FEAT66` `RATIONALE`: « The recorded decision keeps a machine the wiki prints no value for out of the energy tables; a value measured in game … is a source that decision does not foresee », `REF @DECISION.AnUnprintedEnergyValueIsNeitherInferredNorZero`; final text 12:45:34Z « Conflit avec une décision en vigueur, sur FEAT66 »; `corpus-diff.txt`: 0 `DECISION` among the 25 entities; the ruling on `logisticsPaused` (save A, else B, else absent) lives in `SPEC` 2 of `@TASK.FEAT55`, scoped to the task |
| The progress file carried the run across two sessions | `~/.local/state/ai/awawa-refine-task-FEAT55.md`: steps 1–10 by `146a20c2`, « run 2 » steps 11–14 by `f0befb44`; the second run tried to read it in its first command (12:35:50Z, refused, re-run plain) and asked none of the run-1 rulings again |
| Task numbers and the wave number follow the sequence | Step 11: « last task number 64, last WAVE 4 »; `awawa status TASK .` at head: `CHORE64` the highest below the four new ids FEAT65–FEAT68; `--where WAVE==4`: 10 tasks, all archived; `--where WAVE==5`: the 5 of this PR |
| Every commit lints and formats clean | This report on the extracted trees of `1835f91`, `cd68eb3`, `2401f43`: `awawa lint --strict .` exit 0 and `awawa fmt --check .` exit 0, 3 of 3; `corpus-status.txt` at head: 626 reference sites, 0 unresolved |
| The PR body described the head at each push | Created 12:17:24Z (`gh pr create --body-file`), edited 12:34:47Z (« blocked by two open questions », `timeline-01`), rewritten 12:45:11Z with the wave table, the corpus table, the findings and the rulings (`timeline-02`, `pr-rendered.md`) |
| The session left the main worktree before its first write and ran the per-worktree setup | `transcripts.md`: cwd of `146a20c2` is the main worktree; first command 11:57:29Z a read (`awawa context`); `EnterWorktree` 11:58:18Z; `bun install --frozen-lockfile` 11:58:25Z, before the first `bun validate` (`timeline-01`, this report's extract of the tool inputs) |
| Checks green at head | `gh pr checks 107`: audit, build, claude-review, fallow, guards, lint, scenarios, test pass on `2401f43`; this worktree: `bun test` 836 pass 0 fail, `lint:types` exit 0 on 7 packages, `audit:quality` exit 0, `git status --porcelain` empty before and after (`checks-*.txt`) |

### What went wrong

| What | Evidence | Whose fault |
|------|----------|-------------|
| A finding of the discovery missed the ruling questions and entered the corpus 28 minutes later, on the owner's question | Hand-back of `agent-a2f08876` at 12:06:16Z carries `deprecated("2.102","Phytoplankton2","Phytoplankton3")` (transcript `146a20c2`, this report's extract); the 4 questions of 12:07:46Z and the commit `1835f91` (12:15:47Z) say nothing of it; the assistant's own table at 12:22:11Z lists the two ids as « retirés »; the owner at 12:34:14Z: « on va devoir gérer des objets deprecated, c'est mentionné dans la tâche? »; `cd68eb3` at 12:34:35Z adds it and `@OPEN_QUESTION.DeprecatedGroupIdsOfTheSkeoUpdateAreHandled`; one extra commit, one PR-body edit, one owner turn | conduct |
| The recommended answer on the deprecated ids rested on a file of unknown provenance | Step 11: « `input/Skeo/skeo.json` (2.103, logisticsPaused false) carries Phytoplankton2 x144, Phytoplankton3 x86 -> game keeps deprecated gIds »; the recommended option at 12:37:49Z: « Garder, question archivée … La save 2.103 les porte encore (144 et 86) : le jeu les garde »; the file is untracked, dated 2026-09-20 21:21, `saveDisplayName` "Test merge", absent from the private repository whose sample (`Skeo-Together-Skip-Prime.json`, 31 lines) carries no `Phytoplankton` (12:40:32–12:40:54Z); `~/.claude/commands/awawa-refine-task.md` § 3: « Verify every fact … by running the command that proves it », § 4: « a verified recommendation first » | method |
| A figure written into a `RATIONALE` is one save's value stated as the perimeter's | `@HYPOTHESIS.TerraTokensStayWithinTheExactIntegerRangeOfADouble` `RATIONALE`: « the reference saves reach 735473 »; 735473 is the `allTimeTerraTokens` of `Standard-4.json`, the save read at 11:58:44Z (transcript `146a20c2`); `grep -oE '"(allTime)?[tT]erraTokens":[0-9]+'` over the 7 private saves: `Standard-2-merged.json` 3,024,414 and 2,339,989, `Standard-3.json` 2,573,460 and 1,988,910; the hypothesis holds (every value below 2^53) and the figure is wrong by a factor of four (REP-5: a figure carries the perimeter it was measured on) | conduct |
| The first run recommended keeping six `SPEC` draft behind one, and the split came in a second session on the owner's initiative | 12:15:06Z: « Reste draft, OPEN_QUESTION (Recommended) » — the whole task (6 `SPEC` at `1835f91`, `awawa show @TASK.FEAT55` on its tree) blocked by the energy measurement; the third option « todo sans la SPEC énergie » was the split; the owner at 12:34:14Z: « Potentiellement la tâche va devoir être splittée en plusieurs tâches avec un numéro de wave »; second run `f0befb44`: 36 answers, 9 min 58 s active, 3,586,165 cache-read, 30,258 written; the command's § 4 has no line on splitting when a minority of the `SPEC` is blocked | method |
| 10 commands refused by the worktree-isolation check in 215 answers, one of them the command's own base check | `grep -c 'Refusing to run it'`: `146a20c2` 3 (12:06:23Z, 12:13:31Z, 12:15:34Z), `f0befb44` 4 (12:35:50Z, 12:35:58Z `git merge-base --is-ancestor origin/master HEAD; echo $?; git log …` — § 1.2 of the command chained, 12:43:24Z `awawa fmt … ; git diff --stat`, 12:45:19Z `gh pr view … --jq`), `agent-a2f08876` 2, `agent-aaded4e0` 1; plus one answer lost at 11:57:29Z to `echo =====` (`(eval):1: ===== not found`), the zsh separator raised in `2026-09-21-pr-101-102-103.md`; the rule (delairec/.ai#34 `abde632`) has been in `commands.md` since 2026-09-17 | conduct |
| The first session passed its fiftieth answer without asking whether to hand over | `146a20c2`: 63 answers, the 50th at 12:15:44Z (this report's count of distinct assistant message ids), 6,939,244 cache-read; no text matching « cinquant », « fifty », « hand over », « nouvelle session » before the owner's own 12:34:14Z « Donne moi un prompt en ce sens pour une nouvelle session »; `commands.md` § Sessions: « At the fiftieth answer of a session, ask the user once » | conduct |
| Three `grep` reads of corpus files where `awawa show` and `status` answer | 12:14:01Z `grep -n "^TASK FEAT55" -A 20 docs/awawa-project-methodology/tasks.awawa`, 12:34:20Z `grep -n "a label row. The wiki" … tasks.awawa`, 12:36:39Z `grep -rhoE "WAVE [0-9]+" … tasks.awawa` (this report's extract of the tool inputs; 0 `Read` of a `.awawa` file); CORPUS-3 and CORPUS-4 name `awawa show @TASK.FEAT55 .` and `awawa status TASK --where WAVE==<n> .` for the three; the point was flagged eight times before the migration (CORPUS-3) and 0 times in the three reports since | conduct |
| The same measurement gap has two homes in the corpus | `@OPEN_QUESTION.TheEnergyOfTheSkeoMachinesIsMeasuredInGame` `DESC` « how much power do TreePlanter3, PodUnderground and RocketAnimals2 draw or produce » — a measurement gap, which CORPUS-1 keeps out of `OPEN_QUESTION` (the owner was asked at 12:15:06Z and chose it); the same gap for eleven other machines is `@LIMITATION.ElevenPlacedMachinesHaveNoKnownEnergyLevel`, `UNTIL "a game data source giving the energy values of these machines is available"`; the ruling « measured in game by the user » is that source and answers the `UNTIL` too, and rewrites `@DECISION.AnUnprintedEnergyValueIsNeitherInferredNorZero`; nothing links the three | corpus modelling |
| The collection script handed this report the wrong previous report and an outside-corpus diff equal to the corpus diff | `previous-reports.md`: « 1 of 2 reports », `2026-09-18-pr-100.md` selected, while the folder at head holds three files and `2026-09-21-pr-101-102-103.md` is the most recent — `REPORT_NAME` (`collect.py:546`) is `^(\d{4}-\d{2}-\d{2})-pr-(\d+)\.md$`; `git-diff-outside-corpus.patch` and `git-diff-corpus.patch` have the same md5 (`a33661455f6a…`) because `f":(exclude){corpus}"` (`collect.py:238–239`) yields `:(exclude):(glob)**/*.awawa`, a pathspec that matches nothing; `git diff 135ec18...2401f43 -- . ':(exclude)*.awawa'` in this worktree is empty | report |

### Remediations, by return

| # | Change | What it fixes | Recorded as |
|---|--------|---------------|-------------|
| 1 | Before the ruling questions, the refinement inventories every finding of the discovery hand-backs against the task's fields — one row per finding, the field or the question it goes to — so that a line of a hand-back cannot miss the questions asked one minute later | The deprecated ids found at 12:06:16Z and asked about at 12:34:14Z | `~/.claude/commands/awawa-refine-task.md` § 3 edit, tool-specific, no entity |
| 2 | An option that rests on a file names the file's provenance in its description — tracked path and commit, private-repository path, or « untracked, origin unknown » — and a figure about « the reference saves » is the output of one command over all of them, quoted with the command | The « Test merge » save behind a recommended ruling; « reach 735473 » | § 4 of the same command for the first half, tool-specific, no entity; none new for the second — REP-5 already asks for the perimeter; the figure itself is corrected in this PR by the owner before the merge (one line of `hypotheses.awawa`; a draft task for one line would outlive its fix) |
| 3 | When the `SPEC` a question blocks is a minority of the task, the ratification question offers the split first — the blocked `SPEC` to a draft task of the same wave, the rest ratified | The second session (36 answers, 3.59 M cache-read) opened by the owner to do what the third option of 12:15:06Z sketched | `~/.claude/commands/awawa-refine-task.md` § 4 edit, tool-specific, no entity |
| 4 | The one-line refusal rule (« no `&&`, `;` or `$(...)` around `git`, `awawa` or `sed`; one plain command per call ») is quoted above the base check of `awawa-refine-task.md` § 1.2, whose example the session chained at 12:35:58Z, as `2026-09-18-pr-100.md` and `2026-09-21-pr-101-102-103.md` asked for the wave and review commands; and `commands.md` gets the separator line of the previous report (`printf '%s\n' -----`, never `echo ===`) | 10 refusals, 1 separator loss | Command edits, tool-specific, no entity; the separator line is the `~/.ai` pull request remediation 4 of `2026-09-21-pr-101-102-103.md` proposed, still to open |
| 5 | `@TASK.FEAT66`'s refinement settles the three homes of the energy gap together: the question, `@LIMITATION.ElevenPlacedMachinesHaveNoKnownEnergyLevel`'s `UNTIL`, and the rewrite of `@DECISION.AnUnprintedEnergyValueIsNeitherInferredNorZero` that its `RATIONALE` already announces | Two entities that say the same gap two ways, and a decision a `draft` task contradicts | `@TASK.FEAT66` (draft, this PR) already carries the decision rewrite in its `RATIONALE`; cited, not recorded twice — the `LIMITATION` link is the one line to add at its refinement |
| 6 | `collect.py`: `REPORT_NAME` admits `-pr-<N>(-<N>)*.md`, and the outside-corpus diff uses `:(exclude,glob)**/*.awawa` (one magic group) | The wrong previous report and the false outside-corpus patch this run had to correct by hand | `~/.claude/tools/awawa-usage-report/collect.py` edit, tool-specific, no entity |
| 7 | Reading a corpus file with `grep` is replaced by the `awawa` command CORPUS-4 gives; for « the last number of the sequence », `awawa status TASK --json . \| jq` until the tool sorts (part B) | 3 `grep` reads | none new — CORPUS-3 and CORPUS-4 exist; cited |
| 8 | At the fiftieth answer, the interactive session asks once whether to hand over | 63 answers in `146a20c2` with no question; the owner did the hand-over | none new — `commands.md` § Sessions exists since 2026-09-17; cited, counted below |

### Entities proposed to the owner

| Entity | Type | What it records | Ruling expected |
|--------|------|-----------------|-----------------|
| `@PULL_REQUEST.PCST107` | `PULL_REQUEST` | Pull request #107 of `@PROJECT.PCST`, declared because `EVALUATES` of `@SCHEMA.USAGE_REPORT` names nothing else at this head; `@TASK.CHORE57` (`todo` at the base, #106) turns `EVALUATES` into a `uint` and removes the type, so this entity lives until that task lands | Accept as the entity the report cites, until `@TASK.CHORE57` |
| `@USAGE_REPORT.PR107` | `USAGE_REPORT` | This report, anchored on its first-line tag, `EVALUATES @PULL_REQUEST.PCST107` | Accept as the source entity |

No task, question, limitation or fact is opened: remediations 1 to 4 and 6 address command files and the collection script outside this repository, 5 is carried by `@TASK.FEAT66`, 7 and 8 by rules that exist.

## Part B — The tool

### What went well

| Promise | Kept | Evidence |
|---------|------|----------|
| `diff base head` renders a promotion and a rewrite at field level | yes | `corpus-diff.txt`: `@TASK.FEAT55` `STATUS draft -> todo`, `~ TITLE`, `~ RATIONALE`, `~ SPEC`, `~ SOURCE`, `- DESC`, `+ SPEC` ×2, `+ WAVE 5`, 7 `+ RATIONALE.REF`, 8 `+ SOURCE.REF`, 2 `+ SOURCE`; `@RULE.APlanetNumericIdIsStableAcrossSaves` `~ RATIONALE`, `+ SOURCE`, 2 `+ SOURCE.REF`; 23 added; 0.0 s (`index.md`) |
| `status --where FIELD==VALUE` selects on a `uint` field | yes | `awawa status TASK --where WAVE==5 .`: 5 tasks with their `STATUS` and incoming counts (`BEFORE 1`, `BLOCKED_BY 1`); `--where WAVE==4 .`: 10 |
| `context --skip <category>` leaves a task's package at its own lines and prints the incoming edges no traversal finds | yes | `awawa context @TASK.FEAT66 --skip reasoning --skip provenance .`: 1 entity, 6 lines, `// skipped (reasoning): … 1 field`, `// skipped (provenance): 7 URL, 2 fields`, `// BLOCKED_BY (root, 1): @OPEN_QUESTION.TheEnergyOfTheSkeoMachinesIsMeasuredInGame`, `// referenced by (root, 1): @TASK.FEAT55`; `@TASK.FEAT55`: 10 lines, 4 `SOURCE` fields skipped |
| `REQUIRED` on a relation field is enforced under the entity's gate | yes | Probe on a copy of the head tree with the `BLOCKS @TASK.FEAT66` line removed from the first question: `L006 required field BLOCKS is absent from OPEN_QUESTION` at `open_questions.awawa:1:1`, exit 1 — the « `BLOCKS @TASK` est requis » of `agents/corpus-ecrire.md` |
| `lint --strict` and `fmt --check` exit 0 at every commit | yes | 3 of 3 extracted trees (this report); `corpus-lint-strict.txt`, `corpus-fmt-check.txt` exit 0 at head |
| `refs` says what points at an entity, and `status` prints the incoming count by converse name | yes | `corpus-entities-of-diff.txt`: `@GAME_RELEASE.2.102` 7 incoming across 5 entities and 2 files; `@TASK.FEAT65` 3 (`AFTER` from `TEST67` among them); `awawa status OPEN_QUESTION .` on `trees/base`: 0, at head 2; 1 `refs` call in `f0befb44` |
| `new TYPE Name` prints a skeleton the schema accepts | yes | 2 calls in `146a20c2` (`transcripts-measure.txt`); the two entities of the types then written (`OPEN_QUESTION`, `HYPOTHESIS`) lint clean at `1835f91` |
| The corpus was read through the tool in the working sessions | mostly | 76 `awawa` calls in 99 answers (`146a20c2`: show 18, status 9, lint 8, fmt 6, context 3, new 2; `f0befb44`: status 14, lint 5, show 5, fmt 4, context 1, refs 1); 0 `Read` of a `.awawa` file; 3 `grep` reads (part A) |
| Every `awawa` command runs in 0.0 s on a 325-entity corpus | yes | `index.md`: `status`, `diff`, `lint --strict`, `fmt --check`, the 25 `show` and `refs` of `corpus-entities-of-diff.txt`, all 0.0 s |

### What went wrong

| Defect | Observation | Checked against |
|--------|-------------|-----------------|
| `lint --closure` has no `--skip`, so the closure of a task carries the provenance hub | `awawa lint --closure @TASK.FEAT55 .` at head: 46 entities (15 `URL`, 5 `PULL_REQUEST`, 4 `DECISION` …) and a footer of 185 names — `SOURCE_OF (rest, 65)`, `GOVERNED_BY (rest, 52)`, `PULL_REQUESTS (rest, 46)`, `LIMITED_BY (rest, 8)`, `referenced by (rest, 7)`, `CONSTRAINED_BY (rest, 5)`, `APPLIED_BY`, `BLOCKED_BY` — reached through `SOURCE → @PROJECT.DNC` and the `GAME_RELEASE` entities; `@TASK.TEST67` the same 46 and 185; at base the closure of `@TASK.FEAT55` was 4 entities; `awawa context … --skip provenance` lists 1 (raised in `2026-09-18-pr-100.md` and `2026-09-21-pr-101-102-103.md`) | run |
| `status` counts an entity at the `DEFAULT` value of `STATUS` as `(none)` | `corpus-status.txt`: `DECISION (none) 106`, `PULL_REQUEST (none) 51`, `OPEN_QUESTION (none) 2`, `URL (none) 15` while `SCHEMA *` declares `STATUS active\|archived DEFAULT active` (raised in the three previous reports) | run |
| `fmt` wraps strings at about 95 columns with no option | `git-diff-corpus.patch`: 54 of the 243 added lines are `+ "` continuations; the `LOCATION` of `@URL.AkarnokdEnglishPlusLabels` (135 characters) is the one string it does not cut; on this report's own entities, `awawa fmt .` left the 98-character `FILE` anchor of `@USAGE_REPORT.PR107` whole and wrapped its 108-character `DESC` on two lines (raised in the three previous reports) | run |
| `status TYPE` has no numeric or field order, so « the last number of the sequence » is read through a pipe | `awawa status --help`: no `--sort`, no `--order`; `awawa status TASK .` lists 34 rows by id in lexical order (`CHORE3, CHORE46, CHORE49 …`); `agents/corpus-ecrire.md` sends the reader there for the last number, and `f0befb44` at 12:36:35Z ran `awawa status TASK . \| grep -oE '@TASK\.[A-Z]+[0-9]+' \| grep -oE '[0-9]+$' \| sort -n \| tail -1`; `2026-09-21-pr-101-102-103.md` saw two branches number a task 56 the same morning | run |

Sent back to part A: the closure footers of 185 names come through our own `SOURCE.REF REQUIRED` and the `@PROJECT.DNC` hub — the declarations `@TASK.CHORE57` (`todo`) rewrites — and the two homes of the energy gap are our modelling; the binary did what the schema says.

### Remediations

| Proposal | What it would have changed on this PR |
|----------|---------------------------------------|
| `--skip CATEGORY` on `lint --closure`, as on `context` | The closure of `@TASK.FEAT55` lists its 5 tasks, 2 questions, 2 rules and 3 tables, and a footer of 1 name instead of 185 |
| `status` reports the `DEFAULT` value as the entity's `STATUS` | `corpus-status.txt` reads `DECISION active 106` |
| `--width N` or `--no-wrap` on `fmt`, or the width in `fmt --help` | 54 continuation lines read as 54 fewer diff lines |
| `--sort FIELD` or `--sort id:numeric` on `status TYPE`, or a `--max FIELD` | « the last task number » is one flag instead of a four-stage pipe, and the row of `agents/corpus-ecrire.md` names the flag |

## Recurring points

### Recurring pain points

| Point | Times seen | First raised in | Seen again here | Remediation proposed then | Done? |
|-------|------------|-----------------|-----------------|---------------------------|-------|
| Commands refused by the worktree-isolation check — **flagged, 4 reports** | 4 | `2026-09-17-pr-95.md`, part A | 10 refusals in 215 answers; the command's own base check chained at 12:35:58Z; 7 in the two interactive sessions entered with `EnterWorktree`, 3 in the discovery agents | The rule in `commands.md` (`abde632`), the launch prompt quoting the line (`pr-100`), un-isolated review launches (`pr-101-102-103`) | rule merged, not working; the prompt line absent from `awawa-refine-task.md`, `awawa-plan-wave-start.md` and `awawa-pr-review.md` (`grep`: 0 in each); remediation 4 above extends it to the refine command |
| `status` counts the `DEFAULT` `STATUS` as `(none)` — **flagged, 4 reports** | 4 | `2026-09-17-pr-95.md`, part B | `DECISION (none) 106` | `status` reports the `DEFAULT` value | no (tool) |
| `fmt` wraps long strings with no option — **flagged, 4 reports** | 4 | `2026-09-17-pr-95.md`, part B | 54 of 243 added lines | `--width N` or `--no-wrap` | no (tool) |
| `lint --closure` has no `--skip` — **flagged, 3 reports** | 3 | `2026-09-18-pr-100.md`, part B | 185 names on the footers of `@TASK.FEAT55` and `@TASK.TEST67` | `--skip CATEGORY` on `lint --closure` | no (tool); on our side `@TASK.CHORE57` `SPEC` 10 makes `--skip provenance` the documented `context` call |
| The modelling of a delivery overturned by review: one `PULL_REQUEST` per cited pull request — **flagged, 3 reports** | 3 | `2026-09-18-pr-100.md`, part A | This report declares `@PULL_REQUEST.PCST107` because `EVALUATES @PULL_REQUEST` admits nothing else at this head, the fourth report to do so | `@TASK.CHORE57` (from #101), the gates review (`pr-101-102-103` remediation 2, `CHORE61`) | partly — `CHORE61` folded into `@TASK.CHORE57`, ratified `todo` in #106 (`135ec18`, this PR's base): `EVALUATES` becomes a `uint`, `SOURCE.REF` optional; not delivered yet |
| More than fifty answers in one context without a hand-over question — **flagged, 3 reports** | 3 | `2026-09-17-pr-95.md`, part A (93 answers) | `146a20c2`: 63 answers, the 50th at 12:15:44Z, no question; `2026-09-21-pr-101-102-103.md` saw 62 in an agent round | The `commands.md` § Sessions rule (interactive) and PROC-4 (procedures) | rule merged 2026-09-17, not applied by the interactive session; the owner handed over by hand at 12:34:14Z |
| An answer lost to the zsh separator `echo =====` | 2 | `2026-09-21-pr-101-102-103.md`, part A | 1 at 11:57:29Z, the first command of the run (`awawa context … ; echo ======; awawa lint --closure …`) | The separator line in `commands.md` (remediation 4) | no (`grep 'echo ==='` in `commands.md`: 0) |

Raised before and not seen again, closed by observation: a schema change landing mid-wave (no schema change in this PR, `docs/_schema.awawa` untouched); the readiness comment read as an authorisation to merge and the report landing elsewhere (no review round, no readiness comment, the PR open at `2401f43` and this report on its branch); two of three pull requests red on `fallow` at delivery (no code; 8 checks pass); two branches numbering a task 56 (one branch, FEAT65–FEAT68 after CHORE64); PR #103's gate blocking its rebase on uncited releases (`@GAME_RELEASE.2.102` has 7 incoming, `2.103` was cited at base); a rebase dropping a hunk (no rebase); a round past fifty answers leaving its work uncommitted (both runs committed); the owner's answer lost by the CLI (10 questions in 3 calls, every one answered, 18 s to 5 min 15 s); the review's claim wider than its check (no review claim); three `DECISION` written then struck (0 written); five undecided points in the PR body (the 2 open points are `OPEN_QUESTION` entities, `corpus-open-questions-live.txt`); the owner's hand commits introducing gaps (0 commits without a trailer, 3 of 3 by the session); a launch prompt asserting a setup that did not exist (interactive; `bun install` ran at 11:58:25Z); the pre-commit hook blocked on its PATH (three commits went through); the nested `REF` declared 8 times, `diff` pairing by name, `fmt --help` printing the global options (not exercised).

### Recurring strengths

| Mechanism | Times seen | First noted in | Held again here |
|-----------|------------|----------------|-----------------|
| Every commit lints clean from the repository root — **established** | 4 | `2026-09-17-pr-95.md`, part A | 3 of 3 commits, `lint --strict` and `fmt --check` exit 0 |
| Questions to the owner batched in one call — **established** | 4 | `2026-09-17-pr-95.md`, part A | 10 questions in 3 calls, a recommendation first each time |
| Work parallelised in agents with a written brief — **established** | 4 | `2026-09-17-pr-95.md`, part A | 4 discovery agents launched in 27 s, on Sonnet and Haiku, briefs naming the source and the fetch that works |
| The PR body kept describing the branch head — **established** | 4 | `2026-09-17-pr-95.md`, part A | Edited 12:34:47Z, rewritten 12:45:11Z |
| Checks green at head — **established** | 4 | `2026-09-17-pr-95.md`, part A | 8 CI jobs; local `bun test` 836/0, `lint:types` 0, `audit:quality` 0 |
| The recording threshold applied before a `DECISION` is written — **established** | 3 | `2026-09-18-pr-100.md`, part A | 0 `DECISION` in two runs; the one conflict stated in `@TASK.FEAT66`'s `RATIONALE` |
| `refs` says what points at an entity — **established** | 4 | `2026-09-17-pr-95.md`, part B | `@GAME_RELEASE.2.102` 7 incoming, `@TASK.FEAT65` 3, the `BLOCKS` edges on FEAT66 and FEAT68 |
| `status --where FIELD==VALUE` finds an entity without a file read — **established** | 4 | `2026-09-17-pr-95.md`, part B | `WAVE==5` → 5 tasks, `WAVE==4` → 10 |
| `diff base head` gives an entity-level review — **established** | 4 | `2026-09-17-pr-95.md`, part B | 25 entities, the promotion and three rewrites at field level |
| `WHEN <field> <value>` and `REQUIRED` gate a field — **established** | 4 | `2026-09-17-pr-95.md`, part B | L006 on `BLOCKS` of `OPEN_QUESTION` (probe) |
| `new TYPE Name` skeletons — **established** | 3 | `2026-09-17-pr-95.md`, part B | 2 calls, the two entities lint clean |
| The per-worktree setup run before the first check | 2 | `2026-09-18-pr-100.md`, part A (« the launch prompt states the setup as a check to run ») | `bun install --frozen-lockfile` 7 s after `EnterWorktree`, before `bun validate` |
| Discovery delegated to cheaper models with pinned sources recorded as `URL` entities | 1 | this report | 4 agents, 15 `URL`, 6.33 M cache-read on Sonnet and Haiku |
| A progress file carrying a procedure across two runs | 1 | this report | `awawa-refine-task-FEAT55.md`, 14 steps, no ruling asked twice |
| The owner's free-text answer handled as a question and re-asked with its provenance | 1 | this report | 12:40:28Z → 12:41:12Z |

Noted before and not seen holding here: a review round addressing its threads in minutes, incoming references checked before a deletion, `lint --strict` validating a new anchor, `--overlay` testing a gate, the 50-step hand-over of PROC-4, a wave ruling relayed to a peer, the pilot sending an unbacked claim back, the hosted `claude[bot]` review finding gaps (its job passed and posted nothing on a corpus-only diff) — not exercised by this PR.

## Follow-up on the previous report's remediations

| Remediation | Done? | Evidence |
|-------------|-------|----------|
| 1 — `agents/worktree.md` names the acceptance commands, `bun run audit:quality` among them (`@TASK.CHORE60`) | no | `awawa status TASK .`: `@TASK.CHORE60 draft`, `tasks.awawa:789`; `agents/worktree.md` unchanged (3 items, no acceptance list) |
| 2 — The provenance gates reviewed (`@TASK.CHORE61`, `CHORE57` taking the `PULL_REQUEST` type) | partly | `CHORE61` folded into `@TASK.CHORE57`, ratified `todo` in #106 (`135ec18`, 11 `SPEC`: no `PULL_REQUEST` type, `PR uint` under `SOURCE`, `SOURCE.REF` optional, `EVALUATES` a `uint`, no `INCOMING SOURCE_OF` on `URL` and `GAME_RELEASE`); not delivered |
| 3 — A review round reads the highest task number across the open branches before `awawa new TASK` | not exercised | No review round; the refinement read it on its own branch (step 11: 64) |
| 4 — The separator line in `commands.md` | no | `grep -c 'echo ===' ~/.ai/instructions/commands.md`: 0; 1 loss here |
| 5 — Un-isolated review launches and the refusal line in the launch prompts | no | `grep` of the three command files for the line: 0; 10 refusals here |
| 6 — `git range-diff` read after a rebase that met a conflict | not exercised | No rebase |
| 7 — The readiness comment names what remains | not exercised | No readiness comment |
| `@PULL_REQUEST.PCST101`, `PCST103`, `@USAGE_REPORT.PR101_102_103` accepted | yes | All three at the base and at head (`awawa status PULL_REQUEST .`, `USAGE_REPORT .`: `PR101_102_103` incoming 5) |
| `@TASK.DOCS50` (from `pr-95`, restated since) | no | `draft`, `tasks.awawa:510` |

## Appendix

### Measurements

Active time counts gaps under ten minutes only. Figures from `measure.py` (`transcripts-measure.txt` for the two sessions; the four agents measured by this report with the same script). Tool calls exclude `SubagentHandback`.

| Session | Active time | User turns | Tool calls | awawa calls by subcommand | Tokens read from cache | Tokens written |
|---------|-------------|------------|------------|---------------------------|------------------------|----------------|
| `146a20c2` (`/awawa-refine-task 55`, run 1), 63 answers | 31 min 57 s | 11 | 71 | show 18, status 9, lint 8, fmt 6, context 3, new 2 | 6,939,244 | 45,790 |
| `agent-aaded4e0` wiki discovery (Sonnet), 19 answers | 2 min 10 s | 2 | 17 | — | 998,735 | 9,088 |
| `agent-a2f08876` PCEdit and akarnokd discovery (Sonnet), 56 answers | 6 min 44 s | 2 | 54 | — | 3,717,098 | 25,344 |
| `agent-ae3b54bc` Steam news discovery (Sonnet), 15 answers | 2 min 03 s | 2 | 20 | — | 776,114 | 7,001 |
| `agent-a79d74f2` repository impact (Haiku, `Explore`), 26 answers | 1 min 38 s | 2 | 41 | — | 841,909 | 8,008 |
| `f0befb44` (`/awawa-refine-task 55`, run 2: the split), 36 answers | 9 min 58 s | 4 | 50 | status 14, lint 5, show 5, fmt 4, context 1, refs 1 | 3,586,165 | 30,258 |
| **Total (6 rows)** | 54 min 30 s | 23 | 253 | 76 (show 23, status 23, lint 13, fmt 10, context 4, new 2, refs 1) | 16,859,265 | 125,489 |

Journey: `/awawa-refine-task 55` at 11:57:14Z; agents launched 11:59:21–11:59:48Z, back 12:01:22–12:06:16Z; rulings asked 12:07:46Z, answered 12:13:01Z; `1835f91` 12:15:47Z (18 min 33 s after the command); PR #107 opened 12:17:26Z on the owner's « push + PR »; `cd68eb3` 12:34:35Z; run 2 from 12:35:39Z, split asked 12:37:49Z, ratifications 12:43:41Z, `2401f43` 12:44:41Z, body rewritten 12:45:11Z; last text 12:45:34Z — 48 min 20 s end to end.

| Corpus lines changed | PR lines changed | Share |
|----------------------|------------------|-------|
| 249 (+238 −11, 5 files) | 249 | 100 % |

`git-diff-shortstat.txt` on `135ec18...2401f43`; the diff outside `.awawa` files is empty.

### Claims checked

| Claim | Check | Verdict |
|-------|-------|---------|
| Body: `awawa fmt . && awawa lint --strict .` exits 0 | `corpus-lint-strict.txt`, `corpus-fmt-check.txt` exit 0 at head; 0 at `1835f91` and `cd68eb3` too | confirmed |
| Body: FEAT55 `SPEC` 6 → 3, `SOURCE` 6 → 4, `WAVE 5`, `todo` | `awawa show @TASK.FEAT55 .` on the tree of `cd68eb3`: 6 `SPEC`, 6 `SOURCE`; at head 3 and 4, `WAVE 5`, `STATUS todo`; the 6 → figures count from `cd68eb3`, not from the base (1 `SPEC`, 2 `SOURCE`) | confirmed, perimeter the second commit |
| Body: 15 `URL` — Steam API, 7 wiki pages by `oldid`, 5 PCEdit files and 2 akarnokd addresses by commit | `corpus-diff.txt`: 15 `URL` added; `corpus-entities-of-diff.txt`: 7 `LOCATION` with `oldid=`, 5 `Valkerran/PCEdit/blob/ca7760bd…`, 2 `akarnokd/…` (`blob/3f10f457…`, `commit/16fa2a63…`), 1 `api.steampowered.com` | confirmed |
| Body: `@GAME_RELEASE.2.102` new; the format changed with 2.102, 2.103 changes no save key | `awawa status URL .` and `GAME_RELEASE` at base: `2.102` absent, 7 incoming at head; « no save key » rests on the PCEdit save-diff (2.008 → 2.102) and the session's own key diff of the 2.103 sample (step 2, « only NEW field = section0 logisticsPaused »): this report's `grep` of the sample finds `logisticsPaused` as its only key outside the schema | confirmed for the sample, the general claim unchecked beyond it |
| Body: `cli-validate` on a 2.103 save prints `[Global metadata (section 0), entry 0] must NOT have additional properties` | `bun validate -- --file=<private sample>` in this worktree after `bun run private:sync`: that line, exit 1 | confirmed |
| Body: `mergeGlobalMetadata.ts` would drop `logisticsPaused` | `packages/core-mapping/src/domain/rules/merge/mergeGlobalMetadata.ts`: the returned object names the five known fields only | confirmed |
| Body: PCEdit's `PlanetHash.cs` reproduces all six known ids, recomputed independently | `probe.py` on `PlanetHash.cs` at `ca7760bd`: 6 of 6, `Skeo → -440810600`; `PlanetHashTests.cs` lines 15–20 carry the same six | confirmed |
| Body: every group id the update adds already has a row in `worldObjectLabels.json` | `gen_catalog.py` at `ca7760bd` lines 860–862: 10 ids under `added("2.102", …)`; each found once in `packages/core-mapping/src/presentation/worldObjectLabels.json` (`EscapePodInterplanetary`, which the session first reported missing at 12:06:58Z and retracted at 12:14:19Z, has a row too and is not in the 2.102 list) | confirmed |
| Body: the wiki gives T2 Incubator −715 kW/s and Intense Area Lamp −25 kW/s, both already in the table | `energyLevels.json:57` `Incubator2` 715 consumption, `:69` `InsideLamp2` 25 | confirmed |
| Body: `EnergyLevelsPresenter` falls back to the unnamed-planet label without FEAT65 | `EnergyLevelsPresenter.ts:44`: `planet.planetName ?? resolveEnergyLevelsSectionUnnamedPlanetName(planet.planetId)`; `planetNamesByNumericId.json` has 5 rows, no `-440810600` | confirmed |
| Body: `@DECISION.AnUnprintedEnergyValueIsNeitherInferredNorZero` keeps a machine the wiki is silent on out of the tables | `awawa show`: `SPEC` « A machine whose wiki page prints no energy value stays out of the energy tables », `REJECTED "Setting zero for a value the wiki does not print"` | confirmed |
| Body: `SPEC` 6 reworded because fixtures are generated (`@DECISION.ScenarioFixturesAreGeneratedAndReplayedByAGuard`) | `awawa show`: `SPEC` « A script declares every scenario fixture … `bun run generate:scenario-fixtures` writes them »; `@TASK.TEST67` `SPEC` 1 « a scenario fixture generated with … » | confirmed |
| Body: a local save written after 2.102 carries `Phytoplankton2` ×144 and `Phytoplankton3` ×86, provenance unknown | Transcript `f0befb44` 12:40:34–12:40:54Z: `input/Skeo/skeo.json`, 2.8 MB, `saveDisplayName` "Test merge", untracked; the counts are the session's `grep`, not re-run (the file is outside the private repository and this report did not open it) | unchecked (counts), confirmed (provenance unknown) |
| Body: the reference saves reach 735473 (`@HYPOTHESIS.TerraTokensStayWithinTheExactIntegerRangeOfADouble` `RATIONALE`) | `grep` over the 7 private saves: `allTimeTerraTokens` up to 3,024,414 (`Standard-2-merged.json`), `terraTokens` up to 2,339,989; 735473 is `Standard-4.json`'s | wrong (the hypothesis itself holds) |
| Body: « Each open question now blocks the task it actually holds up » | `awawa refs`: `BLOCKS` from `@OPEN_QUESTION.TheEnergyOfTheSkeoMachinesIsMeasuredInGame` → `@TASK.FEAT66`, from `…DeprecatedGroupIdsOfTheSkeoUpdateAreHandled` → `@TASK.FEAT68`; at `cd68eb3` both → FEAT55 | confirmed |
| Body: FEAT55, FEAT65 and FEAT66 share no file; TEST67 needs both FEAT55 and FEAT65 | Section 0 schema and `GlobalMetadata.ts`/`mergeGlobalMetadata.ts` (FEAT55), `planetNamesByNumericId.json` (FEAT65), `energyLevels.json` (FEAT66) are four distinct paths; TEST67's fixture fails validation on the closed schema (checked above) and the presenter falls back without the planet row (checked above) | confirmed |
| Commit `cd68eb3`: « how a save carrying them is handled is an open question blocking FEAT55 » | True at that commit (`awawa status OPEN_QUESTION .` on its tree: 2 questions; `refs` on FEAT55), moved to FEAT68 by `2401f43` | confirmed at its date |
| Final text of run 2: « La PR est `MERGEABLE` » | The command that read it (12:45:19Z) was refused; `gh pr checks 107` today: 8 pass; `pr.json` `state OPEN` | confirmed by this report, not by the session |
| `2026-09-21-pr-101-102-103.md` remediation 5: the launch prompts quote the refusal line | `grep` of `awawa-refine-task.md`, `awawa-plan-wave-start.md`, `awawa-pr-review.md`: 0 | wrong (not done) |
| `2026-09-21-pr-101-102-103.md`: `@TASK.CHORE61` opened | Absent from `awawa status TASK .` at head; `awawa-refine-task-CHORE57.md` step 5: « CHORE61 deleted », folded into `CHORE57` (#106) | confirmed as superseded |
