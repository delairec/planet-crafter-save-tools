<!-- awawa-usage-report -->

# 2026-09-17 — PR #95 docs(awawa): migrate project methodology corpus to the target schema

| Field                                          | Value |
|------------------------------------------------|-------|
| **Author of this report**                      | `Claude Fable 5.1` (`claude-fable-5-1`), effort `high` (`~/.claude/agents/awawa-usage-reporter.md`, `model: fable`, `effort: high`) |
| **Models that drove the sessions under review** | `Claude Opus 5 (1M context)`, effort `medium` — the trailer of 16 of the 23 commits (`git-log.txt`); `/model` and `/effort medium` set and saved as defaults at 2026-09-16T22:30:50Z–22:30:57Z in `session_59926afe`, no later change found in the 9 other piloting transcripts (`grep 'Set effort level'`)<br>7 commits carry no `Co-Authored-By` (`1dbd31b`, `8f154aa`, `73a269c`, `e5ac6fe`, `7ea89fd`, `fa46414`, `1a7725d`): the owner's own commits, model `unchecked`<br>Agents launched by `/awawa-pr-review` (`general-purpose`): model inherited from the launching session, effort `unchecked` |
| **Sessions measured**                          | Piloting: `session_895d269e` (migration, 16 Sept), `session_fc288c99` and `session_59926afe` (pre-commit hook, 16 Sept), `session_971e6ece` (empty), `session_4f59429d` (review round 1 launcher), `session_46d2a0ec`, `session_a1a83a75`, `session_d7cf38f8` (feedback page and assets), `session_95cb7b16` (review round 2 launcher, cleanups, tooling), `session_500f1282` (template, `~/.ai` rules)<br>Agents: 12 of `session_895d269e` (`agent-a0377536`, `agent-a2fe8ef3`, `agent-a33cf168`, `agent-a909a6ea`, `agent-ab2b169a` classify; `agent-a8c87e70`, `agent-a8ea9816`, `agent-aba69343`, `agent-ac17ea64`, `agent-adae64d9`, `agent-aebbe515`, `agent-aed14f41` write), `agent-abc7dd4f` (review round 1), `agent-a37effca` (review round 2), `agent-a4693c2a` (work file cleanup), `agent-aa557eda` (findings cleanup), `agent-a3d8faad` and `agent-a93bc6ac` (report tooling)<br>Cut: `session_44495e5e` and `session_35dbe4a9` launched a reporter and are read up to that turn (`transcripts.md`); commit `05ca237` comes from the cut part of `session_44495e5e` and is unmeasured |
| **Delivered**                                  | The corpus moved from 12 French files under `docs/*.awawa` (235 entities, 6 schemas) to 8 English files under `docs/awawa-project-methodology/` (216 entities, 12 schemas, 403 reference sites, 0 unresolved); `AGENTS.md` rewritten; the feedback-page assets and the migration reports filed; 43 files, +5,547/−5,554 lines, 23 commits |

## Verdict in two lines

**Ours**: the migration landed in three commits that each lint clean (`awawa lint --strict .` exit 0 at `8389f29`, `a8be69c`, `61c80d7` and at head), 235 entities became 216 with 0 unresolved references, and two review rounds answered 17 threads in 8 and 6 minutes; but the session that did it ran 93 answers in one context for 23.9 M cached tokens, met 26 command refusals, wrote 3 review decisions the owner struck the same day, and left five undecided project points in the pull request body while the corpus holds 0 `OPEN_QUESTION` — three of them decision texts the code at head contradicts.

**The tool's**: awawa 2.7.0 carried the migration — L005 forced the commit sequencing, L016 validated every anchor, `refs` guarded the 3 deletions, `diff` rendered a whole-corpus rename in 0.0 s — and cost it three workarounds: a nested `REF` declared 8 times because a redeclared field drops the wildcard's children (L003, L023), a formatter that wraps at 100 columns with no option (one review thread), and a diff that pairs by name, so the rename reads as 221 removals and 202 additions.

## The owner's word

> This is the first report after the second migration of the corpus, carried out far more seriously than the previous one, with an iterative review of the schemas and of the types created. The next reports will therefore be more interesting, as we will be able to discover what this migration to a project managed with awawa specs really brings.

## Part A — Us: method, corpus, conduct

### What went well

| What | Evidence |
|------|----------|
| Every migration commit lints clean from the repository root | `awawa lint --strict .` exit 0 on the extracted trees of `8389f29`, `a8be69c`, `61c80d7` (this report's run) and at head (`corpus-lint-strict.txt`, `corpus-fmt-check.txt`, both exit 0); 403 reference sites, 0 unresolved (`corpus-status.txt`) |
| The sequencing question was asked once, before the first write, with the tool's rule as its ground | `timeline-02` 22:56:26: one `AskUserQuestion` carrying 2 questions (L005 forbids two `SCHEMA *` in one walk — remove the old corpus first, or one final commit, or files out of the walk; how far to go); answered 23:04:18; first commit 23:06:05 |
| Ambiguities were batched, as the owner asked, not asked one by one | The owner's answer at 23:04:18: « on pose toutes les questions en une seule fois »; the session asked nothing more in 93 answers (`transcripts-measure.txt`: 1 `AskUserQuestion` call) and handed 7 open points in one message, then in the PR body |
| The migration was parallelised in two waves of agents with a written brief | 5 classify agents launched 23:08:11–23:08:28 (2–4 min each), 7 write agents 23:14:37–23:14:53 (3–7 min each), every prompt pointing at `brief-classify.md` or `brief-write.md` in the scratchpad, « no git, no repository writes » (`timeline-02` lines 16–27) |
| Review round 1 addressed 10 threads in 8 minutes and recorded the one that was work as a draft task | `agent-abc7dd4f`: 09:00:23–09:08:35, 4 commits, `@TASK.DOCS48` `draft` for the `AGENTS.md` split (thread 4031809862), two « note pour moi-même » threads left to the owner, readiness comment 5711849895 posted 09:08:18Z |
| Review round 2 checked incoming references before each deletion | `agent-a37effca`: 7 `awawa refs` calls for 3 decisions deleted in `fe2003a` (`transcripts-measure.txt`), 6 threads fixed and 1 answered with a probe in 5 min 31 s |
| The two deleted design files were classified section by section, with where each fact now lives, before deletion | `agent-a4693c2a`: 23 sections of `work_in_progress.md` in a table (a/b/c classes) then `a5d97e5`; `agent-aa557eda`: 11 sections of `findings.md` then `d93c51a`, one sentence held for the owner (§7.4) |
| The one sentence found nowhere else became a general rule the same afternoon | `agent-aa557eda` held §7.4 at 13:43; `agent-a20223c8` opened delairec/.ai#33 (REP-6) at 13:50; merged 2026-09-17T13:56:33Z (`gh pr view 33 --repo delairec/.ai`) |
| The four general findings of this journey were written into `~/.ai` before the merge | delairec/.ai#34, 4 commits (`1faf15b` review round on the archived schema, `18f94f3` recording threshold, `55c52b9` 50-answer handover, `abde632` no computed argument in a worktree-isolated session), merged 2026-09-17T16:09:43Z (`timeline-06` 16:07:13; `gh pr view 34`) |
| The hosted `claude[bot]` review found three real gaps and all three were fixed | Threads 4036818954 (decision missing 3 files its README lists), 4036819602 (dead path in a `REJECTED`), 4036820607 (orphan starter draft): fixed in `b1ae3e5`, `b1ae3e5`, `ca2d604`; all three resolved |
| The PR body was kept describing the branch head through 23 commits | Rows added for every commit through `c2068cd`; counts corrected from 96 to 93 `DECISION` after `fe2003a` (`agent-a37effca` handback; `pr-rendered.md` commit table) |
| Checks at head are green | `bun test` 814 pass / 0 fail (`checks-bun-test.txt`), `lint:types` exit 0 on 7 packages, `audit:quality` exit 0, 12 GitHub checks pass (`gh pr checks 95`), tree clean before and after |

### What went wrong

| What | Evidence | Whose fault |
|------|----------|-------------|
| The migration session ran 93 answers in one context | `session_895d269e`: 93 answers, 23,852,435 cache-read tokens, 85,835 output tokens, 4 user turns (`transcripts-measure.txt`); its 12 agents add 238 answers and 19.5 M cache-read; the session alone is 26 % of the 92.2 M cache-read tokens of the whole journey | conduct |
| 26 commands refused by the worktree-isolation check, 6 in the session and 20 across 10 of the 12 agents | `grep -c 'Refusing to run it'` over the timelines: 9 « too complex to verify », 7 « names git in a form too complex », 7 `sed` with a computed value, 2 `awawa` with a loop variable, 1 `python3 $S`; each refusal is one answer spent rewriting | conduct |
| Round 1 wrote three `DECISION` entities for schema rulings the owner then struck: « inutile de consigner ça » three times | `f2d98b6` and `5cb6797` added `AFactIsAHypothesisUntilItIsAttested`, `AUsageReportIsNamedByItsSubjectAlone`, `AUsageReportKeepsTheTemplateTagOnItsFirstLine` (`git-log-p-corpus.patch` lines 161, 171, 211); threads 4037127266, 4037128275, 4037131732; removed in `fe2003a`. `AGENTS.md` already said a `DECISION` is written only if a future PR could do the opposite by mistake | method |
| Five project points the migration left undecided live in the PR body, none in the corpus | Body « Open points » 2, 3, 5, 6 (`pr-rendered.md`); `corpus-open-questions-live.txt`: 0 `OPEN_QUESTION`; the only entity citing `@PULL_REQUEST.PCST95` is `@TASK.DOCS48` (`awawa refs`) | corpus modelling |
| A live decision's `SPEC` is false at the base commit and lint cannot see it | `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped` SPEC 3: « devDependencies at an exact version, the same version in packageManager »; `package.json` line 5 `bun@1.3.14`, line 57 `"bun": "1.4.2"` at head, inherited from `bf98583` (#91, the Dependabot bump); both anchors resolve, so L016 is silent | corpus modelling |
| Two carried decisions state what the code does not | `@DECISION.BothMessageListsShareOneRendering` SPEC 2 « never by a parameter of the component » vs `ValidationMessagesList.tsx` line 11 `severity: 'danger' \| 'warning'`; `@DECISION.MockIsolationIsAGlobalPreload` SPEC 2 « in every package » vs 6 of 7 packages with a `bunfig.toml` (`ui-save-manager` has none and runs no `bun test`, its scripts being `test:ui` only) | corpus modelling |
| One carried decision reads as broken by fixtures that its own README explains | `@DECISION.NoSaveIsCommittedToThePublicRepository` SPEC 1 « No save is committed » vs 4 tracked files under `packages/ui-save-manager/e2e/fixtures/` whose README says « generated, never copied from a real game save » | corpus modelling |
| `PROCESS` lost its edges to packages and tasks | `awawa show @SCHEMA.PROCESS`: no `APPLIES_TO_PACKAGE`, no `APPLIES_TO_TASK`; `@PROCESS.ATestTaskChangesTheUiOnlyForItsOwnSake` no longer points at `@PACKAGE.ui_save_manager` nor `@TASK.TEST40` (body point 6) | schema |
| The owner's three hand commits of the feedback-page assets introduced the three gaps the hosted review caught | `7ea89fd`, `fa46414`, `1a7725d` (12:01–12:50Z, no trailer): the orphan `docs/awawa-project-methodology/awawa-specification-starter.md`, a decision naming 4 of 7 files, a dead folder path; 3 threads, 3 commits of round 2 | conduct |
| The round 1 launch prompt asserted a worktree setup that did not exist | Prompt: « Its dependency install, symlinks and private context clone already exist »; `agent-abc7dd4f` handback: « no `node_modules`, no `input` link and no `.do-not-commit` clone »; round 2 ran `bun install` itself | conduct |
| The pre-commit hook blocked the first commit of the evening on a PATH it did not inherit | `session_fc288c99` 22:27–22:29 and `session_59926afe` 22:31–22:34: « awawa is not on the PATH » while `which awawa` gives `~/.local/bin/awawa`; fixed by delairec/.ai#32 before the migration started at 22:54 | method |
| The migration's `Co-Authored-By` model line is absent from 7 of 23 commits, so the header above is read from 16 | `git-log.txt`: 4 design commits and 3 asset commits without trailer | report |

### Remediations, by return

| # | Change | What it fixes | Recorded as |
|---|--------|---------------|-------------|
| 1 | One draft task carries the five points the migration left in the PR body, one acceptance criterion each; the owner rules each by ratifying, rewriting the decision in place or changing the code | The five undecided points with no home in the corpus; the three decision-versus-code drifts; the `PROCESS` edges; the `AGENTS.md` language | `@TASK.DOCS50` (opened by this report) |
| 2 | Realign `packageManager` with `devDependencies.bun` in `package.json` | The false SPEC of `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped` since #91 | `@TASK.CHORE49` (opened by this report) |
| 3 | At the fiftieth answer, an interactive session asks once whether to hand over | The 93-answer migration session | `~/.ai` pull request delairec/.ai#34, commit `55c52b9`, merged 2026-09-17T16:09Z |
| 4 | No value computed at runtime as an argument of `git`, `awawa`, `sed` or an interpreter in a worktree-isolated session | The 26 refusals | `~/.ai` pull request delairec/.ai#34, commit `abde632`, merged |
| 5 | The recording threshold is applied before a review round writes a `DECISION` | The 3 decisions written then struck | `~/.ai` pull request delairec/.ai#34, commit `18f94f3`, merged |
| 6 | A dated document is never rewritten and quotes rather than links | The §7.4 sentence held by the findings cleanup | `~/.ai` pull request delairec/.ai#33 (REP-6), merged 2026-09-17T13:56Z |
| 7 | The review-round launch prompt states the per-worktree setup as a check to run, not as a fact | The false « already exist » of round 1 | none — a `~/.claude/commands/awawa-pr-review.md` edit, tool-specific, outside both the corpus and `~/.ai` |

### Entities proposed to the owner

| Entity | Type | What it records | Ruling expected |
|--------|------|-----------------|-----------------|
| `@TASK.DOCS50` | `TASK`, `draft` | The five points the migration left in the PR body: `severity` on `ValidationMessagesList.tsx` vs `@DECISION.BothMessageListsShareOneRendering`; the generated e2e fixtures vs `@DECISION.NoSaveIsCommittedToThePublicRepository`; `ui-save-manager` without a preload vs `@DECISION.MockIsolationIsAGlobalPreload`; `PROCESS` without `APPLIES_TO_*`; the language of `AGENTS.md` | Ratify to `todo`, or rule the points here and archive it |
| `@TASK.CHORE49` | `TASK`, `draft` | `package.json` carries one Bun version in `packageManager` and `devDependencies.bun` | Ratify to `todo` |
| `@USAGE_REPORT.PR95` | `USAGE_REPORT` | This report, anchored on its first-line tag, evaluating `@PULL_REQUEST.PCST95`; the `SOURCE` of the two tasks | Accept as the source entity |

## Part B — The tool

### What went well

| Promise | Kept | Evidence |
|---------|------|----------|
| One `SCHEMA *` per walk (L005) makes the sequencing of a migration a decision, not an accident | yes | The session's first question at 22:56:26 was built on it; the old corpus was removed in the same commit as the new schema (`8389f29`), and the tree at that commit lints clean with 22 schema entities and 0 data entities |
| `lint --strict` from the repository root validates every anchor | yes | 0 errors at 4 commits; round 2 added 5 `IMPL` anchors to `@DECISION.ArtifactAssetsFollowThePublishedPage` and lint proved them (`b1ae3e5`) |
| `refs` says what points at an entity before it is deleted | yes | 7 `refs` calls in `agent-a37effca` before deleting 3 decisions; « no incoming reference » in each reply (threads 4037208851, 4037209113, 4037209372) |
| `status --where FIELD==VALUE` finds an entity without a file read | yes | `corpus-project-of-repository.json`, `corpus-pull-request.json`, `corpus-tasks-of-pr.json`: exit 0 in 0.0 s each, resolving `@PROJECT.PCST` → `@PULL_REQUEST.PCST95` → 0 tasks |
| `diff base head` gives an entity-level review of a whole-corpus change | yes | `corpus-diff.txt`: 202 added, 221 removed, 14 changed with field-level lines, 0.0 s over 235 + 216 entities |
| `new TYPE Name` prints a schema-correct skeleton | yes | 18 `awawa new` calls across the journey (6 in the migration session, 6 in `session_46d2a0ec`, 3 in `session_500f1282`, 2 in `agent-aebbe515`, 1 in `session_a1a83a75`), none followed by a lint error on the skeleton |
| `WHEN <field> <value>` gates a required field on another field's value | yes | `SCHEMA FACT`: `BASIS` defaults to `hypothesis`, `WHEN BASIS hypothesis` requires `UNTIL`, `WHEN BASIS attested` requires `ATTESTED_BY`; probed by round 1 (thread 4035016498) and read again here |
| The pre-commit hook lints the whole walk at every commit | yes | 23 commits, every corpus commit clean; the hook's one failure was its PATH, fixed in delairec/.ai#32 |

### What went wrong

| Defect | Observation | Checked against |
|--------|-------------|-----------------|
| A nested field cannot be declared once for every prose field: `INCLUDE` under a `FIELD` is L023, and a type that redeclares a wildcard field loses the wildcard's nested fields (L003) | Probe on 2.7.0 in this report's scratchpad: a `FACT` redeclaring `DESC` gets `L003 field REF is not declared on FACT.DESC`; `INCLUDE @FIELDSET.Refs` under a `FIELD` gets `L023 INCLUDE is not a FIELD declaration`. `_schema.awawa` declares `FIELD REF reference` 8 times (lines 6, 12, 20 under `SCHEMA *`; 84 `FIELDSET Ruling › REJECTED`; 138 `LIMITATION › WORKAROUND`; 158 `FACT › DESC`; 183 `OPEN_QUESTION › DESC`; 243 `PACKAGE › DESC`) | run |
| `fmt` wraps any string past about 100 columns into `+` continuation lines and offers no option | Probe: a 168-character `DESC` comes back on two lines after `awawa fmt .`; `fmt` takes `--check` and `--rename` only. The owner asked why (thread 4037105533, « pourquoi on reste pas dans une seule string? »), one round-2 probe and reply answered it | run, transcript |
| `diff` pairs entities by name only, so a corpus renamed to English reads as removed plus added | `corpus-diff.txt`: 221 removed, 202 added, 14 changed — the 14 are the entities whose name survived (`@PACKAGE.*` among them); no way to tell the reviewer what `@DECISION.LaSpecificationResteEnFrancaisMemePubliee` became | run |
| `awawa fmt --help` prints the global option list, not the command's | Probe: the output lists `--where`, `--strict`, `--closure`, `--depth`, `--with-schema`, which `fmt` does not take | run |
| `status` counts an entity at the `DEFAULT` value of `STATUS` as `(none)`, not as that value | `corpus-status.txt`: `DECISION active 0, (none) 93` while `SCHEMA *` declares `STATUS active\|archived DEFAULT active`; every `--where STATUS!=archived` still works | run |

### Remediations

| Proposal | What it would have changed on this PR |
|----------|---------------------------------------|
| Let a nested field be inherited when a type redeclares a wildcard field, or accept `INCLUDE` under a `FIELD` | 5 of the 8 `FIELD REF reference` declarations of `_schema.awawa` disappear; review thread 4031962403 (« la même forme déclarée plusieurs fois, limite de l'outil ? ») has nothing to ask |
| A `--width N` or `--no-wrap` on `fmt`, or a documented width in `fmt --help` | Thread 4037105533 and its probe do not happen; a string written on one line stays there |
| A rename map for `diff` (`--renamed @T.Old=@T.New`, or reading the `fmt --rename` journal) | The reviewer sees 172 live entities paired old-to-new instead of 221 removals and 202 additions |
| Per-command `--help` | A session reading `fmt --help` learns its 2 flags without a probe |
| `status` reports the `DEFAULT` value as the entity's `STATUS` | The status table reads `DECISION active 93`, and the `(none)` column means what it says |

## Recurring points

### Recurring pain points

| Point | Times seen | First raised in | Seen again here | Remediation proposed then | Done? |
|-------|------------|-----------------|-----------------|---------------------------|-------|
| none | — | — | — | — | — |

No previous report under `docs/awawa-usage-reports/pull-requests/` at `c2068cd` (`previous-reports.md`): every point of this report counts 1. Raised before and not seen again, closed by observation: none.

### Recurring strengths

| Mechanism | Times seen | First noted in | Held again here |
|-----------|------------|----------------|-----------------|
| none | — | — | — |

Noted before and not seen holding here: none — no previous report.

## Follow-up on the previous report's remediations

| Remediation | Done? | Evidence |
|-------------|-------|----------|
| none — no previous report | — | `previous-reports.md`: « No previous report under `docs/awawa-usage-reports/pull-requests/` at c2068cd » |

## Appendix

### Measurements

Active time counts gaps under ten minutes only; figures from `measure.py` over each transcript (`transcripts-measure.txt`). Agents run inside their session's wall time, so active times add across rows but overlap in the clock.

| Session | Active time | User turns | Tool calls | awawa calls by subcommand | Tokens read from cache | Tokens written |
|---------|-------------|------------|------------|---------------------------|------------------------|----------------|
| piloting `session_fc288c99` (hook question, 16 Sept 22:26) | 3 min 10 s | 7 | 0 | — | 139,406 | 2,559 |
| piloting `session_59926afe` (hook fix, delairec/.ai#32) | 3 min 26 s | 10 | 14 | — | 781,156 | 7,529 |
| piloting `session_895d269e` (migration, 3 commits, PR opened) | 40 min 43 s | 4 | 131 | lint 16, show 15, fmt 12, status 9, new 6, diff 2, context 1 | 23,852,435 | 85,835 |
| agent `agent-a0377536` (classify packages, tasks) | 3 min 10 s | 2 | 18 | — | 1,493,226 | 21,415 |
| agent `agent-a2fe8ef3` (classify tests, errors, documentation) | 3 min 53 s | 2 | 18 | — | 1,565,215 | 26,255 |
| agent `agent-a33cf168` (classify merge, format, tooling, limitations) | 3 min 34 s | 2 | 26 | diff 2 | 1,963,740 | 23,950 |
| agent `agent-a909a6ea` (classify open questions) | 2 min 14 s | 2 | 16 | — | 1,095,713 | 14,250 |
| agent `agent-ab2b169a` (classify processus) | 4 min 9 s | 2 | 23 | — | 1,734,103 | 27,631 |
| agent `agent-a8c87e70` (write merge, format, tooling, limitations) | 6 min 44 s | 2 | 44 | — | 4,124,031 | 45,518 |
| agent `agent-a8ea9816` (write test decisions) | 4 min 57 s | 2 | 23 | — | 1,546,855 | 35,207 |
| agent `agent-aba69343` (write question-derived limitations and facts) | 3 min 7 s | 2 | 17 | — | 1,060,322 | 19,619 |
| agent `agent-ac17ea64` (write packages and tasks) | 3 min 20 s | 2 | 20 | — | 1,142,663 | 23,066 |
| agent `agent-adae64d9` (write package-group decisions) | 3 min 52 s | 2 | 20 | — | 1,413,643 | 26,785 |
| agent `agent-aebbe515` (write process-group entities) | 3 min 31 s | 2 | 17 | new 2 | 1,042,362 | 23,473 |
| agent `agent-aed14f41` (write error and doc decisions) | 3 min 29 s | 2 | 23 | — | 1,291,425 | 24,168 |
| piloting `session_971e6ece` (`/clear` only) | 0 s | 2 | 0 | — | 0 | 0 |
| piloting `session_4f59429d` (`/awawa-pr-review 95`, round 1 launcher) | 8 min 51 s | 4 | 6 | — | 341,376 | 3,137 |
| agent `agent-abc7dd4f` (review round 1, 10 threads, 4 commits) | 8 min 12 s | 2 | 32 | lint 13, fmt 7, status 4, show 3, context 2 | 2,779,688 | 26,593 |
| piloting `session_46d2a0ec` (feedback page, assets, starter) | 49 min 10 s | 12 | 68 | lint 7, new 6, show 4, status 4, fmt 4, refs 4, diff 1 | 13,751,129 | 85,142 |
| piloting `session_a1a83a75` (starter v2 draft) | 2 min 37 s | 3 | 8 | lint 1, new 1 | 403,390 | 16,646 |
| piloting `session_d7cf38f8` (feedback page republished) | 15 min 9 s | 9 | 27 | lint 4, show 3, fmt 2 | 3,377,965 | 25,287 |
| piloting `session_95cb7b16` (round 2 launcher, cleanups, `~/.ai` rule, tooling) | 55 min 50 s | 30 | 25 | — | 4,273,402 | 39,363 |
| agent `agent-a37effca` (review round 2, 7 threads, 3 commits) | 5 min 31 s | 2 | 33 | fmt 11, refs 7, lint 5, show 4, status 1 | 1,992,557 | 15,822 |
| agent `agent-a4693c2a` (`work_in_progress.md` cleanup, `a5d97e5`) | 6 min 7 s | 5 | 36 | status 5, show 5, fmt 2, lint 2 | 4,556,482 | 20,483 |
| agent `agent-aa557eda` (`findings.md` cleanup, `d93c51a`) | 5 min 45 s | 5 | 21 | lint 3, status 2, show 2, fmt 1 | 1,310,234 | 11,617 |
| agent `agent-a93bc6ac` (report tooling: collection share measure) | 3 min 17 s | 3 | 13 | — | 750,650 | 16,204 |
| agent `agent-a3d8faad` (report tooling: collection script) | 12 min 9 s | 4 | 50 | status 5, diff 4, lint 2 | 6,038,916 | 60,658 |
| piloting `session_500f1282` (template `c2068cd`, delairec/.ai#34) | 27 min 39 s | 4 | 66 | status 21, show 12, lint 6, new 3, fmt 1 | 8,332,450 | 61,053 |
| **Total, 28 transcripts** | **4 h 53 min** | **130** | **795** | **239** | **92,154,534** | **789,265** |

Subtotals: the migration (`session_895d269e` and its 12 agents) 331 answers, 43.3 M cache-read, 397 k written; the two review rounds with their launchers 71 answers, 5.1 M cache-read; the feedback-page work (3 sessions) 110 answers, 17.5 M cache-read; the report tooling (2 agents) 63 answers, 6.8 M cache-read, not the PR's own work. Not measured: `agent-a20223c8` (delairec/.ai#33) and `agent-a9e92b9d` (collection script, second pass), which name the branch only in tool results; the cut parts of `session_44495e5e` and `session_35dbe4a9`.

| Corpus lines changed | PR lines changed | Share |
|----------------------|------------------|-------|
| 8,831 (5,124 removed from `docs/*.awawa`, 3,707 added under `docs/awawa-project-methodology/`; `git-diff-stat.txt`) | 11,101 (+5,547/−5,554, 43 files; `git-diff-shortstat.txt`) | 79.6 % |

### Claims checked

| Claim | Check | Verdict |
|-------|-------|---------|
| PR body: `awawa lint --strict .` exits 0 on each of the three migration commits | `git archive` of `8389f29`, `a8be69c`, `61c80d7` into the scratchpad, `awawa lint --strict .` in each: exit 0, 0, 0 | confirmed |
| PR body table, after: 93 `DECISION`, 12 `PROCESS`, 9 / 8 `LIMITATION` / `FACT`, 0 `OPEN_QUESTION`, 15 `TASK` (10 archived, 1 implemented, 1 todo, 3 draft), 7 `PACKAGE`, 3 / 47 `PROJECT` / `PULL_REQUEST` | `corpus-status.txt` (`awawa status .` on `trees/head`) | confirmed |
| PR body table, before: 145 `DECISION` (12 superseded), 61 `OPEN_QUESTION` (41 superseded), 12 `TASK`, 7 `PACKAGE`, 1 `PROJECT`, 0 `PULL_REQUEST` | `awawa status .` on `trees/base`: 145 (12 superseded), 61 (41 superseded), 12, 7, 1, no `PULL_REQUEST` type | confirmed |
| PR body: « Old entities not carried over — 98 » | `awawa diff base head`: 221 removed, 202 added, 14 changed; the diff pairs by name and the migration renamed every entity, so the 98 cannot be recounted from it | unchecked |
| PR body: no `OPEN_QUESTION` survives, none blocked a task | `corpus-open-questions-live.txt`: 0 of 0; base had 20 live questions | confirmed for the head; the « none blocked a task » part unchecked |
| PR body point 3: `packageManager` `bun@1.3.14` and `devDependencies.bun` `1.4.2` break `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped` | `package.json` lines 5 and 57 at head; the decision's SPEC 3 « the same version in packageManager »; `git log` shows the bump in `bf98583` (#91) | confirmed |
| PR body point 5: `AnErrorsOriginDecidesItsPath` — « `MergeSaveFiles.ts` calls `presentMergedSaveUnusable` » | `MergeSaveFiles.ts` line 36 calls it; the decision's SPEC 2 anchors exactly `MergeSaveFiles.ts::this.presenter.presentMergedSaveUnusable()` and lint resolves it | overstated — no drift at head |
| PR body point 5: `BothMessageListsShareOneRendering` — the `severity` prop | `ValidationMessagesList.tsx` line 11 `severity: 'danger' \| 'warning'`, line 18 uses it; SPEC 2 says « never by a parameter of the component » | confirmed |
| PR body point 5: `MockIsolationIsAGlobalPreload` — `ui-save-manager` has no `bunfig.toml` | `ls packages/*/bunfig.toml`: 6 of 7 packages, `ui-save-manager` absent; its `package.json` has `test:ui` only, no `bun test` | confirmed, with the nuance that the package runs no `bun test` |
| PR body point 5: `NoSaveIsCommittedToThePublicRepository` — the generated e2e fixtures are committed | `git ls-files`: 4 `.json` under `packages/ui-save-manager/e2e/fixtures/`; README: « generated, never copied from a real game save » | confirmed |
| PR body point 6: `PROCESS` has no `APPLIES_TO_*` | `awawa show @SCHEMA.PROCESS`: `INCLUDE @FIELDSET.Ruling`, `SPEC`, `ENFORCED_BY` only | confirmed |
| PR body point 7 and thread 4035016350: the nested `REF` is declared seven times because of L023 and L003 | Probe on 2.7.0: L003 and L023 reproduced; `grep -c 'FIELD REF reference' _schema.awawa` = 8, of which 5 are repeats of the wildcard's `DESC › REF` | confirmed on the cause, overstated by one on the count (8, not 7) |
| PR body point 2: the decision ruling the language of `AGENTS.md` was not carried over | `awawa status DECISION` on `trees/base`: `@DECISION.LaSpecificationResteEnFrancaisMemePubliee` implemented; absent at head | confirmed |
| PR body point 1: `~/.ai/procedures/awawa-pr-review-round.md` follows the archived schema in delairec/.ai#34, to merge with this PR | `gh pr view 34 --repo delairec/.ai`: MERGED 2026-09-17T16:09:43Z | confirmed, already merged |
| PR body: provenance read again on `pull/15/head` gave 219 of 226 entities from other pull requests, 113 from #61 | At head `awawa refs @PULL_REQUEST.PCST61` counts 22 incoming, `PCST15` 3; the 226/219/113 figures describe a reading of the old corpus this report cannot reproduce | unchecked |
| Round 1 handback: 10 of 10 threads answered, 4 commits, 3 `DECISION`, 1 `TASK`, 1 `PULL_REQUEST` written | `pr-rendered.md`: 10 root threads before 09:05Z, all with a Clauche-bot reply; `git-log-p-corpus.patch`: 3 decisions added in `f2d98b6`/`5cb6797`, `@TASK.DOCS48` and `@PULL_REQUEST.PCST95` in `afe3c0f` | confirmed |
| Round 2 handback: 7 threads, 6 fixed, 1 answered; 3 decisions removed with no incoming reference; count 96 → 93 | `pr-rendered.md`: 7 root threads 12:29–13:05Z, all replied; `fe2003a` removes the 3; `awawa status` at `a8be69c` 93, at head 93 | confirmed |
| Thread 4037208559: `fmt` wraps at about 100 columns, only `--check` and `--rename` | Probe in the scratchpad: 168-character string wrapped; `fmt --help` lists `--check`, `--rename` | confirmed |
| Thread 4035016096: `SHAPE file_name` accepts `PR95_review` | `awawa show @SHAPE.file_name`: `MATCH "[A-Za-z0-9]+(_[A-Za-z0-9]+)*"`; `@USAGE_REPORT.PR95` opened by this report lints clean under it | confirmed |
| Thread 4035016498: a fact without `BASIS` must name its `UNTIL` | `awawa show @SCHEMA.FACT`: `DEFAULT hypothesis`, `WHEN BASIS hypothesis` → `UNTIL REQUIRED` | confirmed |
| Thread 4035016876: `_template.md` starts with `<!-- awawa-usage-report -->` | `reports-template.md` line 1 | confirmed |
| `commands.md` (« 26 commands refused … 6 in the session and 20 across 10 of the 12 agents ») | `grep -c 'Refusing to run it'` over the 22 timelines: 6 + 1+2+4+2+1+2+3+1+3+1 = 26, 10 agents | confirmed |
| `commands.md` (« 93 answers … 23.85 M cache-read tokens ») | `transcripts-measure.txt` for `session_895d269e`: answers 93, `cache_read_input_tokens` 23,852,435 | confirmed |
| Round 1 handback: « CI is green » | `gh pr checks 95` at head: 12 checks pass, 3 Netlify rule checks without a state | confirmed at head |
| Readiness comments: « checks green » at `d93c51a` and `05ca237` | comments 5711849895 and 5716492172 exist (`gh api …/issues/95/comments`); the check state at those heads is not kept | unchecked |
