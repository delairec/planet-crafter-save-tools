<!-- awawa-usage-report -->

# 2026-09-17 — PR #95 docs(awawa): migrate project methodology corpus to the target schema

| Field                                          | Value                                                                                                                                |
|------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------|
| **Author of this report**                      | `claude-fable-5-1` (Claude Fable 5.1), effort `high` (from `~/.claude/agents/awawa-usage-reporter.md`)                               |
| **Models that drove the sessions under review** | `Claude Opus 5 (1M context)`, from the `Co-Authored-By` trailer of 13 of the 20 commits (`git-log.txt`) and the `model` field `claude-opus-5` of the 7 transcripts checked; effort `medium` for the interactive sessions (the `/effort` command recorded at 2026-09-16T22:30:57 in `session_59926afe`, « saved as your default for new sessions »; a later change is `unchecked`)<br>the 5 `general-purpose` agents of the review and cleanup rounds and the 12 of the migration run with no effort in their definition: `unchecked`<br>7 commits carry no trailer (`1dbd31b`, `8f154aa`, `73a269c`, `e5ac6fe`, `7ea89fd`, `fa46414`, `1a7725d`): the owner's own, made outside a session (« j'ai mis à jour les fichiers manuellement », `session_46d2a0ec` 12:02:35) |
| **Sessions measured**                          | `session_fc288c99`, `session_59926afe` (hook failure, 2026-09-16 22:26–22:34)<br>`session_895d269e` (piloting, the migration) and its 12 agents `agent-a0377536` … `agent-aed14f41`<br>`session_4f59429d` (launcher, review round 1) and `agent-abc7dd4f` (round 1)<br>`session_46d2a0ec`, `session_a1a83a75`, `session_d7cf38f8` (feedback page and starter)<br>`session_95cb7b16` (launcher, review round 2, cleanups, report tooling) and `agent-a37effca` (round 2), `agent-a4693c2a`, `agent-aa557eda`, `agent-a93bc6ac`, `agent-a3d8faad`<br>`session_44495e5e` (report path commit, launch of this report)<br>excluded: `session_971e6ece` (a `/clear`, 0 answers) and `agent-a205de15` (this run, captured at its 5th answer by its own collection) |
| **Delivered**                                  | The corpus of `docs/*.awawa` (235 entities, French, 12 files) replaced by `docs/awawa-project-methodology/` (216 entities, English, 8 files, one per type) under a schema of 12 types, 9 shapes and 1 fieldset; `AGENTS.md`, the report template and the feedback-page assets rewritten to cite the new names; 20 commits, 42 files, +5 344 / −5 550 lines |

## Verdict in two lines

**Ours**: the migration landed a corpus that lints clean from the repository root at each of its three migration commits and at head, with all 17 review threads answered in two rounds of 8 and 6 minutes, but what it found undecided — seven open points, among them one live decision the code at head contradicts and four decisions whose text the code no longer matches — lives in the pull request body and nowhere in the corpus, which holds 0 `OPEN_QUESTION`.

**The tool's**: awawa 2.7.0 held every check the migration leaned on (L005 sequencing, L016 on 392 anchors, `refs` before each deletion, `status --where` to find the PR's own entity) and cost it two limits it had to write around — a nested `REF` declared 8 times because a redeclared wildcard field drops it, and a formatter that wraps at 100 columns with no option — plus one trap that swallowed this report's own collection: a workspace root narrower than the anchors is 392 L016 with no hint that the root is the problem.

## The owner's word

> This is the first report after the second migration of the corpus, carried out far more seriously than the previous one, with an iterative review of the schemas and of the types created. The next reports will therefore be more interesting, as we will be able to discover what this migration to a project managed with awawa specs really brings.

## Part A — Us: method, corpus, conduct

### What went well

| What | Evidence |
|------|----------|
| The sequencing was ruled before the first write | `session_895d269e`, ASK at 2026-09-16T22:56:26 (« Ancien et nouveau corpus ne peuvent pas coexister dans un commit (L005 + hook). Comment séquencer ? » and the scope of the session), answered 23:04:18; first corpus commit `8389f29` at 23:06:05 |
| Every migration commit lints clean from the root | `awawa lint --strict .` exit 0 on `git archive` trees of `8389f29` (22 entities), `a8be69c` (213), `61c80d7` (214) and `05ca237` (216), re-run by this report; `awawa fmt --check .` exit 0 at head; 403 reference sites, 0 unresolved (`corpus-status.txt`) |
| The corpus was written by fenced parallel agents | 12 `general-purpose` agents from `session_895d269e`: 5 classifiers launched 23:08:11–23:08:28 (14–19 answers each), 7 writers 23:14:37–23:14:53 (16–40 answers), every prompt ending « No git, no repository writes »; 7 of 7 writers hand back « lint exits 0 »; 213 entities committed in `a8be69c` at 23:27:57, 34 min after the session opened |
| Every review thread got a commit or evidence | 17 threads, 37 inline comments, 15 resolved, 2 « note pour moi-même » left to the author (`pr-rendered.md`); round 1: 10 threads, 4 commits, 8 min 12 s active (`agent-abc7dd4f`); round 2: 7 threads, 3 commits, 5 min 31 s (`agent-a37effca`) |
| One readiness comment, edited in place | the `Clauche-bot` comment of 2026-09-17T09:08:18Z carries « Updated after … head d93c51a » then « head 05ca237 »: three states, one comment |
| The PR body describes the head at each push | its commit table names all 20 commits of the range (`range.txt`: 20 listed, 20 in `base..head`); `DECISION` count rewritten to 93 after `fe2003a`; rewrites at 09:04:45 (`agent-abc7dd4f`), 13:13:34 (`agent-a37effca`), 14:57:11 (`session_44495e5e`) |
| The automated GitHub reviewer found what two humans and one round missed | `claude[bot]` threads 4036818954 (a decision contradicting the README it governs), 4036819602 (a dead path in a `REJECTED`), 4036820607 (an orphaned starter draft), all in files this PR wrote; fixed in `b1ae3e5` and `ca2d604` |
| The recording threshold was enforced by the reviewer | 3 decisions removed in `fe2003a` on « inutile de consigner ça » ×3 (threads 4037127266, 4037128275, 4037131732), `awawa refs` reporting 0 incoming on each before deletion (`agent-a37effca` hand-back) |
| Two design files retired by classification, not by deletion | `work_in_progress.md`: 23 sections classed a/b/c, removed in `a5d97e5` (`agent-a4693c2a`); `findings.md`: 1 sentence found nowhere else, handed to the user, removed in `d93c51a` (`agent-aa557eda`) |
| A general rule went to its home, not to the project | the pre-commit hook PATH defect (`session_fc288c99` 22:27:11, `session_59926afe`) fixed in `~/.ai` PR #32, merged at 22:33:55, 7 min after the first report; the « a dated document is never rewritten » sentence became REP-6 in `~/.ai` PR #33 (`session_95cb7b16` 13:47:25–13:50:31) |
| The starter was reviewed before being handed to the awawa team | 8 inconsistencies (ASK 09:48:45) then 3 more (ASK 12:04:19) found and fixed in `session_46d2a0ec`; committed copy and page export compared word by word: 1 391 words each, same order (`session_d7cf38f8` 12:49:41) |
| The pull request is an entity of the corpus, found through the tool | `awawa status PULL_REQUEST --where NUMBER==95 --where PROJECT==@PROJECT.PCST` → `@PULL_REQUEST.PCST95` (`corpus-pull-request.json`); `awawa refs` → cited by `@TASK.DOCS48` |
| Checks green at head, tree untouched | `bun test` 814 pass / 0 fail, `lint:types` 7 packages exit 0, `audit:quality` exit 0, `git status --porcelain` empty before and after (`checks-*.txt`) |

### What went wrong

| What | Evidence | Whose fault |
|------|----------|-------------|
| Seven open points live in the PR body and nowhere in the corpus | PR body « Open points » 1–7; `corpus-open-questions-live.txt`: 0 of 0; points 2, 3, 5 and 6 are project facts (the language of `AGENTS.md`, the Bun versions, four decisions versus the code, the `PROCESS` edges); the threshold « une `OPEN_QUESTION` que si une tâche l'attend » (`AGENTS.md`; `BLOCKS @TASK` `REQUIRED`) leaves a fact no task waits on with no entity, and neither `LIMITATION` nor a `draft` `TASK` was used for them | corpus modelling |
| A live decision the code at head contradicts, known and left | `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped`, third `SPEC` (« the same version in packageManager »): `package.json` line 5 `"packageManager": "bun@1.3.14"`, line 57 `"bun": "1.4.2"` — bumped by PR #91, whose review missed the realignment the decision's `RATIONALE` assigns to it; lint passes because both `IMPL` anchors resolve textually | conduct (the review of #91) and corpus modelling (no entity form for the drift) |
| Four carried decisions whose text the code no longer matches, listed in the body, recorded nowhere | checked by this report: `@DECISION.BothMessageListsShareOneRendering` second `SPEC` (« never by a parameter of the component ») vs `ValidationMessagesList.tsx` line 11 `severity: 'danger' \| 'warning'`; `@DECISION.MockIsolationIsAGlobalPreload` second `SPEC` (« in every package ») vs no `packages/ui-save-manager/bunfig.toml` (6 of 7 packages have one); `@DECISION.NoSaveIsCommittedToThePublicRepository` vs 4 generated save fixtures tracked under `packages/ui-save-manager/e2e/fixtures/`; `@DECISION.AnErrorsOriginDecidesItsPath` `RATIONALE` (« no presenter outcome is added for » a parse error met by the merger) vs `MergeSaveFiles.ts` lines 35–37 calling `presentMergedSaveUnusable()` on parse errors | corpus modelling |
| Three decisions written by round 1 and deleted by round 2 | `f2d98b6` and `5cb6797` wrote `AFactIsAHypothesisUntilItIsAttested`, `AUsageReportIsNamedByItsSubjectAlone`, `AUsageReportKeepsTheTemplateTagOnItsFirstLine` for three schema edits; `fe2003a` removed them on the user's three « inutile de consigner ça » — against « une `DECISION` … ne s'écrit que si une pull request future pourrait faire l'inverse par erreur » (`AGENTS.md`) | conduct (round 1) |
| `PROCESS` lost two edges | body point 6; `awawa show PROCESS .` declares no `APPLIES_TO_*`, so `@PROCESS.ATestTaskChangesTheUiOnlyForItsOwnSake` has no edge to `@PACKAGE.ui_save_manager` nor to `@TASK.TEST40` | schema |
| The migration session ran 93 answers in one context | `session_895d269e`: 23.85 M cache-read tokens, 15.26 M of them (64 %) after the 50th answer; median read per answer 139 k over answers 1–25, 384 k over 76–93 (per-answer usage extracted from the transcript by this report) | method (PROC-4 names the fifty-step hand-over for procedures, the interactive session had no equivalent) |
| 26 commands refused by the worktree-isolation guard | 6 in `session_895d269e` (23:05:45 → 23:27:41) and 20 across 10 of the 12 migration agents (timelines 11–22), each a compound command with a runtime-computed value or a `git` the guard cannot verify; one wasted answer each, of the 331 the migration spent | conduct |
| The base of the PR was deduced, not asked | `session_895d269e` 23:34:55: « je l'ai déduite de l'historique »; `commands.md`: « Where no pull request exists yet, ask »; the deduction (`master`) was right and stated | conduct |
| Round 1 skipped the per-worktree setup | `agent-abc7dd4f` hand-back: no `node_modules`, no `input` link, no private clone, « I did not set them up »; round 2 ran `bun install --frozen-lockfile`; no test ran in round 1, so no verdict was wrong | conduct |
| The collection instrument of this report, on its first run as a script, wrote three wrong files and swept in its own run | `--corpus docs/awawa-project-methodology` made every awawa command run with the corpus folder as workspace root: `corpus-lint-strict.txt` exit 1 with 392 L016 where `awawa lint --strict .` exits 0; `tree-base.txt` failed (« no corpus file at bf98583 », the base corpus being `docs/*.awawa`), so no `corpus-diff` nor `corpus-entities-of-diff` exists; `corpus-project-of-repository.json` carries the JSON plus an exit-code trailer that `json.load` refuses; the transcript sweep by branch name measured the launcher `session_44495e5e` and this run itself (`agent-a205de15`, 5 answers) | report |

### Remediations, by return

| # | Change | What it fixes |
|---|--------|---------------|
| 1 | Record the five corpus-versus-code drifts as entities in a follow-up: a `draft` `TASK` for each fix the code should take (realign `packageManager` with `devDependencies.bun`; add `packages/ui-save-manager/bunfig.toml`), a `LIMITATION` with `UNTIL` for each drift kept, a rewrite in place of each decision whose text is stale while the code is right (the `severity` prop, the parser outcome, the generated fixtures) — a proposal to the user; this report opens no `OPEN_QUESTION` for it, the schema requiring `BLOCKS @TASK` and no task waiting on these | rows 1–3 of « What went wrong »: the corpus stops asserting what the code contradicts, and each drift gets an anchor lint can fail on |
| 2 | Give the points a migration or a review leaves undecided a home other than the PR body: a `draft` `TASK` the user ratifies or drops, or a `LIMITATION` — a `PROCESS` to rule, proposal to the user | row 1: seven points readable after the merge by `awawa status TASK --where STATUS==draft .` |
| 3 | Add `APPLIES_TO_PACKAGE` and `APPLIES_TO_TASK` to `SCHEMA PROCESS` (one `INCLUDE` line if they move into a `FIELDSET`, `INCLUDE` working at type level) and restore the two edges of `@PROCESS.ATestTaskChangesTheUiOnlyForItsOwnSake` — schema change to ratify | row 5 |
| 4 | In `~/.ai/procedures/awawa-pr-review-round.md`: before writing a `DECISION` for a thread, apply the threshold « could a future pull request do the opposite by mistake », and record nothing beyond the commit when the thread only changes a schema line — general rule, by PR on `~/.ai` | row 4: 3 entities, 1 commit and 3 threads |
| 5 | `collect.py`: read the workspace root from `awawa status --json .` and pass `.` to every awawa command, never the corpus folder; write the exit-code trailer beside the `.json` files, not inside; exclude the launching session and the running agent from the transcript sweep; when the base holds no corpus at the head's path, say so and diff the whole workspace — a fix in `~/.claude`, addressed to the launching session | row 10 |
| 6 | Plan the hand-over of an interactive session expected to pass fifty answers (a migration, a wave) as PROC-4 does for procedures: a progress file and a fresh session at the fiftieth — general rule, `~/.ai` | row 6: 15.26 M of the 23.85 M cache-read tokens of the migration session |
| 7 | Write the commands the worktree guard refuses as plain ones from the worktree: no loop calling `awawa` or `git` on a computed value, no `sed -n "$(...)"` — one line in `commands.md`, general | row 7: 26 answers |

## Part B — The tool

### What went well

| Promise | Kept | Evidence |
|---------|------|----------|
| One `SCHEMA *` per walk (L005), so the two corpora could not coexist | yes | the ASK of 22:56:26 names « L005 + hook »; `8389f29` removes `docs/*.awawa` (12 files at `e5ac6fe`, 1 `.awawa` file at `8389f29`) in the commit that writes the schema; no commit carries both |
| `lint --strict` from the root on 216 entities, 403 sites, 392 anchors | yes | exit 0 on the 4 trees re-run by this report |
| `status TYPE --where` finds a pull request's entity without `grep` | yes | `NUMBER==95` + `PROJECT==@PROJECT.PCST` → `@PULL_REQUEST.PCST95` (`corpus-pull-request.json`) |
| `refs` gives the incoming edges before a deletion | yes | 3 deletions in `fe2003a`, each preceded by `refs` = 0 incoming; `@PULL_REQUEST.PCST95` ← `@TASK.DOCS48 SOURCE.REF` |
| `new` renders the migrated schema as a skeleton, wildcard fields and `WHEN` summary included | yes | `awawa new OPEN_QUESTION FooBar .` prints `DESC`, `REF`, `BLOCKS`, the commented `RATIONALE`, `SOURCE`, `STATUS active` and « WHEN STATUS archived: ARCHIVED_ON (required), PURGE » (this report) |
| L016 refuses `..` and resolves `path::literal` at the symbol | yes | probe corpus of this report: « anchor path ../outside.txt climbs out of the workspace with .. »; `sub/file.txt::hello` resolves |
| `fmt --check` then `lint --strict .` as the commit gate | yes | the repository's `pre-commit` hook runs both on the tracked `.awawa` files; `corpus-fmt-check.txt` exit 0 |
| The 25 strengths of `2026-09-16-corpus-bootstrap-method.md` part B | yes | none contradicted by this PR's evidence; cited, not restated |

### What went wrong

| Defect | Observation | Checked against |
|--------|-------------|-----------------|
| A nested `REF` cannot be declared once for every prose field | `INCLUDE @FIELDSET.X` under a `FIELD` is L023 (« `INCLUDE` is not a FIELD declaration »); a type redeclaring the wildcard's `DESC` loses its nested `REF` (L003 « field REF is not declared on FOO.DESC »); `_schema.awawa` at head carries 8 `FIELD REF reference` lines, 3 of them the wildcard's `DESC` repeated on `FACT`, `OPEN_QUESTION` and `PACKAGE` (thread 4035016350, PR body open point 7) | run (probe of this report on 2.7.0), manual |
| `fmt` wraps any string past about 100 columns onto `+` lines, with no option | a 146-character `DESC` came back on two lines; `fmt` takes `--check` and `--rename` only; the user asked why (thread 4037105533) and accepted once told | run (probe), transcript (`agent-a37effca`) |
| A workspace root narrower than the anchors is a wall of L016 with no hint | `awawa lint --strict docs/awawa-project-methodology` from the repository root: 392 × « anchor path README.md does not exist under the workspace root », exit 1; `awawa lint --strict .`: exit 0; the skill documents the case, the output never says the path exists one directory up | run (`corpus-lint-strict.txt`, re-run) |
| `show` refuses the wildcard schema entry | `awawa show '*' .` → « `*` is not a target; expected @TYPE.Name or TYPE »; the fields `SCHEMA *` declares are read through `new` or `--with-schema` only | run (this report) |
| `status TYPE` prints `(none)` for a `STATUS` left to its `DEFAULT` | 201 of 216 entities at head print `(none)` (`corpus-status.txt`); defect 2 of `2026-09-16-corpus-bootstrap-method.md` part B, seen again | run |

The 23 defects of `2026-09-16-corpus-bootstrap-method.md` part B stand as written; this PR's evidence adds the first four rows above. None of the five is caused by our corpus: the `PROCESS` edges of part A row 5 are our schema's.

### Remediations

| Proposal | What it would have changed on this PR |
|----------|---------------------------------------|
| Let `INCLUDE` apply under a `FIELD`, or make a type's redeclaration of a wildcard field inherit that field's nested fields unless it redeclares them | 3 of the 8 `FIELD REF reference` lines disappear, and the next prose field costs one line instead of three |
| `fmt --width N`, or the wrap column stated in `fmt --help` | thread 4037105533 and its probe would not have been needed |
| When an anchor fails under the root but resolves under an ancestor holding a `.git`, say so in the L016 message | the 392 L016 of this report's collection would read as a wrong root, not as a failing corpus |
| `show *` (or `show SCHEMA`) prints the wildcard entry | the wildcard's fields read through `show` like any type's |

## Recurring points

No previous report: `docs/awawa-usage-reports/pull-requests/` holds nothing at `05ca237` (`previous-reports.md`), and the eleven reports under `migration/2026_09_15-first-project-methodology-migration/` and `2026-09-16-corpus-bootstrap-method.md` are not previous reports (command section 1, item 4; launch instruction). Every point of this report counts 1.

### Recurring pain points

| Point | Times seen | First raised in | Seen again here | Remediation proposed then | Done? |
|-------|------------|-----------------|-----------------|---------------------------|-------|
| none  | —          | —               | —               | —                         | —     |

Raised before and not seen again, closed by observation: none (no previous report).

### Recurring strengths

| Mechanism | Times seen | First noted in | Held again here |
|-----------|------------|----------------|-----------------|
| none      | —          | —              | —               |

Noted before and not seen holding here: none (no previous report).

## Follow-up on the previous report's remediations

| Remediation | Done? | Evidence |
|-------------|-------|----------|
| none        | —     | no previous report |

## Appendix

### Measurements

Figures from `measure.py` over the transcripts `transcripts.md` marks measured (`transcripts-measure.txt`); active time counts gaps under ten minutes only; « answers » is one model answer, the step of PROC-2. Agents of one session run in parallel, so their active time does not add to the session's. `session_971e6ece` (0 answers) and `agent-a205de15` (this run) are left out.

| Session | Active time | User turns | Answers | Tool calls | awawa calls by subcommand | Tokens read from cache | Tokens written |
|---------|-------------|------------|---------|------------|---------------------------|------------------------|----------------|
| `session_fc288c99` (hook failure) | 3 min 10 s | 7 | 4 | 0 | — | 139 406 | 2 559 |
| `session_59926afe` (hook fix, `~/.ai` #32) | 3 min 26 s | 10 | 15 | 14 | — | 781 156 | 7 529 |
| piloting `session_895d269e` (migration) | 40 min 43 s | 4 | 93 | 131 | lint 16, show 15, fmt 12, status 9, new 6, diff 2, context 1 | 23 852 435 | 85 835 |
| 12 migration agents (5 classify, 7 write) | 2 min 14 s – 6 min 44 s each | 2 each | 238 | 265 | diff 2, new 2 | 19 473 298 | 311 337 |
| `session_4f59429d` (launcher, round 1) | 8 min 51 s | 4 | 8 | 6 | — | 341 376 | 3 137 |
| agent `agent-abc7dd4f` (review round 1) | 8 min 12 s | 2 | 33 | 32 | lint 13, fmt 7, status 4, show 3, context 2 | 2 779 688 | 26 593 |
| `session_46d2a0ec` (feedback page, starter) | 49 min 10 s | 12 | 72 | 68 | lint 7, new 6, show 4, status 4, fmt 4, refs 4, diff 1 | 13 751 129 | 85 142 |
| `session_a1a83a75` (starter v2 draft) | 2 min 37 s | 3 | 7 | 8 | lint 1, new 1 | 403 390 | 16 646 |
| `session_d7cf38f8` (feedback page) | 15 min 9 s | 9 | 31 | 27 | lint 4, show 3, fmt 2 | 3 377 965 | 25 287 |
| `session_95cb7b16` (launcher, round 2, cleanups, report tooling) | 55 min 50 s | 30 | 52 | 25 | — | 4 273 402 | 39 363 |
| agent `agent-a37effca` (review round 2) | 5 min 31 s | 2 | 30 | 33 | fmt 11, refs 7, lint 5, show 4, status 1 | 1 992 557 | 15 822 |
| agent `agent-a4693c2a` (`work_in_progress.md` cleanup) | 6 min 7 s | 5 | 35 | 36 | status 5, show 5, fmt 2, lint 2 | 4 556 482 | 20 483 |
| agent `agent-aa557eda` (`findings.md` cleanup) | 5 min 45 s | 5 | 22 | 21 | lint 3, status 2, show 2, fmt 1 | 1 310 234 | 11 617 |
| agent `agent-a93bc6ac` (collection-share measure) | 3 min 17 s | 3 | 14 | 13 | — | 750 650 | 16 204 |
| agent `agent-a3d8faad` (collection script) | 12 min 9 s | 4 | 49 | 50 | status 5, diff 4, lint 2 | 6 038 916 | 60 658 |
| `session_44495e5e` (report path commit, launch of this report) | 17 min 10 s | 6 | 47 | 49 | status 18, show 7, fmt 1, lint 1, refs 1 | 3 979 272 | 24 439 |
| **Total** | 3 h 16 min of top-level active time | 85 (top-level) | 750 | 778 | 224 (lint 54, status 48, fmt 40, show 43, new 15, refs 12, diff 9, context 3) | 87 801 356 | 752 651 |

Two agents of `session_95cb7b16` are listed as not measured by `transcripts.md` (`agent-a20223c8`, the `~/.ai` REP-6 pull request; `agent-a9e92b9d`, the second pass on the collection script): their task notifications give 62 027 and 106 401 subagent tokens.

| Corpus lines changed | PR lines changed | Share |
|----------------------|------------------|-------|
| 8 831 (+3 707 / −5 124 over the `.awawa` files, `git diff --numstat`) | 10 894 (+5 344 / −5 550, `git-diff-shortstat.txt`) | 81 % |

### Claims checked

| Claim | Check | Verdict |
|-------|-------|---------|
| PR body: « `awawa lint --strict .` exits 0 on each of the three migration commits » | re-run on `git archive` trees of `8389f29`, `a8be69c`, `61c80d7` | confirmed |
| PR body, « Before »: `DECISION` 145 (12 superseded), `OPEN_QUESTION` 61 (41 superseded), `TASK` 12, `PACKAGE` 7, `PROJECT` 1, `PULL_REQUEST` 0 | `awawa status .` and `--where STATUS==superseded` on the tree of `bf98583` | confirmed |
| PR body, « After »: `DECISION` 93, `PROCESS` 12, `LIMITATION` 9, `FACT` 8, `OPEN_QUESTION` 0, `TASK` 15 (10 archived, 1 implemented, 1 todo, 3 draft), `PACKAGE` 7, `PROJECT` 3, `PULL_REQUEST` 47 | `corpus-status.txt` | confirmed |
| PR body: « Old entities not carried over — 98 » | base holds 226 data entities (235 minus 9 schema entities); the mapping tables of the migration (`scratchpad/mig/map/*.tsv`) no longer exist | unchecked |
| PR body: « The old files stay in git history at `e5ac6fe` » | `git ls-tree e5ac6fe docs/`: 12 `.awawa` files | confirmed |
| PR body: « Anchors are workspace-relative and `..` is refused (L016) » | probe: L016 « climbs out of the workspace with .. » | confirmed |
| PR body: « Provenance: … 219 of them came from other pull requests (113 from #61) » | needs `git log -S` per entity on `pull/15/head` | unchecked |
| PR body, open point 3: `packageManager` `bun@1.3.14` and `devDependencies.bun` `1.4.2` break `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped` | `package.json` lines 5 and 57; the decision's third `SPEC` | confirmed |
| PR body, open point 5: `@DECISION.AnErrorsOriginDecidesItsPath` — « `MergeSaveFiles.ts` calls `presentMergedSaveUnusable` » | line 36 calls it on parse errors; the `RATIONALE` says no outcome is added for that case | confirmed |
| PR body, open point 5: `@DECISION.BothMessageListsShareOneRendering` — « the `severity` prop » | `ValidationMessagesList.tsx` line 11 declares `severity` | confirmed |
| PR body, open point 5: `@DECISION.MockIsolationIsAGlobalPreload` — « `ui-save-manager` has no `bunfig.toml` » | `ls packages/*/bunfig.toml`: 6 packages, not `ui-save-manager` | confirmed |
| PR body, open point 5: `@DECISION.NoSaveIsCommittedToThePublicRepository` — « the generated e2e fixtures are committed » | `git ls-files`: 4 `.json` fixtures under `packages/ui-save-manager/e2e/fixtures/`, README: « Save files used by the scenarios. They are generated » | confirmed |
| PR body, open point 6: « `PROCESS` has no `APPLIES_TO_*` » | `awawa show PROCESS .` | confirmed |
| PR body, open point 7 and thread 4035016350: the nested `REF` « declared seven times » because of L023 and L003 | probe: L023 and L003 reproduced; at head 8 `FIELD REF reference` lines, 3 forced repeats | confirmed in substance; count at head is 8 lines, 3 repeats |
| Thread 4035016096: `SHAPE file_name` no longer imposes the prefix, `[A-Za-z0-9]+(_[A-Za-z0-9]+)*` | `awawa show @SHAPE.file_name .` | confirmed |
| Thread 4035016876: `_template.md` starts with `<!-- awawa-usage-report -->` | first line of the template at head | confirmed |
| Thread 4037208559: `fmt` « breaks any string longer than about 100 columns at a space and continues it with `+` », no flag | probe: a 146-character `DESC` wrapped; `fmt` flags `--check`, `--rename` | confirmed |
| Thread 4037207783: the first `SPEC` of `@DECISION.ArtifactAssetsFollowThePublishedPage` anchors four exports and the starter | `awawa show`: 5 `IMPL` under the first `SPEC` (zip + 4 exports), starter under the second; lint 0 | confirmed |
| Round 1 hand-back: « This PR has no `TASK` naming it in `DELIVERED_BY` » | `corpus-tasks-of-pr.json`: 0 entities | confirmed |
| Round 2 hand-back: « `@PULL_REQUEST.PCST95` is still cited by `@TASK.DOCS48` »; « 93 `DECISION` (it was 96) » | `awawa refs`; `corpus-status.txt` | confirmed |
| Round 1 hand-back: « CI is green … MERGEABLE / CLEAN » at `5cb6797` | GitHub state at that head not re-read; checks re-run at `05ca237` green | unchecked at that head |
| `session_895d269e` final text: 7 commits on the branch at PR creation, base `master` deduced | `git-log.txt`: 7 commits before 2026-09-17; `range.txt`: merge-base `bf98583` = `origin/master` | confirmed |
