<!-- awawa-usage-report -->

# 2026-09-18 — PR #100 chore(deps): align packageManager on the Bun release of devDependencies.bun

| Field                                          | Value |
|------------------------------------------------|-------|
| **Author of this report**                      | `Claude Fable 5.1` (`claude-fable-5-1`), effort `high` (`~/.claude/agents/awawa-usage-reporter.md`, `model: fable`, `effort: high`) |
| **Models that drove the sessions under review** | `Claude Opus 5`, the trailer of both commits of the range (`git-log.txt`: `1f99ea1`, `7809fbe`) and of the four commits the review rewrote (`ae3a761`, `c64e864`, `fa462a3`, `ed04589`, `git show --format=%B`)<br>Wave pilot `session_51c18626`: effort `medium`, set by `/effort medium` at 2026-09-18T13:34:59Z and saved as the default for new sessions (`timeline-01` line 3)<br>Review pilot `session_d9c08253`, opened by `/clear` at 13:54:20Z: effort `unchecked` (no `/effort` line before the cut; the saved default above makes `medium` likely, not read)<br>Agents launched by `/awawa-plan-wave-start` and `/awawa-pr-review` (`general-purpose`): model inherited from the launching session, effort `unchecked` |
| **Sessions measured**                          | Piloting: `session_51c18626` (wave launch, 13:34–13:52Z, shared with PRs #101–#103), `session_d9c08253` (review rounds and the sibling rebases, read up to 14:11:30.792Z, the turn that launched this reporter)<br>Agents of this PR: `agent-ada9d212` (CHORE49 delivery), `agent-acea5d22` (review round 1), `agent-a62b89e8` (review round 2, cut at 14:11:30Z)<br>Agents this PR's schema change caused on the sibling PRs, measured apart: `agent-ab415d3f` (PR #101 rebase, measured by hand with `measure.py`, cut at 14:11:30Z), `agent-a559648d` (PR #102), `agent-a84a7e10` (PR #103)<br>Listed by `transcripts.md` because they share `session_51c18626`, attributed to PRs #101–#103 and excluded from this PR's figures: `agent-a29933ea`, `agent-aa2411a6`, `agent-ac59e89a`, `agent-a1ccac75`, `agent-a367d94a`, `agent-a4791676`, `agent-af2e08bd` |
| **Delivered**                                  | `package.json` line 5 `packageManager` from `bun@1.3.14` to `bun@1.4.2` (1 line); `@TASK.CHORE49` to `implemented` with `DELIVERED_BY 100` and one `IMPL`; the review's rework of how a delivery is recorded: `@SCHEMA.TASK` `DELIVERED_BY` from `@PULL_REQUEST` with `CONVERSE DELIVERS` to `uint`, `@SCHEMA.PULL_REQUEST` `DESC`, the `DELIVERED_BY` of 11 archived tasks rewritten as numbers, `@PULL_REQUEST.PCST100` created then deleted, `AGENTS.md` « Suivi d'une PR »; 4 files, +23/−19 lines, 2 commits, rebase-merged as `ecaf399` and `6a1b54d` at 2026-09-18T14:08:00Z |

## Verdict in two lines

**Ours**: a one-line task was delivered in 3 min 19 s and merged 28 min 43 s after its agent was launched, and its review overturned in one round the way the corpus records a delivery (`DELIVERED_BY` from a `@PULL_REQUEST` reference to a number, 11 tasks rewritten, `@PULL_REQUEST.PCST100` created then deleted) at the price of 3 rebase agents on PRs #101–#103 (47 answers, 2.36 M cache-read tokens); but the owner merged at 14:08:00Z, before CI had finished on the final head (14:11:22Z) and before this report, which the pilot had announced at 14:01:40Z as the next step — so it lands in a docs pull request of its own, against `@DECISION.AUsageReportIsCommittedInTheTaskPullRequest` — and the delivery agent met 4 command refusals in 28 answers, 3 of them after reading the rule written against them the day before.

**The tool's**: awawa 2.7.0 carried the schema change with no workaround — `diff` rendered the field-type change, the `CONVERSE` removal and the 12 task edits at field level in 0.0 s, `status --where DELIVERED_BY==100` finds the task on a `uint`, L007 refuses the old reference form on every live task and L006 refuses the promotion without a number — and cost two observations: `lint --closure` has no `--skip`, so the closure of a one-`SPEC` task reached `@PROJECT.PCST` through its `SOURCE` chain and listed 53 referrers and 43 unrelated pull requests in its footer (`context --skip provenance` lists 1), and `status` still counts the 106 `DECISION` at their default `STATUS` as `(none)`.

## Part A — Us: method, corpus, conduct

### What went well

| What | Evidence |
|------|----------|
| The task was delivered in 3 min 19 s, from a written brief, in its own worktree | `agent-ada9d212`: launched 13:39:19Z, `ae3a761` at 13:40:29Z, PR #100 opened at 13:41:06Z (1 min 47 s after launch), `c64e864` at 13:41:36Z, hand-back 13:42:00Z; 28 answers, 42 tool calls (`transcripts-measure.txt`, `timeline-06`) |
| The task was read through the tool, not the files | `timeline-06` 13:39:36Z: `awawa context @TASK.CHORE49 --with-schema .`, `awawa lint --closure @TASK.CHORE49 .`, `awawa show @DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped .` in the first minute; 19 `awawa` calls in 28 answers (`context` 1, `lint` 7, `show` 4, `refs` 1, `fmt` 5, `status` 1); `awawa refs @TASK.CHORE49 .` → 0 incoming, reported in the hand-back |
| The `IMPL` anchor names the field, not the version | `@TASK.CHORE49` `IMPL "package.json::\"packageManager\""` (`corpus-show-TASK-CHORE49.txt`); the hand-back at 13:42:00Z: « a value anchor would break lint on every Dependabot bump, which is the per-PR failure the decision rejects »; L016 resolves it (`corpus-lint-strict.txt` exit 0) |
| The pilot sent an unbacked claim back before relaying it | The hand-back at 13:42:00Z flagged its own PR body line (« 814 pass, 0 fail » under the global Bun 1.3.14 « not shown by the output I captured »); the pilot's `SendMessage` at 13:42:13Z asked for a re-run; the agent re-ran, read « 814 pass », « 0 fail » and kept the line at 13:42:34Z (`timeline-01`, `timeline-06`) |
| The wave asked its three questions in one call | `timeline-01` 13:36:07Z: one `AskUserQuestion` with 3 questions (cap, scope, extra instruction); `transcripts-measure.txt`: 1 call in 33 answers |
| Review round 1 addressed 2 threads in 6 min 43 s, the modelling change in 1 min 48 s | `agent-acea5d22`: launched 13:54:51Z, `fa462a3` at 13:56:39Z (schema, 12 `DELIVERED_BY` lines, `sources.awawa`, `AGENTS.md`), reply 4047369064 at 13:56:56Z; owner's suggestion 4047378669 at 13:58:07Z applied in `ed04589` at 13:59:27Z; readiness comment 14:01:18Z (`pr-rendered.md`, `timeline-08`) |
| Round 1 checked the incoming edges before deleting `@PULL_REQUEST.PCST100` | `timeline-08` 13:55:30Z `awawa status PULL_REQUEST . \| head -70` (incoming counts before), 13:56:16Z `awawa status PULL_REQUEST . \| grep 'incoming 0'` (after); at head 0 `PULL_REQUEST` with `incoming 0` (this report, `awawa status PULL_REQUEST .` on `trees/head`); `@PULL_REQUEST.PCST90` incoming 2 at base, 1 at head (`awawa refs`) |
| Round 1 named the ripple to the tooling outside the repository instead of editing it | Hand-back 14:01:31Z: `~/.claude/commands/awawa-plan-wave-start.md:172`, `awawa-usage-report.md:41` and `:110` still expected `DELIVERED_BY @PULL_REQUEST.<pr>`, « whether the fix is general or project-specific is the user's call »; the owner answered at 14:03:25Z; the pilot edited the files and `collect.py` at 14:05–14:06Z (`timeline-02`); today `awawa-usage-report.md` line 39 reads `DELIVERED_BY==<N>` and `awawa-plan-wave-start.md` lines 165 and 173 carry the number |
| Round 1 wrote no `DECISION` and said why | Reply 4047369064: « No DECISION recorded: the schema now refuses the old form »; `corpus-diff.txt` lists no `DECISION` among the 14 entities changed |
| Round 2 split the branch in two commits that each lint clean alone, with a leased force-push | `timeline-07`: `git reset --mixed origin/master` 14:05:57Z, commits 14:06:44Z and 14:06:58Z, `git push --force-with-lease=chore/49-bun-version-alignment:ed04589` 14:07:04Z; trees: `7809fbe` = `ed04589` = `6a1b54d` (`ef68597`), `1f99ea1` = `ecaf399` (`8fa5d2d`); `awawa lint --strict .` and `awawa fmt --check .` exit 0 on the extracted tree of `ecaf399`, whose `package.json` still reads `bun@1.3.14` |
| The PR body described the head through the rework | Body edited at 13:42:25Z (tests line), in round 1 (« I also updated the PR body », hand-back 14:01:31Z) and at 14:07:55Z (« Commits » section with the two-commit order and its reason) (`timeline-06`, `timeline-07`, `pr-rendered.md`) |
| The round-1 launch prompt stated the per-worktree setup as checks to run | `timeline-08` first line: « Do not assume the per-worktree setup … check each item in the worktree itself »; round 1 ran `bun install --frozen-lockfile` (nothing to change), created the `input` symlink and ran `private:sync` at 13:55:57Z |
| Both commits carry the model trailer | `git-log.txt`: `Co-Authored-By: Claude Opus 5` on `1f99ea1` and `7809fbe` |
| Checks green on the merged tree | 9 GitHub checks pass on `7809fbe` (`gh pr checks 100`); on this worktree at `6a1b54d`, whose tree equals `7809fbe`'s: `bun test` 814 pass, 0 fail under the global Bun 1.3.14, `bun run lint:types` exit 0, `bun run audit:quality` exit 0, `git status --porcelain` empty after; the script's own check step was skipped (`checks-skipped.txt`: HEAD is not the PR head) |

### What went wrong

| What | Evidence | Whose fault |
|------|----------|-------------|
| The PR was merged before its usage report and before CI had finished on the final head | `gh pr view 100 --json mergedAt`: 2026-09-18T14:08:00Z; `gh run list --commit 7809fbe`: runs created 14:07:17Z, UI tests completed 14:08:57Z and 14:09:17Z, Claude Code Review 14:11:22Z; round 2's last text at 14:08:02Z « Now waiting for CI on the new head »; the pilot's relay at 14:01:40Z: « The usage report (`/awawa-usage-report 100`) runs next, before you merge »; `@DECISION.AUsageReportIsCommittedInTheTaskPullRequest` SPEC 1 and `@PROCESS.EverythingLandsInTheTaskPullRequest` SPEC 5 say the report is committed on the task branch before the merge; this report is a docs pull request of its own and recreates `@PULL_REQUEST.PCST100`, which round 1 had deleted | conduct |
| The readiness comment reads as an authorisation to merge now | `~/.ai/procedures/awawa-pr-review-round.md` line 169: « Ready to be merged — review addressed, corpus updated, checks green. Merging is yours. »; line 177 of the same file: « On a task PR, the usage-report procedure commits its report on this branch next »; the comment posted at 14:01:18Z carried the first sentence and not the second | method |
| The modelling of a delivery set by the migration was overturned by the first task PR after it | Base `docs/_schema.awawa` 203–205: `FIELD DELIVERED_BY @PULL_REQUEST`, `CONVERSE DELIVERS`; the wave prompt to the agent: « the task `implemented` with `DELIVERED_BY @PULL_REQUEST.<pr>` », « A PR the corpus cites is a `PULL_REQUEST` entity »; `c64e864` created `@PULL_REQUEST.PCST100` as told; the owner at 13:45:53Z: « est-ce qu'on va vraiment ajouter toutes les PR ? on a l'historique git pour ça »; `fa462a3` rewrote 11 archived tasks and deleted the entity; PR #95 had two review rounds on the schema (17 threads) without this question | corpus modelling |
| The schema change landed mid-wave and rippled to three open PRs, on the owner's initiative | User at 14:07:13Z: « il faudra aussi relancer les agents qui ont ouvert des PR après le merge de la 100 »; `agent-ab415d3f`, `agent-a559648d`, `agent-a84a7e10` launched 14:08:54–14:09:11Z: 47 answers, 5 min 20 s, 2,359,573 cache-read tokens, 15,440 written (`transcripts-measure.txt`; PR #101 measured by hand); nothing in `/awawa-pr-review` or `/awawa-plan-wave-start` watches a merged change of `docs/_schema.awawa` | method |
| 4 command refusals in the delivery agent's 28 answers, 3 of them after it read the rule | `timeline-06`: refused at 13:39:24Z (`git status && git branch … && git fetch … ; echo anc=$?`), 13:39:34Z (`git merge-base … ; echo anc=$?`), 13:40:11Z (`git diff --stat; node_modules/.bin/bun …`), 13:40:55Z (`mkdir -p … && cat > …`, « too complex »); `cat ~/.ai/instructions/commands.md` at 13:39:27Z (`extract.py`); the rule is delairec/.ai#34 `abde632`, merged 2026-09-17T16:09Z; one more answer lost at 13:39:36Z to a shell error `(eval):1: == not found` in a chained `awawa` command; across the wave's 4 task agents: 16 refusals in 152 answers | conduct |
| The review's claim « the schema now refuses the old form » holds for live tasks only | Probe on `trees/head` copies: `DELIVERED_BY @PULL_REQUEST.PCST90` on `@TASK.CHORE3` (`archived`) → `awawa lint --strict .` exit 0; the same on `@TASK.CHORE49` (`implemented`) → `L007 field DELIVERED_BY expects uint, given @PULL_REQUEST.PCST90`, exit 1; `SCHEMA *` `WHEN STATUS archived GATE suppressed`; 11 of the 12 tasks carrying `DELIVERED_BY` are archived. A future delivery is a live task, so the guard holds where it matters; the claim was wider than its check (REP-5) | method |
| The owner's answer to the wave's questions was lost by the CLI | `timeline-01`: `AskUserQuestion` 13:36:07Z, « The user doesn't want to proceed » 13:37:10Z, the owner at 13:38:35Z: « j'ai répondu mais ça a buggué » with the three answers retyped; 2 min 28 s and one turn | none of ours — the CLI's, for its authors |

### Remediations, by return

| # | Change | What it fixes | Recorded as |
|---|--------|---------------|-------------|
| 1 | The readiness comment of a task PR names what remains: « Review addressed, corpus updated, checks green on `<sha>`. Next: the usage report is committed on this branch; the merge comes after it. » — `~/.ai/procedures/awawa-pr-review-round.md` line 169, and the pilot's relay says the same in its first line | The merge at 14:08:00Z, before the report and before CI on `7809fbe` | `~/.ai` pull request to open (general procedure, no entity) |
| 2 | After a merge that changes `docs/_schema.awawa`, the review command lists the open task PRs whose diff touches the changed fields and proposes their rebase agents | The owner having to remember the three sibling PRs at 14:07:13Z | `~/.ai` pull request to open on `procedures/awawa-pr-review-round.md` (the rule); the launch itself is a `~/.claude/commands/awawa-pr-review.md` edit, tool-specific, no entity |
| 3 | The wave launch prompt quotes the one line of the refusal rule (« no `&&`, `;` or `$(...)` around `git`, `awawa` or `sed`; one plain command per call ») above its base-branch check, whose example the agent chained | 3 refusals after the rule was read; 16 across the wave | `~/.claude/commands/awawa-plan-wave-start.md` edit, tool-specific, no entity; the rule itself exists (`abde632`) |
| 4 | A claim about what lint refuses names the gate it holds under | « the schema now refuses the old form » | none — REP-5 already asks for the perimeter; cited here |

### Entities proposed to the owner

| Entity | Type | What it records | Ruling expected |
|--------|------|-----------------|-----------------|
| `@PULL_REQUEST.PCST100` | `PULL_REQUEST` | Pull request #100 of `@PROJECT.PCST`, recreated because this report evaluates it — the one case `@SCHEMA.PULL_REQUEST`'s `DESC` keeps since `ecaf399` | Accept as the entity the report cites |
| `@USAGE_REPORT.PR100` | `USAGE_REPORT` | This report, anchored on its first-line tag, `EVALUATES @PULL_REQUEST.PCST100` | Accept as the source entity |

No project remediation opens a task, a question, a limitation or a fact this time: the four remediations above address a general procedure, a CLI command file and an existing rule.

## Part B — The tool

### What went well

| Promise | Kept | Evidence |
|---------|------|----------|
| `diff base head` renders a schema change at field level | yes | `corpus-diff.txt`: `@SCHEMA.TASK` `~ FIELD DELIVERED_BY @PULL_REQUEST -> DELIVERED_BY uint`, `- FIELD.CONVERSE DELIVERS`, `+ FIELD.DESC.REF @PROJECT.PCST`; `@SCHEMA.PULL_REQUEST` `~ DESC`; 12 tasks `~ DELIVERED_BY`; 14 entities, 0.0 s |
| L007 refuses a payload that misses its slot type, under the entity's gate | yes | Probes on `@TASK.CHORE49` (`implemented`, `GATE error`): `abc`, `@PULL_REQUEST.PCST90`, `-5` on `DELIVERED_BY` and `abc` on `WAVE` each give one `L007 … expects uint, given …`, exit 1 |
| A `WHEN STATUS` block makes a field required at that value | yes | `@SCHEMA.TASK` `WHEN STATUS implemented … FIELD DELIVERED_BY REQUIRED`; probe removing `DELIVERED_BY 100` from `@TASK.CHORE49`: `L006 required field DELIVERED_BY is absent from TASK`, exit 1 |
| `status --where FIELD==VALUE` selects on a `uint` as written | yes | `corpus-tasks-of-pr.json`: `DELIVERED_BY==100` → `@TASK.CHORE49`, exit 0, 0.0 s; round 1 ran the same at 13:56:16Z |
| A `REF` nested under a `FIELD`'s `DESC` inside a `SCHEMA` entity is indexed | yes | `awawa refs @PROJECT.PCST .` on `trees/head`: `./docs/_schema.awawa:205:8 @SCHEMA.TASK FIELD.DESC.REF`, among 98 incoming; the owner's suggestion 4047378669 (« in @PROJECT.PCST ») kept the mention indexed |
| `refs` and `status` incoming counts make a deletion checkable | yes | `@PULL_REQUEST.PCST90`: incoming 2 at base (`SOURCE.REF`, `DELIVERED_BY`), 1 at head; 0 `PULL_REQUEST` with `incoming 0` at head |
| `lint --strict` from the root validates a new anchor | yes | `IMPL "package.json::\"packageManager\""` resolves; `corpus-lint-strict.txt` exit 0, 533 reference sites, 0 unresolved (`corpus-status.txt`) |
| `context --skip <category>` removes a provenance hub from a package | yes | `awawa context @TASK.CHORE49 --with-schema .`: 8 entities, 233 lines, footer `PULL_REQUESTS (rest, 43)` and `referenced by (rest, 53)`; with `--skip provenance`: 2 entities, 169 lines, `referenced by (rest, 1): @TASK.CHORE3` |

### What went wrong

| Defect | Observation | Checked against |
|--------|-------------|-----------------|
| `lint --closure` has no `--skip`, so the closure of a one-`SPEC` task carries its provenance hub | `corpus-closure-TASK-CHORE49.txt`: 8 entities reached through `SOURCE → @USAGE_REPORT.PR95 → @PULL_REQUEST.PCST95 → @PROJECT.PCST`, footer `PULL_REQUESTS (rest, 43)` and `referenced by (rest, 53)` — rules, sections and decisions that share nothing with `@TASK.CHORE49`; `awawa lint --closure @TASK.CHORE49 --skip provenance .` → `awawa lint: unknown flag --skip`; `awawa lint --help` lists `--strict`, `--rule`, `--closure`, `--summary`, `--json` | run |
| `status` counts an entity at the `DEFAULT` value of `STATUS` as `(none)` | `corpus-status.txt`: `DECISION (none) 106`, `PULL_REQUEST (none) 47`, `PROCESS (none) 13` while `SCHEMA *` declares `STATUS active\|archived DEFAULT active` (raised in `2026-09-17-pr-95.md`) | run |
| `fmt` wraps a string at about 95 columns into `+` lines, with no option | `git-diff-corpus.patch`, `docs/_schema.awawa` 246–248: the new `@SCHEMA.PULL_REQUEST` `DESC` written on three `+` lines (raised in `2026-09-17-pr-95.md`; no thread this time) | run |
| L008 (unreferenced entity) is deferred, so a `PULL_REQUEST` left with no incoming edge once `CONVERSE DELIVERS` is gone has no lint | Manual § lint index: « L008 (unreferenced entity) and L012 (unused declaration) are deferred »; round 1 checked by `awawa status PULL_REQUEST . \| grep 'incoming 0'`; `@SCHEMA.PULL_REQUEST` `DESC` hands the deletion to « the cleanup pass » | manual, run |

Not a defect of the binary: a stale `DELIVERED_BY @PULL_REQUEST.<pr>` on an archived task is invisible because `SCHEMA *` declares `WHEN STATUS archived GATE suppressed` — the corpus's own gate, sent back to part A (« holds for live tasks only »).

### Remediations

| Proposal | What it would have changed on this PR |
|----------|---------------------------------------|
| `--skip CATEGORY` on `lint --closure`, as on `context` | The delivery agent's closure footer lists 1 name instead of 96; the closure counts the task's own 2 entities |
| `status` reports the `DEFAULT` value as the entity's `STATUS` | `corpus-status.txt` reads `DECISION active 106` |
| A `--width N` or `--no-wrap` on `fmt` | The three-line `DESC` of `@SCHEMA.PULL_REQUEST` stays one line |
| L008 gated per type, so a type whose `DESC` says « deleted once nothing cites it » is checked by lint | Round 1's `grep 'incoming 0'` at 13:56:16Z becomes a lint line, and the next orphan is caught by the pre-commit hook |

## Recurring points

### Recurring pain points

| Point | Times seen | First raised in | Seen again here | Remediation proposed then | Done? |
|-------|------------|-----------------|-----------------|---------------------------|-------|
| Commands refused by the worktree-isolation check | 2 | `2026-09-17-pr-95.md`, part A | 4 refusals in `agent-ada9d212`'s 28 answers, 3 after `commands.md` was read at 13:39:27Z; 16 in the wave's 4 task agents (152 answers) | No computed argument and no chained `git` in a worktree-isolated session (delairec/.ai#34 `abde632`) | yes, merged 2026-09-17T16:09Z — read and not followed; remediation 3 above moves the line into the launch prompt |
| `status` counts the `DEFAULT` `STATUS` as `(none)` | 2 | `2026-09-17-pr-95.md`, part B | `corpus-status.txt`: `DECISION (none) 106` | `status` reports the `DEFAULT` value | no (tool) |
| `fmt` wraps long strings with no option | 2 | `2026-09-17-pr-95.md`, part B | `@SCHEMA.PULL_REQUEST` `DESC` on three `+` lines | `--width N` or `--no-wrap` | no (tool) |
| A live decision's `SPEC` false at the base commit, invisible to lint | 2 | `2026-09-17-pr-95.md`, part A | At base (`8cb908e`) `package.json` 5 `bun@1.3.14`, 57 `"bun": "1.4.2"`, SPEC 3 of `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped` false; this PR is its fix | `@TASK.CHORE49` | yes — this PR; closed from here on |

Raised before and not seen again, closed by observation: 93 answers in one context (the longest session here: 33 answers in `session_51c18626`); three `DECISION` entities written then struck (0 written); five undecided points in the PR body (none; `corpus-open-questions-live.txt`: 0 `OPEN_QUESTION`); two carried decisions and a fixture rule contradicted by code, and `PROCESS` without `APPLIES_TO_*` (not this PR's subject; `@TASK.DOCS50` still `draft`); the owner's hand commits introducing gaps (no commit without a trailer); a launch prompt asserting a setup that did not exist (round 1's prompt asks for checks); the pre-commit hook blocked on its PATH (no such error in the five timelines); commits without `Co-Authored-By` (0 of 2); the nested `REF` declared 8 times (L003/L023), `diff` pairing by name and `fmt --help` printing the global options — not exercised by this PR.

### Recurring strengths

| Mechanism | Times seen | First noted in | Held again here |
|-----------|------------|----------------|-----------------|
| Every commit lints clean from the repository root | 2 | `2026-09-17-pr-95.md`, part A | `awawa lint --strict .` exit 0 on the trees of `ecaf399` and `7809fbe` |
| Questions to the owner batched in one call | 2 | `2026-09-17-pr-95.md`, part A | 3 questions in one `AskUserQuestion` at 13:36:07Z |
| Work parallelised in agents with a written brief | 2 | `2026-09-17-pr-95.md`, part A | 4 task agents launched 13:39:17–13:40:16Z, each with a ~7 k-character prompt naming the task, the base check, the setup and the acceptance commands |
| A review round addresses its threads in minutes | 2 | `2026-09-17-pr-95.md`, part A | Round 1: 2 threads in 6 min 43 s; round 2: the two-commit split in 3 min 17 s |
| Incoming references checked before a deletion | 2 | `2026-09-17-pr-95.md`, part A | `awawa status PULL_REQUEST .` incoming counts before and after `fa462a3`; 0 orphans at head |
| The PR body kept describing the branch head | 2 | `2026-09-17-pr-95.md`, part A | Edited at 13:42:25Z, in round 1 and at 14:07:55Z |
| Checks green at head | 2 | `2026-09-17-pr-95.md`, part A | 9 GitHub checks pass; `bun test` 814/0, `lint:types` 0, `audit:quality` 0 on the identical tree |
| The recording threshold applied before a `DECISION` is written | 1 | this report | 0 `DECISION` in the review, the reason given in reply 4047369064 |
| `lint --strict` validates every anchor | 2 | `2026-09-17-pr-95.md`, part B | The new `IMPL "package.json::\"packageManager\""` |
| `refs` says what points at an entity | 2 | `2026-09-17-pr-95.md`, part B | `@PULL_REQUEST.PCST90` 2 → 1 incoming across the range; `@PROJECT.PCST` 98 incoming, the schema `REF` among them |
| `status --where FIELD==VALUE` finds an entity without a file read | 2 | `2026-09-17-pr-95.md`, part B | `DELIVERED_BY==100` → `@TASK.CHORE49` |
| `diff base head` gives an entity-level review | 2 | `2026-09-17-pr-95.md`, part B | 14 entities, field-level lines including `FIELD.CONVERSE` and `FIELD.DESC.REF` |
| `WHEN <field> <value>` gates a required field | 2 | `2026-09-17-pr-95.md`, part B | `WHEN STATUS implemented` requires `DELIVERED_BY` and `IMPL` (L006 probe) |

Noted before and not seen holding here: one `SCHEMA *` per walk (L005) forcing a commit sequence, `new TYPE Name` skeletons, the two deleted design files classified section by section, a held sentence turned into a general rule the same afternoon, the general findings written into `~/.ai` before the merge, the hosted `claude[bot]` review finding gaps (its job passed on `7809fbe` and posted no thread) — not exercised by this PR.

## Follow-up on the previous report's remediations

| Remediation | Done? | Evidence |
|-------------|-------|----------|
| 1 — `@TASK.DOCS50` carries the five points the migration left in the PR body | no | `awawa status TASK --where STATUS==draft .`: `@TASK.DOCS50 draft`, `tasks.awawa:504` |
| 2 — `@TASK.CHORE49` realigns `packageManager` with `devDependencies.bun` | yes | This PR: `package.json` line 5 `bun@1.4.2`, `@TASK.CHORE49 implemented`, `DELIVERED_BY 100` |
| 3 — At the fiftieth answer an interactive session asks whether to hand over | not exercised | Longest session of this PR: 33 answers; in the same wave, `agent-aa2411a6` (PR #103) stopped at its 50-step budget at 13:49:18Z and a hand-over agent finished (`timeline-01`) — PROC-4 held, outside this PR's perimeter |
| 4 — No computed argument in a worktree-isolated session | merged, not working | 4 refusals in `agent-ada9d212`, 3 after the rule was read |
| 5 — The recording threshold before a review round writes a `DECISION` | yes | 0 `DECISION` written in two rounds |
| 6 — A dated document is never rewritten (REP-6) | not exercised | No dated document touched by this PR |
| 7 — The launch prompt states the setup as a check to run | yes | Round 1 and round 2 prompts: « Do not assume the per-worktree setup … check each item »; the `input` symlink created by round 1 at 13:55:57Z |

## Appendix

### Measurements

Active time counts gaps under ten minutes only; figures from `measure.py` over each transcript (`transcripts-measure.txt`; `agent-ab415d3f` measured by hand with the same cut). Agents run inside their session's wall time, so active times add across rows but overlap in the clock. The two pilots are shared: `session_51c18626` launched four tasks, `session_d9c08253` also launched the three sibling rebases.

| Session | Active time | User turns | Tool calls | awawa calls by subcommand | Tokens read from cache | Tokens written |
|---------|-------------|------------|------------|---------------------------|------------------------|----------------|
| piloting `session_51c18626` (`/awawa-plan-wave-start`, 4 tasks, 33 answers) | 17 min 6 s | 16 | 28 | status 4, lint 2, show 2, context 1 | 3,161,528 | 25,713 |
| agent `agent-ada9d212` (CHORE49 delivery, PR opened, 28 answers) | 3 min 19 s | 4 | 42 | lint 7, fmt 5, show 4, context 1, refs 1, status 1 | 1,943,623 | 11,295 |
| piloting `session_d9c08253` (`/awawa-pr-review 100`, two rounds, three rebase launches, 30 answers, cut 14:11:30Z) | 16 min 59 s | 10 | 27 | status 7 | 1,819,074 | 19,803 |
| agent `agent-acea5d22` (review round 1, 2 threads, `fa462a3`, `ed04589`, 24 answers) | 6 min 43 s | 2 | 23 | status 6, lint 5, fmt 4, refs 3, show 1 | 1,528,731 | 12,149 |
| agent `agent-a62b89e8` (review round 2, two-commit split, 24 answers, cut 14:11:30Z) | 4 min 40 s | 3 | 24 | lint 6, fmt 3 | 1,318,622 | 9,483 |
| **This PR's five transcripts** | **48 min 47 s** | **35** | **144** | **63** | **9,771,578** | **78,443** |
| agent `agent-ab415d3f` (PR #101 rebase onto the new schema, 20 answers, cut) | 2 min 20 s | 4 | 20 | fmt 5, lint 5, show 2, refs 1 | 1,079,776 | 5,642 |
| agent `agent-a559648d` (PR #102 rebase, 13 answers, cut) | 1 min 28 s | 4 | 15 | lint 3, fmt 3, show 2, refs 1 | 611,932 | 4,816 |
| agent `agent-a84a7e10` (PR #103 rebase, 14 answers, cut) | 1 min 32 s | 4 | 15 | show 4, fmt 3, lint 3, refs 1 | 667,865 | 4,982 |
| **Caused on the sibling PRs by this PR's schema change** | **5 min 20 s** | **12** | **50** | **33** | **2,359,573** | **15,440** |

Excluded, attributed to PRs #101–#103 though listed as measured by `transcripts.md` (they share `session_51c18626`): `agent-a29933ea`, `agent-aa2411a6`, `agent-ac59e89a`, `agent-a1ccac75`, `agent-a367d94a`, `agent-a4791676`, `agent-af2e08bd` — 187 answers, 17,430,085 cache-read tokens, 147,192 written. The wall clock of the journey: agent launched 13:39:17Z, PR opened 13:41:06Z, first review comment 13:45:53Z, approval 13:59:08Z, readiness 14:01:18Z, final head pushed 14:07:04Z, merged 14:08:00Z — 28 min 43 s from launch to merge.

| Corpus lines changed | PR lines changed | Share |
|----------------------|------------------|-------|
| 37 (`docs/_schema.awawa` 11, `docs/awawa-project-methodology/tasks.awawa` 26) | 42 (`git-diff-stat.txt`: 4 files, +23/−19; `package.json` 2, `AGENTS.md` 3) | 88 % |

### Claims checked

| Claim | Check | Verdict |
|-------|-------|---------|
| Body: `packageManager` `bun@1.3.14` → `bun@1.4.2`, `devDependencies.bun` 1.4.2 unchanged, `engines.bun` `>=1.3.14` unchanged | `package.json` at head lines 5, 57, 64; `git-diff-outside-corpus.patch` touches line 5 only | confirmed |
| Body: every workflow reads `bun-version-file: package.json`, so CI moves to Bun 1.4.2 | `grep setup-bun .github/workflows/*.yml`: 6 steps in 3 files, each `bun-version-file: package.json` | confirmed |
| Body: `oven-sh/setup-bun` reads `packageManager` first | Not read from the action's documentation; the same sentence stands in the `RATIONALE` of `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped`, which is a corpus statement, not a check | unchecked |
| Body: `bun.lock` is unaffected, it records `devDependencies.bun` | `bun.lock` line 9 `"bun": "1.4.2"`, no `packageManager` key; the PR diff touches no `bun.lock` | confirmed |
| Body: `@TASK.CHORE49` promoted to `implemented` with `DELIVERED_BY 100`, SPEC anchored on `package.json::"packageManager"` | `corpus-show-TASK-CHORE49.txt` | confirmed |
| Body: the third SPEC of `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped` holds again; no decision rewritten | SPEC 3 text against `package.json` 5 and 57 at head; `corpus-diff.txt` lists no `DECISION` | confirmed |
| Body: `DELIVERED_BY` is a `uint`; the eleven earlier tasks carry their numbers | `corpus-diff.txt`: `@SCHEMA.TASK` `FIELD DELIVERED_BY uint`; 11 tasks `~ DELIVERED_BY @PULL_REQUEST.PCSTnn -> nn` plus `@TASK.CHORE49` | confirmed |
| Body and comment: each commit passes `awawa lint --strict .` alone | Exit 0 on the extracted trees of `ecaf399` (= `1f99ea1`) and `7809fbe`; `fmt --check` exit 0 on both | confirmed |
| Comment 14:08:00Z: the tree of `7809fbe` is byte-identical to `ed04589` | `git rev-parse` `ef68597…` for both, and for `6a1b54d` | confirmed |
| Body: `bun test` 814 pass, 0 fail under the global Bun 1.3.14 and under `node_modules/.bin/bun` 1.4.2 | This worktree, identical tree, global `bun --version` 1.3.14: 814 pass, 0 fail, 114 files; the 1.4.2 run not repeated here | confirmed for 1.3.14, unchecked for 1.4.2 |
| Reply 4047369064: `@PULL_REQUEST.PCST100` was the only pull request whose sole incoming edge was `DELIVERS`; the eleven others stay cited as sources | Head: 47 `PULL_REQUEST`, 0 with `incoming 0`; `@PULL_REQUEST.PCST90` keeps its `SOURCE.REF` | confirmed |
| Reply 4047369064: `awawa status TASK --where DELIVERED_BY==100 .` returns `@TASK.CHORE49` | `corpus-tasks-of-pr.json`, exit 0 | confirmed |
| Reply 4047369064: the nested `REF @PROJECT.PCST` is indexed | `awawa refs @PROJECT.PCST .`: `_schema.awawa:205:8 @SCHEMA.TASK FIELD.DESC.REF` | confirmed |
| Reply 4047369064 and body: « the schema now refuses the old form, so a future PR cannot do the opposite by mistake » | L007 fires on `draft`, `todo`, `implemented` (`GATE error`); silent on `archived` (`GATE suppressed`), where 11 of the 12 carriers sit | overstated |
| Round 1 hand-back: CI green on `ed04589` | `gh run list --commit ed04589` returns nothing after the force-push | unchecked |
| Round 2 comment (edited 14:15:01Z): CI green on `7809fbe` | `gh run list --commit 7809fbe`: 8 runs `success`, last completed 14:11:22Z — after the merge at 14:08:00Z | confirmed, the merge preceding it |
| Wave hand-back: `gh pr view 100` returns `MERGEABLE` | State is `MERGED` now | unchecked |
| Previous report: SPEC 3 of `@DECISION.NodeFollowsReleasesWhileBunIsPinnedAndBumped` false at the base, both anchors resolving | `trees/base/package.json` 5 `bun@1.3.14`, 57 `"bun": "1.4.2"`; base lint exit 0 in the collection | confirmed |
