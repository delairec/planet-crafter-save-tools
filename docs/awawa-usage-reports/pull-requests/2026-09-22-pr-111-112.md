<!-- awawa-usage-report -->

# 2026-09-22 — PR #111, #112 — wave 5: `logisticsPaused` and the Skeo planet id

One report for two pull requests, on the owner's instruction (« 111 et 112 en un seul rapport »). The launching session chose, and this report states as its own choice rather than a ruling: the file is committed on the head of PR #112 (`feat/65-skeo-planet-id`); PR #111 (`feat/55-logistics-paused`) was read in its own worktree and nothing was written there; one `USAGE_REPORT` entity carries both numbers, `EVALUATES` being `REPEATABLE` in `@SCHEMA.USAGE_REPORT`; the PR comment of the procedure goes on #112 and a one-line pointer on #111. PR #111 is measured at its head `8f99053`, PR #112 at `11df2fc`, both on the base `1c4ca7b` (`range.txt` of each collection). The two journeys share their sessions: one wave pilot launched both tasks, one review pilot launched the first round of each, and the collection found the same 14 transcripts for both.

| Field                                          | Value |
|------------------------------------------------|-------|
| **Author of this report**                      | `Claude Fable 5.1` (`claude-fable-5-1`), effort `high` (from `~/.claude/agents/awawa-usage-reporter.md`) |
| **Models that drove the sessions under review** | `Claude Opus 5` (`claude-opus-5`), effort `medium` — the wave pilot, the FEAT55 agent, the two review pilots, the six review rounds and the two `~/.ai` agents: `"effort":"medium"` on every request record of those 12 transcripts (62, 96, 42, 30, 60, 61, 43, 27, 35, 60, 18 and 20 of as many records; this report's extraction); 13 of 13 commit trailers say `Claude Opus 5`<br>`Claude Sonnet 5` (`claude-sonnet-5`), effort `high` — the FEAT65 agent `agent-a15b0540` (161 of 161 records) and the GREEN sub-agent of FEAT55 `agent-af13e5b0` (44 of 44); **the two FEAT65 commits `ffe91cc` and `929a609` are signed `Claude Opus 5`**, the lines the wave launch prompt carried (part A); the model here is read from the transcripts, not from the trailers |
| **Sessions measured**                          | Commit trailers `session_01DK2j133RF8ewzakFLzFzeF` (the 4 commits of 2026-09-21) and `session_01LxvdRrAUzs9d9PUmT4Hyqs` (the 9 commits of 2026-09-22)<br>`2b54c14f` (wave pilot, `/awawa-plan-wave-start FEAT55 et FEAT65 …`, 23:05–23:33Z; it holds all 93 records of `88777eca`, which the collection listed and measured as a second session — counted once here) and its agents `agent-a4fdb792` (FEAT55, Opus), `agent-af13e5b0` (GREEN phase of FEAT55, Sonnet, measured by hand with `measure.py`), `agent-a15b0540` (FEAT65, Sonnet)<br>`6c2f5774` (review pilot, `/awawa-pr-review 111 et 112 (lance un sous-agent par review)`, 09:50–10:57Z) and its agents `agent-a1cc563a` (#111 round 1), `agent-acf65a95` (#112 round 1), `agent-aebc78ed` (#111 round 2), `agent-a6595522` (#111 round 3), `agent-aebbd02e` (`~/.ai` PR #42)<br>`304d5724` (review pilot after the owner's `/clear`, `/awawa-pr-review 112 …`, 10:58–11:13Z) and its agents `agent-a49459c2` (#112 round 2), `agent-a6c1ceac` (#112 round 3), `agent-aa4cac17` (`~/.ai` PR #43)<br>Excluded: `36ae04d6`, the session that launched this report, and this report's own run |
| **Delivered**                                  | #111 `@TASK.FEAT55`: the section 0 schema and `GlobalMetadata` accept an optional boolean `logisticsPaused`, `mergeGlobalMetadata` keeps save A's value, else save B's, else writes no key; `docs/save-format.md` row; `@RULE.TheGlobalMetadataEntryMayOmitLogisticsPaused` applied by `@COMMAND.MergeSaves` and `@COMMAND.ValidateSave`; a `SPEC` and a `SOURCE` on `@RULE.GlobalMetadataIsSummedAndUnioned`; from the review, the draft tasks `@TASK.FEAT84` (display the field) and `@TASK.DOCS86` (type `HOLDS_FOR` by game release)<br>#112 `@TASK.FEAT65`: the Skeo row `-440810600` in `planetNamesByNumericId.json`, an `ATTESTED_BY` on `@RULE.APlanetNumericIdIsStableAcrossSaves`; from the review, `SaveSectionsReaderService.spec.ts` rewritten three times, `planetNamesByNumericId.spec.ts` deleted, `@TASK.TEST85` written then removed; as by-products, two general testing rules merged in `~/.ai` (#42 TEST-5, #43 TEST-3) |

## Verdict in two lines

**Ours**: two tasks left one wave to an open pull request each in 4 min 37 s and 4 min 53 s, on a model chosen per task, both green at head (841 and 836 tests, 8 CI jobs each, `lint:types` and `audit:quality` exit 0), the corpus promoted by the delivering agent with 0 `DECISION` in 13 commits, six review rounds of 3 min 44 s to 6 min 30 s answering 5 threads, a challenge withdrawn on the owner's ruling into `@TASK.DOCS86`, and two general rules sent to `~/.ai` by pull request and merged within 3 min and 1 min; but the FEAT65 agent shipped a test of the data table that its own pilot had flagged at 23:17:04Z and nobody posted, which cost three review rounds, three rewrites of one spec file and a draft task the owner struck (6.0 M cache-read tokens in the three rounds), the round-1 rebase of #111 was denied by the auto-mode classifier (4 denials in 2 rounds, the owner asking why), commit `26eddcf` is red under `lint --strict` (L016) because its corpus fix came one commit later and the hook lints the working tree, the two Sonnet commits are signed `Claude Opus 5`, the second review pilot told the owner a merged `~/.ai` pull request was « pas encore sur main » on the strength of `gh pr list`, `docs/save-format.md:222` and `agents/corpus-ecrire.md:54` contradict the corpus at both heads, the isolation check refused 7 commands (fifth report), both wave agents passed fifty answers without handing over (fourth), and `echo =====` lost 7 answers (third, flagged).

**The tool's**: awawa 2.7.0 gave the review its facts and its gates — `status --where HOLDS_FOR==both` on `RULE` and `SECTION` (9 + 11) is where the 34 entities of the challenge came from, L025 refused a prose mention in `@TASK.DOCS86` before it was committed (10:44:55Z), L016 is what makes `26eddcf` red (`tasks.awawa:398:8`), `refs @TASK.TEST85` (0 incoming) preceded its deletion, `diff` rendered 7 and 2 changed entities at field level in 0.0 s, and 184 `awawa` calls read the corpus in 9 working transcripts — and still costs four things: `lint --closure` has no `--skip`, so `@TASK.FEAT55`'s footer lists 106 unrelated names (fourth report), `status` counts 239 of 277 entities at their `DEFAULT` `STATUS` as `(none)` (fifth), `fmt` wrapped 51 of the 122 corpus lines #111 added (fifth), and no command searches field text, so the round that renamed a test fell back to `git grep` to find who cited it (11:10–11:13Z), as this report did for `EN-PLANET`.

## Part A — Us: method, corpus, conduct

### What went well

| What | Evidence |
|------|----------|
| Two tasks delivered in parallel from one wave, each to an open pull request within five minutes of its launch | `timeline-01`: `Agent` calls at 23:06:47Z (FEAT55) and 23:07:06Z (FEAT65); `pr.json`: #111 created 23:11:24Z (4 min 37 s), #112 created 23:11:59Z (4 min 53 s); hand-backs 23:12:17Z and 23:16:43Z; 51 and 72 answers (`transcripts-measure.txt`) |
| The model was chosen per task, as the owner asked | `timeline-01` args « avec modèle/effort adaptés à la tâche »; `Agent` inputs `model=opus` for FEAT55 (3 `SPEC`, a merge rule) and `model=sonnet` for FEAT65 (one table row); progress line 23:07:09Z « feat-55 (opus) and feat-65 (sonnet) launched »; the Sonnet run cost 8.66 M cache-read tokens for 72 answers, the Opus run 4.43 M for 51 |
| FEAT55 was built test-first with a GREEN sub-agent forbidden to touch the specs | `timeline-14`: `Agent` at 23:09:10Z « Do NOT edit any *.spec.* file, do NOT commit »; hand-back « RED: three new tests failed on their assertion (`undefined`), not on a type error »; `agent-af13e5b0`: 23 answers, 1 min 43 s, 1.11 M cache-read (this report's `measure.py` run); `bun test` 841 pass at head (`checks-bun-test.txt`) |
| The Skeo id was recomputed before the row was written, and the claim reproduces | FEAT65 hand-back: the six values; this report's Python djb2 over UTF-16 code units: Prime −1140328421, Selenea −1016990411, Aqualis −1291310150, Humble −486276833, Toxicity 110910045, Skeo −440810600 — 6 of 6 equal to `planetNamesByNumericId.json` at head |
| Each task was promoted in its own pull request by the delivering agent, with anchors that resolve | `corpus-tasks-of-pr.json` of each collection: `@TASK.FEAT55` `implemented`, `DELIVERED_BY 111`, 8 `IMPL`; `@TASK.FEAT65` `implemented`, `DELIVERED_BY 112`, 1 `IMPL`; `corpus-lint-strict.txt` exit 0 at both heads; `corpus-status.txt`: 436 reference sites, 0 unresolved |
| The new format rule arrived with the commands that apply it, its source pinned, and no `DECISION` | `corpus-diff.txt` of #111: `@RULE.TheGlobalMetadataEntryMayOmitLogisticsPaused` added, `+ APPLIES` on `@COMMAND.MergeSaves` and `@COMMAND.ValidateSave`, `+ SOURCE` with `PR 111` and `REF @GAME_RELEASE.2.102` on `@RULE.GlobalMetadataIsSummedAndUnioned`; 0 `DECISION`, 0 `OPEN_QUESTION` in the 7 + 2 entities of the two diffs |
| The review challenge rested on counted facts, every one of which this report reproduces | Reply `r4070404481`: « 34 entities: 20 both, 13 current, 1 legacy (22 rules, 12 sections) », « 4 releases », the `SPEC` and `REJECTED` of `@DECISION.AGameReleaseIsCitedAsASource` quoted; this report on `trees/head`: `RULE` both 9 / current 13 / legacy 0, `SECTION` both 11 / current 0 / legacy 1; `awawa status GAME_RELEASE .` 4; `awawa show` of the decision: same text |
| The challenge was withdrawn on the owner's ruling and deferred to a draft task that names its conflict | Owner at 10:42:38Z (`timeline-03`); round 2 wrote `@TASK.DOCS86` (`76c2b94`, 10:45:20+02:00) with `REF @DECISION.AGameReleaseIsCitedAsASource`, `REF @DECISION.TheEraIsNotTheArchive`, `REF @FIELDSET.Era`, two `SOURCE` with `PR 111`; round 3 restated it on the clarification of 10:53:24Z in `8f99053` at 10:55:06+02:00, 1 min 42 s after its launch; its `SPEC` on `INDEX`, `LEGACY_INDEX` and `INCOMING CONSTRAINED_BY` matches `@SCHEMA.SECTION` (this report: `WHEN HOLDS_FOR current` and `both` carry both) |
| The UI gap the owner named became a task that says which existing tasks do not cover it | Owner 08:53:12Z « On a une tâche prévue pour le faire ? »; `@TASK.FEAT84` draft, `AFTER @TASK.FEAT55`, `RATIONALE` naming `@TASK.TEST67` and `@TASK.FEAT72` as not covering it, `SOURCE` `PR 111` (`corpus-entities-of-diff.txt` of #111) |
| Task numbers were coordinated between the two parallel rounds by message, and this report read them across both branches | `timeline-08` 09:55:45Z `SendMessage` « take 85 or above »; `timeline-10` 09:55:56Z `sed 's/^TASK TEST84$/TASK TEST85/'`, 09:55:59Z reply; round 2 of #111 took 86 « 85 is `@TASK.TEST85` in d1103d5 »; this report: highest number 83 on `feat/65-skeo-planet-id`, 86 on `feat/55-logistics-paused`, so its task is `DOCS87` |
| General rules went to `~/.ai` by pull request, never into the corpus, and were merged within minutes | `ASK` 11:07:21Z « où l'enregistrer ? » answered « Générale (~/.ai) » in 27 s; `gh pr view` on `delairec/.ai`: #42 created 10:54:45Z, merged 10:57:58Z (agent `aebbd02e`, 54 s, 11 answers); #43 created 11:09:00Z, merged 11:10:09Z (agent `aa4cac17`, 59 s, 11 answers); `instructions/testing/index.md` on `origin/main` carries both lines (19 and 30); 0 `DECISION` for either |
| Every review round was a fresh agent, and the owner cleared the pilot between rounds | 6 rounds of 3 min 44 s to 6 min 30 s active, 16 to 34 answers each; `/clear` at 10:58:21Z after 23 answers and 1.45 M cache-read of `6c2f5774`; `304d5724` then ran 17 answers |
| The per-worktree setup was checked by each round, not assumed | Hand-backs: #112 round 1 « `bun install --frozen-lockfile` reported no changes », #111 round 2 « made no changes », #112 round 3 « I created the missing `input` symlink and ran `bun install --frozen-lockfile` » |
| The PR body of #112 was rewritten at each round | `gh pr edit` at 23:14:08Z (agent), body files written 09:56:26Z, 11:02:00Z, 11:11:58Z (`timeline-10`, `-05`, `-06`); `pr-rendered.md` describes `11df2fc` |
| Checks green at both heads, worktrees clean before and after | `gh pr checks`: 8 jobs pass on each; `checks-bun-test.txt` 841 / 836 pass, 0 fail; `checks-lint-types.txt` 7 packages exit 0; `checks-audit-quality.txt` exit 0; `checks-status-before.txt` and `-after.txt` empty; `gh pr view`: both `MERGEABLE` `CLEAN` |

### What went wrong

| What | Evidence | Whose fault |
|------|----------|-------------|
| FEAT65 shipped a test of the data table, its pilot saw it and told nobody who could act | `ffe91cc` adds `planetNamesByNumericId.spec.ts` (20 lines) reading one JSON row through a world event no production code consumes (reply `r4070402713`); the pilot's final text 23:17:04Z lists it first under « Things for the review of #112 », with two other items, and posts none of the three on the PR nor back to the agent; the owner finds it at 08:42:03Z « Pas sûre que ce fichier de test soit utile »; cost: three rounds on #112 (6 min 30 s + 3 min 50 s + 5 min 07 s, 5.71 M cache-read), `SaveSectionsReaderService.spec.ts` rewritten three times (`86087e7` six-row `it.each`, `f33a952` two rows, `26eddcf` one case), `@TASK.TEST85` written (`d1103d5`) and struck (`1b319e5`, « retire cette tâche ») | conduct (pilot), method (`awawa-plan-wave-start.md` § 7 has no step for what the pilot's own verification finds) |
| Round 1 of #112 obeyed the letter of the comment and copied the table into the test, then the unknown-id row changed position three times | Owner 08:45:27Z « Un test paramétré serait plus approprié que de faire du mapping »; `86087e7`: `it.each` over the six rows of `planetNamesByNumericId.json`; owner 10:59:43Z « on est en train de recopier une table qui devrait plutôt être la source unique de vérité »; the unknown id dropped (reply `r4070402826`), restored (`r4070892583`), dropped again (`r4070960791`) in 1 h 15 min | conduct |
| Two of the pilot's three flagged items survive at both heads | `docs/save-format.md:222` « No known deterministic hash function … reproduces these IDs » while `@RULE.APlanetNumericIdIsStableAcrossSaves` `RATIONALE` says djb2 since PR #107 (6 of 6 above); the test title `(Rule EN-PLANET-3)` kept and now copied into `@TASK.FEAT65`'s `RATIONALE`, an identifier defined nowhere (`docs/energy-levels.md` defines EN-PLANET-1 at line 199 and EN-PLANET-2 at 207; `grep EN-PLANET-3 docs/` finds no definition) | conduct |
| `agents/corpus-ecrire.md` names a model task the cleanup pass deleted | Line 54 « `@TASK.FIX45` (tâche) »; `awawa show @TASK.FIX45 .`: « not defined in the workspace »; deleted by `1a23069` on `master` (the base of both PRs); `@DECISION.AgentsMdNamesOneModelEntityPerType` `SPEC` « each existing in the corpus »; the other 7 model entities exist; L016 cannot see it, an entity name in a markdown sentence being no anchor | corpus modelling |
| One commit of #112 is red under `lint --strict`, and the hook let it through | `26eddcf` (spec only, 28+/21−): L016 `tasks.awawa:398:8` « `should resolve the planet name of $numericIdCase (Rule EN-PLANET-3)` does not occur in … `SaveSectionsReaderService.spec.ts` » (this report on its extracted tree); the fix is `11df2fc`, same second (13:11:16+02:00), both commits from one command at 11:11:15Z (`timeline-06`); `.git/hooks/pre-commit:21–31` lints `git ls-files -- '*.awawa'`, the working tree, which already held the fix; 12 of 13 commit trees lint clean | conduct (a rename and the anchor that cites it belong to one commit); method (the `~/.ai` hook template lints the tree, not the index) |
| The two FEAT65 commits misname their model | `git-log.txt` of #112: `ffe91cc`, `929a609` « Claude Opus 5 »; `agent-a15b0540`: `claude-sonnet-5` on 161 of 161 assistant records, its own system reminder « End git commit messages with: Co-Authored-By: Claude Sonnet 5 »; the wave pilot's `Agent` input for FEAT65 carries « Co-Authored-By: Claude Opus 5 » (23:07:06Z) and the agent copied it into both commit commands (23:11:40Z, 23:13:47Z); `awawa-plan-wave-start.md:149` « Follow this session's attribution rules » | method |
| The rebase of #111 was denied by the auto-mode classifier, and a round ended behind its base | `timeline-08`: 09:52:46Z a compound command writing the progress file « denied … Reason: [Git Destructive] », 09:53:23Z `git -C … rebase origin/master` « [Git Destructive] »; `timeline-10`: 09:53:08Z progress-file write « [Modify Shared Resources] », 09:53:24Z backup branch and progress file « [Git Destructive] »; round 1 of #111 posted no readiness comment, 3 commits behind; owner 10:42:38Z « je ne comprends pas pourquoi le rebase est refusé »; round 2 ran the same rebase at 10:44Z undenied (`687a21a` → `76c2b94`) | method (no allow-list for the rebase and the progress file in a review round) |
| A false fact about `~/.ai` stated to the owner, from a command that could not show it | `304d5724` 11:01:08Z `gh pr list --head docs/test-static-data --json number,title,state` — open pull requests only —, then 11:01:14Z « la règle existe, mais elle n'est pas encore sur `main` »; #42 was merged at 10:57:58Z; the owner corrected it at 11:01:00Z and 11:07:08Z « je te dis que c'est déjà mergé dans .ai, et ne revérifie pas » | conduct (`~/.claude/CLAUDE.md`: a fact about a repository is proved by the command that proves it) |
| Two `Read` calls on corpus files in the FEAT65 agent | `agent-a15b0540` `Read` inputs: `docs/awawa-project-specification/rules.awawa`, `docs/awawa-project-methodology/tasks.awawa` (this report's extraction, 17 `Read` in all); CORPUS-3 and CORPUS-4 name `awawa show`; the FEAT55 agent read 0 | conduct |
| 7 commands refused by the worktree-isolation check, all in the two wave agents | `agent-a4fdb792`: 23:07:12Z and 23:07:15Z (the base check chained with `git status` and `git fetch`), 23:10:01Z (`until git -C … diff --quiet` loop), 23:10:31Z (`bun test …; awawa fmt .; git diff --stat`), 23:10:49Z (« feeds bun a program assembled at runtime »), 23:11:46Z (`awawa lint … ; git diff --stat`); `agent-a15b0540` 23:08:24Z; the 6 review rounds ran in existing worktrees without isolation: 0 refusals | conduct |
| 7 answers lost to the zsh separator `echo =====` | Tool results carrying `(eval): ===== not found`: wave pilot 23:05:49Z (the first `awawa context` of the wave), `agent-a4fdb792` 23:07:31Z, `agent-aebc78ed` 10:43:41Z, `agent-aebbd02e` 10:54:03Z, `agent-a6595522` 10:54:10Z, `agent-aa4cac17` 11:08:18Z and 11:08:21Z (this report's extraction); `commands.md` still carries no separator line (`grep -c 'echo ===' ~/.ai/instructions/commands.md`: 0) | conduct |
| Both wave agents passed fifty answers without handing over | `agent-a15b0540`: 72 answers, the 50th at 23:13:22Z, hand-back 23:16:43Z; `agent-a4fdb792`: 51, the 50th at 23:12:04Z, hand-back 23:12:17Z; both launch prompts open on « At fifty steps without being done, hand over »; the pilot's final text: « both agents went past the 50-step point … Neither handed over » | conduct |
| The PR body of #111 does not name the two tasks its review added | `awk '/^## Body/,/^## Commits/'` on `pr-rendered.md` of #111: 0 occurrence of `FEAT84` or `DOCS86`; the body was written at 23:11:22Z and never edited; #112's was rewritten three times | conduct |
| A reprojection TEST-5 and TEST-6 forbid remains, and nothing records it since the owner struck the task that did | `EnergyLevelsPresenter.spec.ts:93` `expect(presenter.viewModel.planets.map((planet) => planet.planetId))` at both heads; `@TASK.TEST85` (`d1103d5`) named it and the `check:assertions` gap (« catches the boolean forms only »); removed in `1b319e5` at the owner's request; TEST-5 merged in `~/.ai` at 10:57:58Z. This report opens nothing for it: the removal is the owner's ruling of 10:56:14Z, and the point is stated here for them to rule on | — (stated, not judged) |
| The instrument counted one session twice and still mis-cuts two files | `transcripts.md` lists `88777eca` (18 answers, 1.55 M cache-read) and `2b54c14f` (36, 3.69 M) as two measured sessions; `jq -r .uuid` on both: 93 of the 93 records of the first are in the second (a resumed session); `git-diff-outside-corpus.patch` of #111 opens on `docs/awawa-project-methodology/tasks.awawa` (`collect.py:235` `:(exclude){corpus}` → `:(exclude):(glob)**/*.awawa`); `previous-reports.md` « 1 of 3 reports » for a folder of 4 files (`REPORT_NAME`, `collect.py:534`, `-pr-(\d+)\.md$`) — remediation 6 of `2026-09-21-pr-107.md` not done | report |

### Remediations, by return

| # | Change | What it fixes | Recorded as |
|---|--------|---------------|-------------|
| 1 | The wave command's § 7: every item the pilot's verification flags « for the review » is posted on the pull request as a review comment, or sent back to the agent, before the hand-back is accepted; a hand-back with an unposted finding is not accepted | The table test found by the owner 9 h 25 min after the pilot saw it; the two items still at head | `~/.claude/commands/awawa-plan-wave-start.md` edit, tool-specific, no entity |
| 2 | Realign the two documents the corpus contradicts at head: `docs/save-format.md:222` restated on the djb2 hash, `agents/corpus-ecrire.md:54` naming a live model task, and a guard refusing an entity name that `AGENTS.md` or `agents/*.md` cites and the corpus does not define | The stale hash sentence since #107; the purged `@TASK.FIX45`; the decision's `SPEC` « each existing in the corpus » that no check enforces | `@TASK.DOCS87` (draft, this report) |
| 3 | The wave launch prompt carries no attribution lines: the agent signs with the lines its own session carries, as `awawa-usage-report.md` § 0 already says | Two commits signed by a model that did not write them | `~/.claude/commands/awawa-plan-wave-start.md` edit, tool-specific, no entity |
| 4 | The `~/.ai` pre-commit hook template lints the index, not the working tree (a temporary checkout of the index, `git checkout-index --temp` or `git archive $(git write-tree)`), and the review round procedure states that a renamed anchored test and the anchor that cites it land in one commit | `26eddcf` red under L016 with a green hook | `~/.ai` pull request (`hooks/pre-commit`, `procedures/awawa-pr-review-round.md`), no entity |
| 5 | The review rounds' permissions allow `git rebase origin/<base>`, `git branch backup-*`, `git push --force-with-lease` and the progress-file write in a linked worktree, or the pilot runs the rebase before launching round 1 | 4 classifier denials, one round ending 3 commits behind its base, one owner question | `~/.claude` settings and `awawa-pr-review.md` § 0, tool-specific, no entity |
| 6 | `commands.md` § GitHub: the state of a pull request is `gh pr view <n> --json state,mergedAt`, never inferred from `gh pr list`, which hides merged and closed pull requests | « pas encore sur main » on a pull request merged 3 min earlier | `~/.ai` pull request, no entity |
| 7 | `collect.py`: a transcript whose records include every record of another measured transcript replaces it (same `uuid` set, resumed session); `REPORT_NAME` admits `-pr-<N>(-<N>)*.md`; the outside-corpus diff uses `:(exclude,glob)**/*.awawa` | 18 answers and 1.55 M counted twice; the two files of remediation 6 of `pr-107` | `~/.claude/tools/awawa-usage-report/collect.py` edit, tool-specific, no entity |
| 8 | CORPUS-4 gets the row « search a string across field values » → the `awawa` command of part B once it exists, `git grep -n '<text>' -- '*.awawa'` named as the exception until then | The round-3 fallback and this report's two greps | `~/.ai` pull request (`instructions/corpus.md`), no entity |
| 9 | The refusal line above the git steps of the three command prompts and the separator line in `commands.md` — remediation 4 of `pr-107`, still 0 of 3 files and 0 lines | 7 refusals, 7 separator losses | none new — carried, counted below |
| 10 | The PR body reread against the log before the readiness comment — the rule exists (`commands.md` « the body of a pull request describes the branch head ») | #111's body silent on `@TASK.FEAT84` and `@TASK.DOCS86` | none new — cited |

### Entities proposed to the owner

| Entity | Type | What it records | Ruling expected |
|--------|------|-----------------|-----------------|
| `@TASK.DOCS87` | `TASK`, `draft` | Remediation 2: `docs/save-format.md` restated on the hash, `agents/corpus-ecrire.md` naming a live model task, a guard on entity names cited by the agent instructions | Ratify (`todo`), give it a wave, or strike it |
| `@USAGE_REPORT.PR111_112` | `USAGE_REPORT` | This report, anchored on its first-line tag, `EVALUATES 111` and `EVALUATES 112` | Accept as the source entity |

## Part B — The tool

### What went well

| Promise | Kept | Evidence |
|---------|------|----------|
| `status --where FIELD==VALUE` answers a scope question the review needed | yes | Round 1 of #111 cited 34 entities carrying `HOLDS_FOR`; this report: `awawa status RULE --where HOLDS_FOR==both .` 9 of 22, `current` 13, `legacy` 0; `SECTION` 11, 0, 1 — 20 / 13 / 1, the challenge's numbers; `--where DELIVERED_BY==111` → `@TASK.FEAT55` (`corpus-tasks-of-pr.json`) |
| L025 refuses a prose mention that no field indexes | yes | `timeline-12` 10:44:55Z, writing `@TASK.DOCS86`: « L025 … `@DECISION.AGameReleaseIsCitedAsASource` is mentioned in prose and indexed by nothing; nest a field », exit 1; the committed entity carries the `REF` (`corpus-entities-of-diff.txt`) |
| L016 refuses an anchor whose fragment left the file | yes | `awawa lint --strict .` on the tree of `26eddcf`: 1 error, `tasks.awawa:398:8` « does not occur in ./packages/core-mapping/src/infrastructure/SaveSectionsReaderService.spec.ts »; exit 0 at `11df2fc` |
| `refs` says what points at an entity before it is deleted | yes | `awawa refs @TASK.TEST85 .` on the tree of `d1103d5`: `incoming (0)` (this report); reply `r4070892428` « nothing referenced it (0 incoming) »; `refs @TASK.FEAT55 .` at head: 5 incoming across `TEST67`, `FEAT84`, `DOCS86` |
| `diff base head` renders a delivery at field level | yes | `corpus-diff.txt` of #111: 3 added, 4 changed — `STATUS todo -> implemented`, 8 `+ SPEC.IMPL`, `~ RESOLUTION`, `+ SPEC`, 3 `+ SPEC.ATTESTED_BY`, `+ SOURCE.PR 111`, 2 `+ APPLIES`; #112: 2 changed, `+ SPEC.ATTESTED_BY`, `~ RATIONALE`; 0.0 s each (`index.md`) |
| `new TYPE Name` skeletons | yes | 4 calls (`agent-a1cc563a`, `agent-acf65a95`, `agent-aebc78ed`, `agent-a4fdb792`, `transcripts-measure.txt`); the 3 tasks and the rule written after them lint clean at head |
| `lint --strict` and `fmt --check` exit 0 at every commit that changed the corpus | yes | 12 of 13 extracted trees exit 0 on both (this report); the one exit 1 is a commit that changed no corpus file |
| The corpus was read through the tool | mostly | 184 `awawa` calls in 9 working transcripts (`transcripts-measure.txt`: wave pilot 23, FEAT55 34, FEAT65 16, #111 rounds 29 + 21 + 15, #112 rounds 15 + 14 + 17); 2 `Read` of a `.awawa` file (part A) |
| Every `awawa` command runs in 0.0 s on a 277-entity corpus | yes | `index.md` of both collections: `status`, `diff`, `lint --strict`, `fmt --check`, the `show` and `refs` of 9 entities, all 0.0 s |

### What went wrong

| Defect | Observation | Checked against |
|--------|-------------|-----------------|
| `lint --closure` has no `--skip`, so a task's closure carries the provenance hub | `corpus-closure-TASK-FEAT55.txt`: 40 entities (15 `URL`, 4 `DECISION`, 4 `GAME_RELEASE` …) and a footer of 106 names — `GOVERNED_BY (rest, 52)`, `SOURCE_OF (rest, 17)`, `LIMITED_BY (rest, 8)`, `CONSTRAINED_BY (rest, 6)`, `referenced by` 2 + 19, `APPLIED_BY` 1, `BLOCKED_BY` 1; `@TASK.FEAT65`: 9 entities, 27 names; down from 185 at `pr-107` since `@TASK.CHORE57` landed, the same defect | run |
| `status` counts an entity at the `DEFAULT` value of `STATUS` as `(none)` | `corpus-status.txt`: `(none)` 239 of 277 — `DECISION (none) 107`, `RULE (none) 22`, `URL (none) 15` — while `SCHEMA *` declares `STATUS active\|archived DEFAULT active` (`awawa show '@SCHEMA.*' .`) | run |
| `fmt` wraps strings at about 95 columns with no option | `git-diff-corpus.patch` of #111: 51 of the 122 added lines are `+ "` continuations; #112: 3 of 7 | run |
| No command searches field text | Round 3 of #112, hand-back 11:13:13Z: « awawa has no command that searches field values, so I checked with `git grep` instead; that gap in the CORPUS-4 table is worth noting »; its attempt at 11:10:25Z `for f in $(git ls-files 'docs/*.awawa'); do awawa show $f …` exit 1; this report grepped `EN-PLANET` and the task numbers of the remote branches the same way; `awawa --help` lists no `grep`, `search` or `find` | run, transcript |

Sent back to part A: the closure footer of 106 names comes through our own `SOURCE.REF` on `@PROJECT.DNC` and the `GAME_RELEASE` entities; the model task the instructions name is our purge; the binary did what the schema says.

### Remediations

| Proposal | What it would have changed on this PR |
|----------|---------------------------------------|
| `--skip CATEGORY` on `lint --closure`, as on `context` | `@TASK.FEAT55`'s closure lists its 5 tasks, 1 question, 2 rules and 3 tables, and a footer of 4 names instead of 106 |
| `status` reports the `DEFAULT` value as the entity's `STATUS` | `corpus-status.txt` reads `DECISION active 107` |
| `--width N` or `--no-wrap` on `fmt`, or the width in `fmt --help` | 51 continuation lines read as 51 fewer diff lines |
| `awawa search '<text>' [--field FIELD] [--json] <root>` printing entity, field and `file:line` of every value holding the text | Round 3 finds the one `IMPL` citing the renamed title in one call instead of a `git grep`; this report reads `EN-PLANET` through the tool |

## Recurring points

### Recurring pain points

| Point | Times seen | First raised in | Seen again here | Remediation proposed then | Done? |
|-------|------------|-----------------|-----------------|---------------------------|-------|
| Commands refused by the worktree-isolation check — **flagged, 5 reports** | 5 | `2026-09-17-pr-95.md`, part A | 7 refusals in the two wave agents (6 in 51 answers, 1 in 72); 0 in the six review rounds launched without isolation | The rule in `commands.md`, the launch prompt quoting it (`pr-100`), un-isolated review launches (`pr-101-102-103`), the line above the base check of the three commands (`pr-107`) | partly — un-isolated review launches done and working (0 refusals); the prompt line still absent from `awawa-refine-task.md`, `awawa-plan-wave-start.md`, `awawa-pr-review.md` (`grep`: 0 in each) |
| `status` counts the `DEFAULT` `STATUS` as `(none)` — **flagged, 5 reports** | 5 | `2026-09-17-pr-95.md`, part B | 239 of 277 entities | `status` reports the `DEFAULT` value | no (tool) |
| `fmt` wraps long strings with no option — **flagged, 5 reports** | 5 | `2026-09-17-pr-95.md`, part B | 51 of 122 added lines | `--width N` or `--no-wrap` | no (tool) |
| `lint --closure` has no `--skip` — **flagged, 4 reports** | 4 | `2026-09-18-pr-100.md`, part B | 106 names on `@TASK.FEAT55`'s footer | `--skip CATEGORY` on `lint --closure` | no (tool); our side done: `@TASK.CHORE57` delivered (`45c9ab5`, #109), the footer went from 185 to 106 |
| More than fifty answers in one context without a hand-over — **flagged, 4 reports** | 4 | `2026-09-17-pr-95.md`, part A | `agent-a15b0540` 72 answers, `agent-a4fdb792` 51, both told « At fifty steps … hand over » in their first line; the interactive sessions stayed at 36, 23 and 17 | `commands.md` § Sessions (interactive), PROC-4 (procedures) | no — the launch prompt states it and neither agent obeyed; the pilot noted it and accepted both hand-backs |
| An answer lost to the zsh separator `echo =====` — **flagged, 3 reports** | 3 | `2026-09-21-pr-101-102-103.md`, part A | 7 tool results across 6 transcripts, the wave's first `awawa context` among them | The separator line in `commands.md` (`pr-107` remediation 4) | no (`grep 'echo ==='` in `commands.md`: 0) |
| The collection script's previous-report selection and outside-corpus diff | 2 | `2026-09-21-pr-107.md`, part A | « 1 of 3 reports » for 4 files; the outside-corpus patch opens on `tasks.awawa` | `REPORT_NAME` and `:(exclude,glob)` (`pr-107` remediation 6) | no (`collect.py:534` and `:235` unchanged) |
| Corpus files read with the shell or a file tool | 2 (since the migration; CORPUS-3 counts 8 before it) | `2026-09-21-pr-107.md`, part A (3 `grep`) | 2 `Read` of `.awawa` files in `agent-a15b0540` | CORPUS-3 and CORPUS-4, cited | rule exists, not applied by one agent |

Raised before and not seen again, closed by observation: the modelling of a delivery by one `PULL_REQUEST` per pull request (`@TASK.CHORE57` delivered by #109, `EVALUATES` a `uint`, this report declares no `PULL_REQUEST`); `status TYPE` with no numeric order (the numbers were read by message and by branch, no pipe); a finding of the discovery missing the ruling questions, an option resting on a file of unknown provenance, a figure of one save stated for the perimeter, a split coming in a second session (no refinement in this PR; the `735473` figure itself still stands in `@HYPOTHESIS.TerraTokensStayWithinTheExactIntegerRangeOfADouble`, see the follow-up); the same measurement gap with two homes (`@TASK.FEAT66` untouched, still `draft`); an interactive session past fifty answers (36 at most).

### Recurring strengths

| Mechanism | Times seen | First noted in | Held again here |
|-----------|------------|----------------|-----------------|
| Questions to the owner batched in one call — **established** | 5 | `2026-09-17-pr-95.md`, part A | 1 `ASK`, 1 question, answered in 27 s |
| Work parallelised in agents with a written brief — **established** | 5 | `2026-09-17-pr-95.md`, part A | 2 wave agents (briefs of 6.6 k and 6.7 k characters), 6 review rounds, 2 `~/.ai` agents, 1 GREEN sub-agent |
| The PR body kept describing the branch head — **established** | 5 | `2026-09-17-pr-95.md`, part A | #112 rewritten at each of its three rounds; #111 not for the two drafts (part A) |
| Checks green at head — **established** | 5 | `2026-09-17-pr-95.md`, part A | 8 CI jobs on each; local 841 / 836 tests, `lint:types` 0, `audit:quality` 0 |
| The recording threshold applied before a `DECISION` is written — **established** | 4 | `2026-09-18-pr-100.md`, part A | 0 `DECISION` in 13 commits; the ruling on `HOLDS_FOR` in a `draft` task, the two testing rules in `~/.ai` |
| `refs` says what points at an entity — **established** | 5 | `2026-09-17-pr-95.md`, part B | `@TASK.TEST85` 0 incoming before its deletion; `@TASK.FEAT55` 5 |
| `status --where FIELD==VALUE` finds an entity without a file read — **established** | 5 | `2026-09-17-pr-95.md`, part B | `HOLDS_FOR` on two types, `DELIVERED_BY` |
| `diff base head` gives an entity-level review — **established** | 5 | `2026-09-17-pr-95.md`, part B | 7 and 2 entities at field level |
| `WHEN <field> <value>` and `REQUIRED` gate a field — **established** | 5 | `2026-09-17-pr-95.md`, part B | L025 at 10:44:55Z, L016 at `26eddcf` |
| `new TYPE Name` skeletons — **established** | 4 | `2026-09-17-pr-95.md`, part B | 4 calls |
| The per-worktree setup run before the first check — **established** | 3 | `2026-09-18-pr-100.md`, part A | `bun install --frozen-lockfile` in 3 hand-backs, the `input` symlink created in round 3 |
| A review round addressing its threads in minutes | 2 | `2026-09-21-pr-101-102-103.md`, part A | 6 rounds, 3 min 44 s to 6 min 30 s |
| Incoming references checked before a deletion | 2 | `2026-09-21-pr-101-102-103.md`, part B | `refs @TASK.TEST85` before `1b319e5` |
| `lint --strict` validating an anchor | 2 | `2026-09-21-pr-101-102-103.md`, part B | L016 at `26eddcf` |
| A wave ruling relayed to a peer | 2 | `2026-09-21-pr-101-102-103.md`, part A | The task number reserved by `SendMessage` at 09:55:45Z |
| A model chosen per task in a wave | 1 | this report | Opus for FEAT55, Sonnet for FEAT65 |
| A review challenge withdrawn on the owner's ruling into a draft task naming the decision it conflicts with | 1 | this report | `@TASK.DOCS86`, two `SOURCE` with `PR 111` |
| General rules sent to `~/.ai` by pull request during a review and merged within minutes | 1 | this report | #42 (3 min 13 s from creation to merge), #43 (1 min 09 s) |
| The pilot session cleared by the owner between rounds | 1 | this report | `/clear` at 10:58:21Z after 1.45 M cache-read |

Noted before and not seen holding here: every commit lints clean from the repository root (12 of 13 — `26eddcf`, part A; the mechanism leaves the established list until it holds again); discovery delegated to cheaper models with pinned sources (no discovery); a progress file carrying a procedure across two runs (each round wrote its own, none resumed); the owner's free-text answer re-asked with its provenance (not exercised); `--overlay` testing a gate, the 50-step hand-over of PROC-4, the pilot sending an unbacked claim back (not exercised); the hosted `claude[bot]` review finding gaps (« No issues found » on #111 at 10:00:40Z, nothing on #112).

## Follow-up on the previous report's remediations

| Remediation | Done? | Evidence |
|-------------|-------|----------|
| 1 — The refinement inventories every discovery finding before the ruling questions (`awawa-refine-task.md` § 3) | not exercised | No refinement in these PRs |
| 2 — An option names its file's provenance; « the reference saves reach 735473 » corrected by the owner before the merge of #107 | no | `awawa show @HYPOTHESIS.TerraTokensStayWithinTheExactIntegerRangeOfADouble .` at head: `RATIONALE` still « reach 735473 » |
| 3 — The split offered first when a minority of the `SPEC` is blocked (§ 4) | not exercised | No refinement |
| 4 — The refusal line above the base check of `awawa-refine-task.md`; the separator line in `commands.md` | no | `grep` of the three command files for the line: 0; `grep -c 'echo ===' commands.md`: 0; 7 refusals and 7 separator losses here |
| 5 — `@TASK.FEAT66`'s refinement settles the three homes of the energy gap | no | `@TASK.FEAT66` `draft`, `BLOCKED_BY 1` (`awawa status TASK .`), untouched by both PRs |
| 6 — `collect.py`: `REPORT_NAME` admits multi-PR names, the outside-corpus diff excludes the corpus | no | `collect.py:534` regex unchanged; `previous-reports.md` « 1 of 3 reports »; `git-diff-outside-corpus.patch` holds `tasks.awawa` |
| 7 — `grep` reads of corpus files replaced by the `awawa` command CORPUS-4 gives | partly | 0 `grep` of a `.awawa` file in the 9 working transcripts; 2 `Read` in one agent; 1 `git grep` for a field-text search the tool has no command for |
| 8 — The interactive session asks at its fiftieth answer | not exercised | 36 answers at most in an interactive session; the two agents past fifty are counted above |
| `@PULL_REQUEST.PCST107`, `@USAGE_REPORT.PR107` accepted | yes, then superseded | `@USAGE_REPORT.PR107` at head (`sources.awawa:106`, `EVALUATES 107`); the `PULL_REQUEST` type and its entities removed by #109 (`45c9ab5`) |
| `@TASK.DOCS50` (from `pr-95`, restated since) | no | `draft`, `tasks.awawa:53` |
| `@TASK.CHORE60` — `agents/worktree.md` names the acceptance commands (from `pr-101-102-103`) | no | `draft`, `tasks.awawa:345`; `agents/worktree.md`: 0 occurrence of `audit:quality` |

## Appendix

### Measurements

Active time counts gaps under ten minutes only; figures from `transcripts-measure.txt`, the GREEN sub-agent from this report's `measure.py` run. `88777eca` is not a row: its 93 records are all inside `2b54c14f`.

| Session | Active time | User turns | Tool calls | awawa calls by subcommand | Tokens read from cache | Tokens written |
|---------|-------------|------------|------------|---------------------------|------------------------|----------------|
| wave pilot `2b54c14f` (36 answers) | 34 min 26 s | 9 | Bash 27, Agent 2 | status 7, context 9, lint 7 | 3,691,145 | 22,516 |
| agent `agent-a4fdb792` FEAT55, Opus (51 answers) | 5 min 18 s | 3 | Bash 54, Edit 3, Agent 1, Skill 1 | lint 10, show 9, fmt 6, refs 4, context 2, status 2, new 1 | 4,429,807 | 2,902 |
| agent `agent-af13e5b0` GREEN of FEAT55, Sonnet (23 answers) | 1 min 43 s | 2 | Bash 16, Edit 4, Read 3 | — | 1,110,882 | 2,791 |
| agent `agent-a15b0540` FEAT65, Sonnet (72 answers) | 9 min 39 s | 4 | Bash 72, Read 17, Edit 5, Write 2, WebFetch 2, Skill 1 | lint 4, fmt 4, status 4, show 3, context 1 | 8,663,166 | 4,565 |
| review pilot `6c2f5774` (23 answers) | 23 min 45 s | 15 | Bash 6, Agent 5 | — | 1,453,239 | 16,734 |
| agent `agent-a1cc563a` #111 round 1 (34 answers) | 5 min 03 s | 3 | Bash 31, SendMessage 1 | show 15, status 8, refs 3, new 1, fmt 1, lint 1 | 2,344,270 | 1,519 |
| agent `agent-acf65a95` #112 round 1 (33 answers) | 6 min 30 s | 3 | Bash 32, Write 3, SendMessage 1 | lint 6, show 3, fmt 3, status 2, new 1 | 2,293,359 | 409 |
| agent `agent-aebc78ed` #111 round 2 (27 answers) | 4 min 14 s | 4 | Bash 23, Monitor 1, Read 1 | show 12, status 3, lint 3, fmt 2, new 1 | 1,730,008 | 1,105 |
| agent `agent-a6595522` #111 round 3 (16 answers) | 3 min 44 s | 3 | Bash 13, Read 1 | show 10, status 2, refs 1, fmt 1, lint 1 | 765,618 | 309 |
| agent `agent-aebbd02e` `~/.ai` PR #42 (11 answers) | 54 s | 3 | Bash 7, Read 3, Edit 1 | — | 480,919 | 318 |
| review pilot `304d5724` (17 answers) | 15 min 03 s | 12 | Bash 5, Agent 3, AskUserQuestion 1 | — | 964,200 | 10,669 |
| agent `agent-a49459c2` #112 round 2 (19 answers) | 3 min 50 s | 2 | Bash 18 | lint 6, show 3, refs 3, status 1, fmt 1 | 989,697 | 252 |
| agent `agent-a6c1ceac` #112 round 3 (34 answers) | 5 min 07 s | 2 | Bash 32 | show 8, lint 5, fmt 2, refs 1, context 1 | 2,427,161 | 4,461 |
| agent `agent-aa4cac17` `~/.ai` PR #43 (11 answers) | 59 s | 2 | Bash 10, Read 1, Edit 1 | — | 497,402 | 895 |
| **Total, 14 transcripts, 407 answers** | | | | 184 | **31,840,873** | **69,445** |

By stage: the wave (pilot and 3 agents) 182 answers, 17.90 M cache-read, 32.8 k written; the review (2 pilots, 6 rounds) 203 answers, 12.97 M, 35.5 k; the two `~/.ai` pull requests 22 answers, 0.98 M, 1.2 k.

| Pull request | Corpus lines changed | PR lines changed | Share |
|--------------|----------------------|------------------|-------|
| #111 | 128 (`tasks.awawa` 90, `rules.awawa` 36, `commands.awawa` 2) | 217 (207+, 10−, 9 files) | 59 % |
| #112 | 9 (`tasks.awawa` 8, `rules.awawa` 1) | 63 (38+, 25−, 4 files) | 14 % |

### Claims checked

| Claim | Check | Verdict |
|-------|-------|---------|
| #111 body: `logisticsPaused` taken from save A « even `false` », otherwise save B, no key when neither, « appended last » | `mergeGlobalMetadata.ts`: `metadataA?.logisticsPaused ?? metadataB?.logisticsPaused` (`??` keeps `false`), `...(logisticsPaused === undefined ? {} : {logisticsPaused})` as the last property; test « from save A » with A `false`, B `true` | confirmed |
| #111 body: `additionalProperties` stays `false`, a non-boolean is refused | `section0-player-progression.schema.json` diff adds `"logisticsPaused": {"type": "boolean"}` only; test « When logisticsPaused is not a boolean » expects `SCHEMA_VIOLATION` | confirmed |
| #111 body: `bun test` 841 pass, `lint:types` 0, `check:guards` 0, `awawa fmt --check` / `lint --strict` 0 | `checks-bun-test.txt` 841 pass 0 fail; `checks-lint-types.txt` 7 × exit 0; `checks-audit-quality.txt` exit 0; `corpus-fmt-check.txt`, `corpus-lint-strict.txt` exit 0 | confirmed |
| #111 body: `bun merge` on `Test`, `Test2` before vs after identical md5; `bun validate` on the private 2.103 sample valid; merged metadata ends on `"logisticsPaused":false` | Private saves; no base build in this run | unchecked |
| `@TASK.FEAT55` `SPEC` 1 `IMPL` « should report the save as valid, with no error and no warning » | `validateSaveContent.spec.js:33`, a pre-existing case on a save without the key — the second clause of the `SPEC`; the Skeo case is the `describe` at line 125 (anchored) and its `it` « should accept the save » appears 4 times in the file (lines 126, 244, 429, 550), hence not anchored | confirmed |
| `@TASK.FEAT55` `SPEC` 3 « the same bytes as before the task » `IMPL` the « no logisticsPaused key » test | The test asserts `Object.keys(mergeResult)` equals the five keys in order; the byte identity itself was checked on private saves (unchecked above) | confirmed as a key-order proof, the byte claim unchecked |
| Reply `r4070404481`: `HOLDS_FOR` on 34 entities, 20 `both`, 13 `current`, 1 `legacy`, 22 rules, 12 sections | `awawa status RULE --where HOLDS_FOR==<v> .` and `SECTION` on `trees/head`: 9 + 11, 13 + 0, 0 + 1 | confirmed |
| Reply `r4070404481`: the corpus holds 4 releases; `@DECISION.AGameReleaseIsCitedAsASource` `SPEC` « No type of the specification area carries the game version as a field of its own » and `REJECTED` « A GAME_VERSION field on RULE » | `awawa status GAME_RELEASE .`: 1.618, 2.008, 2.102, 2.103; `awawa show` of the decision: both texts present | confirmed |
| `@TASK.DOCS86` `SPEC`: `INDEX`, `LEGACY_INDEX` and the incoming `CONSTRAINED_BY` are keyed on the era; `@DECISION.TheEraIsNotTheArchive` names `HOLDS_FOR legacy` | `awawa show @SCHEMA.SECTION .`: `WHEN HOLDS_FOR current` and `both` carry `INCOMING CONSTRAINED_BY` and `FIELD INDEX`, `legacy` and `both` carry `LEGACY_INDEX`; the decision's first `SPEC` names `HOLDS_FOR legacy` | confirmed |
| Reply `r4070779699`: « The four `GAME_RELEASE` entities it needs already exist » | above | confirmed |
| Round 2 of #111 hand-back: « `git diff` of `packages/` between the backup ref and HEAD is empty » | The backup ref was deleted at 10:47:33Z | unchecked |
| #112 body: the hash recomputed for the six names matches `PlanetHash.cs` | This report's recomputation: 6 of 6 equal to the table | confirmed |
| #112 body: `bun test` 836 pass; `check:guards` includes `validate:tables` | `checks-bun-test.txt` 836 pass; `package.json` `check:guards` chain ends on `validate:tables` (`checks-audit-quality.txt`) | confirmed |
| Reply `r4070960791`: no `.map`, `.filter`, `.reduce`, loop or `if` remains in the file; 8 expectations typed on `expect` | `grep -cE '\.map\(\|\.filter\(\|\.reduce\(\|\bfor\b\|\bif\b\|\bwhile\b'` on the file at head: 0; 8 `expect<` lines in the diff of `26eddcf`, 10 in the file | confirmed |
| Reply `r4070960791`: `@TASK.FEAT65`'s `IMPL` is the table row and it « no longer cites a test » | `corpus-show-TASK-FEAT65.txt`: one `IMPL`, `planetNamesByNumericId.json::-440810600` | confirmed |
| Reply `r4070892428`: `awawa refs @TASK.TEST85` reported 0 incoming | Re-run on the tree of `d1103d5`: `incoming (0)` | confirmed |
| Reply `r4070402826`: `check:assertions` catches the boolean forms only | `scripts/check-test-assertions.ts:109` filters on `hasFabricatedBooleanAssertion`; `EnergyLevelsPresenter.spec.ts:93` passes the guard at head | confirmed |
| Wave pilot 23:17:04Z: « EN-PLANET-3 … an old rule identifier that the corpus no longer uses » | No `RULE` of the corpus carries it; `docs/energy-levels.md` defines EN-PLANET-1 and -2, not -3; `@TASK.FEAT65`'s `RATIONALE` now uses it | overstated — it was never defined, and the corpus now uses it |
| Wave pilot 23:17:04Z and FEAT65 hand-back: `docs/save-format.md` lines 222–223 still deny the hash | `docs/save-format.md:222` at `11df2fc`: « No known deterministic hash function … reproduces these IDs » | confirmed |
| Wave pilot 23:17:04Z: « both agents went past the 50-step point … at 60 and 101 tool calls » | Answers: 51 and 72; tool calls (`transcripts-measure.txt`): 60 and 101 | confirmed |
| Review pilot 11:01:14Z: the static-data rule « n'est pas encore sur `main` » | `gh pr view 42 --repo delairec/.ai`: `MERGED` at 10:57:58Z, 3 min 16 s before the statement | wrong |
| Round 1 of #111 hand-back: « The auto-mode classifier refused `git rebase origin/master` twice » | Denials in `agent-a1cc563a`: 09:52:46Z (a compound command writing the progress file) and 09:53:23Z (the rebase); one rebase denied, one other command | overstated |
| Round 1 of #112 hand-back: « I made no backup branch: the classifier refused to create one » | Denial at 09:53:24Z on a command creating the branch and writing the progress file, « [Git Destructive] » | confirmed |
| `2026-09-21-pr-107.md`: « the figure itself is corrected in this PR by the owner before the merge » | `@HYPOTHESIS.TerraTokensStayWithinTheExactIntegerRangeOfADouble` `RATIONALE` at head: « reach 735473 » | wrong |
| `2026-09-21-pr-107.md`: `lint --closure` footer of `@TASK.FEAT55` 185 names | At this head 106 (`@TASK.CHORE57` delivered since) | confirmed for its date |
