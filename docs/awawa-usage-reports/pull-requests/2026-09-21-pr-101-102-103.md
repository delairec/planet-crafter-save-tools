<!-- awawa-usage-report -->

# 2026-09-21 — PR #101, #102, #103 — the three task pull requests of the wave of 2026-09-18

One report for three pull requests, on the owner's instruction (« 101, 102, 103 : on fait un seul rapport d'usage pour les 3 »). The launching session chose, and this report states as its own choice rather than a ruling: the file is committed on the head of PR #102 (`fix/45-empty-metadata-section`), the last of the three in the merge order #101, #103, #102; one `USAGE_REPORT` entity names the three `PULL_REQUEST` entities, `EVALUATES` being `REPEATABLE` in `@SCHEMA.USAGE_REPORT`, so no encoding compromise was needed; the same comment is posted on the three pull requests. PR #101 is measured at its head `029b884` and was merged into `master` (`d3940a2`, 2026-09-21T10:40:48Z) by another agent while this report was being collected; PR #103 is measured at `8d3496e` and its branch was rebased onto the new `master` afterwards, mechanically, by that agent. The owner confirmed that #101 needed a third review round before this report; that round (`029b884`, 10:31–10:36Z) is inside the perimeter.

| Field                                          | Value |
|------------------------------------------------|-------|
| **Author of this report**                      | `Claude Fable 5.1` (`claude-fable-5-1`), effort `high` (from `~/.claude/agents/awawa-usage-reporter.md`) |
| **Models that drove the sessions under review** | `Claude Opus 5` (`claude-opus-5`), effort `medium` — 17 of the 18 commits of the three ranges (`Co-Authored-By` trailers, `git-log.txt` of each collection); the effort is read from the `effort` field of every request record of the measured transcripts (`"effort":"medium"` on 57 of 57 records of the wave session, 35 of 35 of the review session, 90 of 90 of `agent-a0aee5fb`)<br>`Claude Sonnet 5` (`claude-sonnet-5`), effort `medium` — commit `01aa2ec` of #102 (the fallow exemption), interactive session `7c8c9316` in the #102 worktree, 41 of 41 records |
| **Sessions measured**                          | Commit trailer `session_01N2jLGfdecUAwv2zVxhVKb7` (the 11 commits of 2026-09-21; the 7 commits of 2026-09-18 carry no `Claude-Session` trailer and were found by the branch names)<br>`51c18626` (wave pilot, `/awawa-plan-wave-start`, 2026-09-18, shared with PR #100) and its agents `agent-a29933ea` (CHORE51), `agent-a1ccac75` (GREEN of CHORE51), `agent-ac59e89a` (FIX45), `agent-af2e08bd` (GREEN of FIX45), `agent-aa2411a6` (CHORE52), `agent-a367d94a` (GREEN of CHORE52), `agent-a4791676` (CHORE52 hand-over)<br>`d9c08253` (PR #100 review pilot, read up to its own report launch at 14:11:30Z) and its agents `agent-ab415d3f`, `agent-a559648d`, `agent-a84a7e10` (rebases of #101–#103 after #100), `agent-a72fd73e` (fallow fix of #101)<br>`7c8c9316` (interactive fallow fix of #102, in the #102 worktree)<br>`a756b5f6` (review pilot, `/awawa-pr-review 101, 102 et 103`, 2026-09-21) and its agents `agent-a0aee5fb` (#101 round 2), `agent-a2b5dd95` (#103 round 1), `agent-a7abe9f0` (#103 round 2), `agent-aef0c1aa` (#102 rebase)<br>`3373c56c` (`/awawa-pr-review 102`) and `agent-a311ea` (#102 round)<br>`agent-a71e2b62` (#101 round 3, launched by the session that later launched this report; measured by hand with `measure.py`, `transcripts.md` having cut it as posterior to the first report launch) |
| **Delivered**                                  | #101 `@TASK.CHORE51`: `scripts/check-corpus-anchors.ts`, a guard refusing a corpus anchor on a path git does not track, chained in `check:guards`; 4 draft tasks from the review (`@TASK.CHORE56`–`@TASK.CHORE59`)<br>#102 `@TASK.FIX45`: validation refuses a save whose section 0 is empty, `NoGlobalMetadataToMergeError` in the domain; `@DECISION.ABrokenInvariantOfADomainRuleFailsWithADomainErrorType`; draft `@TASK.REFACTOR56` from the review<br>#103 `@TASK.CHORE52`: five value tables moved to JSON files with a JSON Schema each under `scripts/validate-tables/schemas/`, `bun run validate:tables`, five `DATATABLE` entities, `@DECISION.AValueTableIsAJsonFileBesideTheModuleThatReadsIt`, the `INCOMING SOURCE_OF` gate on `URL` and `GAME_RELEASE` |

## Verdict in two lines

**Ours**: three tasks were delivered from a written brief in 5 min 29 s, 8 min 11 s and 8 min 48 s to an open pull request each, every one of the 18 commits of the three branches lints clean, and five review rounds answered 12 threads in 4 min 56 s to 6 min 17 s each without writing one `DECISION`; but two of the three pull requests were red on the `fallow` CI job at delivery because the wave's acceptance list omits `bun run audit:quality` (43 answers and 2.71 M cache-read tokens to fix), the same owner instruction on the `PULL_REQUEST` entities got three different answers on three branches (deleted, deleted with its decision, challenged and kept under L004) and this report re-creates two of them because `EVALUATES` admits nothing else, PR #103's own orphan-source gate blocked its rebase on two releases `master` had added uncited (a 62-answer round ending on a question), two branches opened tasks numbered 56 in parallel, and the worktree-isolation check refused 24 commands in 238 answers, a point flagged for the third report in a row.

**The tool's**: awawa 2.7.0 held every gate it was asked to hold — L004 refuses the deletion of the cited `@PULL_REQUEST.PCST102` (probe: 2 errors, exit 1), the new `INCOMING SOURCE_OF` gate raises L026 on an orphan release (probe), `diff` rendered 4, 6 and 18 changed entities at field level in 0.0 s including the schema change, and `refs` preceded every deletion — and cost the journey four things: it cannot run in CI (a local x86-64 binary with no install source the project names, so `check:anchors` re-parses the corpus text in 255 lines and no job lints the corpus, owner's request 4060968303), L016 tests the disk and not git (the reason `@TASK.CHORE51` exists at all), L025 refuses an `@TYPE.Name` inside an anchor fragment as if it were prose (probe: 1 error), and `lint --closure` still has no `--skip`, so the footer of each delivered task lists 44 to 73 unrelated entities.

## The owner's word

> Schema constraints are sometimes applied too strictly by the AI agents, which forces us into contortions on subjects that could be handled more simply. There are probably avenues to evaluate for reducing this side effect by reworking the schemas to fit our way of working (currently, we are the ones adapting to the schema's constraints, which is an indicator: our schema needs to be reviewed).

## Part A — Us: method, corpus, conduct

### What went well

| What | Evidence |
|------|----------|
| Three tasks delivered in parallel from one wave, each to an open pull request within nine minutes of its launch | `timeline-02` (wave pilot): agents launched 13:39:35Z (CHORE51), 13:39:57Z (CHORE52), 13:40:16Z (FIX45); `gh pr view`: #101 created 13:45:04Z (5 min 29 s), #103 13:48:45Z (8 min 48 s), #102 13:48:27Z (8 min 11 s); hand-backs 13:46:20Z, 13:49:16Z, 13:49:02Z (`timeline-06`, `-15`, `-16`) |
| Each delivery read its task through the tool, not the files | `transcripts-measure.txt`: `context` 1, `lint` 9, `show` 4 in `agent-a29933ea`; `context` 1, `lint` 7, `show` 2, `refs` 3 in `agent-ac59e89a`; `context` 1, `lint` 9, `show` 4, `status` 3, `new` 1, `refs` 1 in `agent-aa2411a6`; 0 `Read` of a `.awawa` file in the three |
| TDD with a GREEN sub-agent barred from editing the specs, RED confirmed first | `agent-a1ccac75` (CHORE51): « FORBIDDEN: editing any spec file », 11 tests failing on `not implemented` then 11 pass (`timeline-06` 13:42:31Z, `-12`); `agent-af2e08bd` (FIX45): 4 failing tests named in the brief, « confirmed each one failed on the real bug, including the TypeError itself » (`timeline-16` hand-back); `agent-a367d94a` (CHORE52): 6 answers, 32 s |
| The guard caught a real case during its own development | `agent-a1ccac75` hand-back 13:44:11Z: « The guard exits 1 on the real corpus: it reports two anchors, and both are the new script files, which git does not track yet »; the delivery hand-back: « That confirms it asks git rather than testing the disk » |
| The 50-step hand-over of PROC-4 worked on the largest task | `agent-aa2411a6`: 50 answers, stopped at 13:49:16Z with « I stopped at the fifty-step budget », state in `awawa-plan-wave-start-chore-52.md`; `agent-a4791676` launched 13:49:41Z, promotion commit `316fd06` pushed and hand-back at 13:51:44Z: 17 answers, 2 min 11 s, 984,819 cache-read tokens (`transcripts-measure.txt`) |
| A ruling taken in one task was relayed to the peer it concerned, during the wave | `timeline-02` 13:46:31Z: `SendMessage` to `agent-aa2411a6` — « a repository script run by CI must not call the awawa binary » (from CHORE51's hand-back at 13:46:20Z); CHORE52's hand-back: « nothing I added calls awawa or touches CI »; `validate:tables` imports Ajv, not awawa (`scripts/validate-tables/validate-tables.ts` at `8d3496e`) |
| Every commit of the three branches lints and formats clean | This report's `lint-each-commit.py` on the extracted tree of each of the 18 commits (7 of #103, 7 of #101, 4 of #102): `awawa lint --strict .` exit 0 and `awawa fmt --check .` exit 0, 18 of 18; #103 round 2 moved the ruling commit `d7a5d83` to the bottom of the branch for that purpose (`timeline-09` hand-back) |
| Review rounds answered their threads in minutes, three of them on the same morning in parallel | `agent-a0aee5fb` (#101, 3 threads + 1 comment): 09:24:54–09:31:11Z, 3 commits; `agent-a2b5dd95` (#103, 4 threads + 1 comment): 09:24:59–09:30:41Z, all five handled in the tree; `agent-a7abe9f0` (#103 round 2): 09:31:52–09:37:11Z, 5 commits, 5 replies, readiness 09:36:49Z; `agent-a311ea` (#102, 2 threads): 09:44:47–09:50:18Z, readiness 09:49:44Z; `agent-a71e2b62` (#101 round 3, 2 comments): 10:31:25–10:36:21Z |
| One review comment was challenged with a probe instead of applied | Thread 4060876116 (« cité par aucune source = on retire ») answered by 4060952577: `awawa refs @PULL_REQUEST.PCST102 .` → 2 incoming, removal tried on a copy → `L004`; this report's probe on `trees/head` of #102 gives the same 2 `L004` errors, exit 1; thread resolved by the owner |
| Incoming references were read before every deletion | `refs` calls: 8 in `agent-a311ea`, 4 in `agent-a2b5dd95`, 2 in `agent-a0aee5fb`, 1 in each of the three rebase agents (`transcripts-measure.txt`); `@PULL_REQUEST.PCST101` had 1 incoming at `9e23ad9` (`@DECISION.TheAnchorGuardReadsTheCorpusTextItself SOURCE.REF`, this report's `awawa refs` on the extracted tree) and both went in `2c9e2b0`; 0 `PULL_REQUEST` with `incoming 0` at the head of #102 (`awawa status PULL_REQUEST .`) |
| Out-of-scope requests became draft tasks, and no `DECISION` was written in five review rounds | `corpus-diff.txt`: `@TASK.CHORE56`, `CHORE57`, `CHORE58`, `CHORE59` (#101), `@TASK.REFACTOR56` (#102), all `draft`; #103 round 2 hand-back: « I wrote no DECISION for it: it falls below the recording threshold, and the lint gate (L026) already enforces the rule »; #101 round 2 deleted `@DECISION.TheAnchorGuardReadsTheCorpusTextItself` for failing the threshold (reply 4060809350) |
| The readiness comment was edited in place and the bodies rewritten to the head | #101: comment 5731394981 created 2026-09-18T14:23:34Z, « (Updated 2026-09-21 after the review round on `029b884`.) »; #102: the 2026-09-18 rebase comment 5731213582 edited by `agent-aef0c1aa` rather than re-posted; `gh pr edit --body-file` in `timeline-07` 09:29:32Z, `timeline-09` 09:35:27Z, round 3 10:33:51Z |
| Round 3 read the task numbers of the other open branches before numbering | `agent-a71e2b62` hand-back: « I read the task lists with awawa status TASK on master (c8039f8), #102 (37f2ca3) and #103 (8d3496e). The highest number in use was 57 » — checked: `FEAT55` is the highest on `master`, `REFACTOR56` on #102, `CHORE57` on #101; it also reported the 56 collision instead of renumbering |
| The one question of the review day went to the owner in one call, with the options and a recommendation | `timeline-03` 09:30:47Z: one `AskUserQuestion`, 3 options (give the citations, remove the releases, cite from `@TASK.FEAT55`); answered 09:31:33Z; round 2 launched 09:31:52Z |
| Checks green on the three heads | `gh pr checks`: audit, build, claude-review, fallow, guards, lint, scenarios, test pass on `029b884`, `37f2ca3` and `8d3496e`; this report's worktree runs at `37f2ca3`: `bun test` 818 pass, 0 fail; `lint:types` exit 0 on 7 packages; `audit:quality` exit 0; at `8d3496e`: 821 pass, 0 fail, `lint:types` 0, `audit:quality` 0; `git status --porcelain` empty before and after (`checks-*.txt`) |

### What went wrong

| What | Evidence | Whose fault |
|------|----------|-------------|
| Two of the three pull requests were red on the `fallow` CI job at delivery, and the acceptance list they were given could not see it | Wave prompt § 5 (`agent-a29933ea` first message): « bun test, bun run lint:types, bun run check:guards, awawa fmt --check . and awawa lint --strict . » — no `audit:quality`, which `AGENTS.md` § Commandes names as the whole quality gate; #101: `readAnchorFields` cognitive complexity 25 against 15 (`agent-ab415d3f` hand-back), fixed by `agent-a72fd73e` (22 answers, 4 min 22 s, 1,299,517 cache-read) on the owner's ruling « simplifier, le workflow quality a raison »; #102: unresolved import of `save-file.schema.json` (`agent-a559648d` hand-back), fixed by the owner in session `7c8c9316` (21 answers, 1,413,143 cache-read, `01aa2ec`) | method |
| The same owner instruction on the `PULL_REQUEST` entities got three different treatments, then this report writes two of them back | The wave prompt § 6 told each agent « A PR the corpus cites is a PULL_REQUEST entity » and to source every ruling with `REF @PULL_REQUEST.<pr>`; three were written (`PCST101`, `PCST102`, `PCST103`); the owner asked for their removal on #101 (4057063342), #103 (4057029273) and #102 (4060876116); #103 deleted the entity and its six `SOURCE` fields (`65b03fe`), #101 deleted the entity and the decision that cited it (`2c9e2b0`), #102 kept it because `agents/corpus-ecrire.md` requires every `SOURCE` to name a source entity and L004 refuses the removal; `@SCHEMA.USAGE_REPORT` `EVALUATES @PULL_REQUEST`, so this report declares `PCST101` and `PCST103` again in the same commit as its entity | corpus modelling (the `SOURCE.REF REQUIRED` and `EVALUATES @PULL_REQUEST` declarations), method (the prompt line) |
| PR #103's own gate blocked its rebase, on entities the base had added uncited, and the fix is a citation written for the gate | `@TASK.CHORE52` SPEC 6 makes an uncited `GAME_RELEASE` an error; `master` `0af76d4` and `c8039f8` (the owner's hand commits) added `@GAME_RELEASE.1.618` and `2.008` with 0 incoming (`awawa refs` on `trees/base`); after the rebase `awawa lint --strict .` gave 2 × L026 and the pre-commit hook refused every commit (`agent-a2b5dd95` hand-back, 62 answers, 4,481,942 cache-read, ending on a question); the owner chose « Citer depuis @TASK.FEAT55 »; `d7a5d83` writes `SOURCE "Earlier game releases recorded alongside this task; cited here by user ruling of 2026-09-21 on pull request #103"` | schema (a gate on a provenance type with no transition for what the base already holds), conduct (releases added by hand with no citation, legal at the time) |
| Two branches opened a task numbered 56 within nineteen minutes | `8e971cb` (#101, 09:28:48Z) adds `@TASK.CHORE56`; `37f2ca3` (#102, 09:47:30Z) adds `@TASK.REFACTOR56`; `~/.ai/procedures/awawa-pr-review-round.md` has no numbering step across open branches (`grep` for `highest`, `next free`: 0 lines); the wave had reserved one number per agent (« The task number reserved for me, 54, was not used », `agent-a29933ea`), the review rounds had none; the owner ruled merge order, so `REFACTOR56` moves at #102's rebase, and the two tasks this report opens (`CHORE60`, `CHORE61`) may move too | method |
| 24 commands refused by the worktree-isolation check in the six isolated agents, none in the nine that worked without isolation | `grep -c 'Refusing to run it'`: `agent-a2b5dd95` 7 of 62 answers, `agent-a29933ea` 6 of 40, `agent-a0aee5fb` 4 of 44, `agent-aa2411a6` 4 of 50, `agent-ac59e89a` 2 of 34, `agent-a1ccac75` 1 of 8 — 24 in 238 answers; 0 in `agent-a7abe9f0`, `agent-a311ea`, `agent-aef0c1aa`, `agent-a71e2b62` and the four agents of 2026-09-18 14:08–14:23Z, which ran the same git and awawa commands from an un-isolated session; the rule against computed arguments (delairec/.ai#34 `abde632`) was in `commands.md` since 2026-09-17 | conduct |
| Ten answers lost to a zsh separator: `echo =====` is `=cmd` expansion in zsh | `grep -c 'ERROR after Bash.*echo ='` on the timelines: `agent-ac59e89a` 2, `agent-ab415d3f` 2, `agent-aef0c1aa` 2, `agent-a311ea` 1, `agent-a559648d` 1, `agent-a7abe9f0` 1, `agent-a84a7e10` 1 — each a `(eval):1: ===== not found` that failed the chained reads after it; `2026-09-18-pr-100.md` saw one in PR #100's agent; `commands.md` has no line on it | conduct |
| A rebase onto `DOCS48` dropped one hunk of #101 and one of #103, found by the owner two days later | Owner on #101 (2026-09-20T16:30:04Z): « Le rebase a supprimé la phrase … à revérifier »; on #103 (16:40:13Z): « La reformulation du paragraphe a été perdue pendant le rebase »; both restored (`e26935a`, `6e463a6`); the rebase (`7197f1f`, per reply 5758…, « the earlier rebase onto DOCS48 ») is in no measured transcript — the sessions `f7635a63` and `48aa7d1d` of 2026-09-18 evening mention the branch only in tool results | conduct |
| Round 1 of #103 ran 62 answers, past the fifty of PROC-4, and left its work uncommitted | `transcripts-measure.txt`: 62 answers, 70 tool calls; hand-back: « nothing is committed, pushed or answered yet »; round 2 had to rebuild five commits from the tree and the scratchpad (`timeline-09` 09:33:38Z `git write-tree`, staged and unstaged patches) | conduct |
| The schema change of PR #100 cost these three pull requests a rebase each and a `DELIVERED_BY` rewrite | `agent-ab415d3f`, `agent-a559648d`, `agent-a84a7e10`: 64 answers, 3,485,512 cache-read, 24,832 written, 2026-09-18 14:08:56–14:13:42Z (full runs; `2026-09-18-pr-100.md` counted 47 answers and 2.36 M up to its own cut at 14:11:30Z); raised in that report as a pain point, counted below, not re-flagged here | method (inherited) |

### Remediations, by return

| # | Change | What it fixes | Recorded as |
|---|--------|---------------|-------------|
| 1 | `agents/worktree.md` names the acceptance commands a task branch runs green before its pull request is opened or updated, `bun run audit:quality` among them; the wave launch prompt copies that list instead of its own | 2 of 3 pull requests red on `fallow` at delivery, 43 answers and 2.71 M cache-read to fix | `@TASK.CHORE60` (draft, opened by this report); the prompt line is a `~/.claude/commands/awawa-plan-wave-start.md` edit, tool-specific, no entity |
| 2 | The provenance gates of the schema are reviewed against the way the corpus is worked: `SOURCE.REF REQUIRED`, the `INCOMING SOURCE_OF` gate on `URL` and `GAME_RELEASE`, `EVALUATES @PULL_REQUEST`; each kept, demoted to a warning or removed, `@TASK.CHORE57` taking the `PULL_REQUEST` type | The three treatments of one instruction, the L026 block of #103, the two entities this report re-creates | `@TASK.CHORE61` (draft, opened by this report), naming `CHORE57` in its `DESC` prose — a `REF @TASK.CHORE57` is refused by L004 on this branch, which does not carry #101's tasks until its rebase (`awawa lint --strict .` at the first attempt: 1 error, `tasks.awawa:657`) |
| 3 | A review round reads the highest task number across `master` and every open task pull request before `awawa new TASK`, as round 3 of #101 did by hand | The two tasks numbered 56 | `~/.ai` pull request to open on `procedures/awawa-pr-review-round.md` (general procedure, no entity) |
| 4 | A separator line is printed with `printf '%s\n' -----` or `echo ---`, never `echo ===`, in a zsh session | 10 answers lost | `~/.ai` pull request to open on `instructions/commands.md` (general rule, no entity) |
| 5 | A review round launched into an existing worktree runs without `isolation: "worktree"`, as the 2026-09-21 rebase and round-3 agents did, and the wave prompt quotes the one line of the refusal rule above its base check | 24 refusals in the isolated agents, 0 outside them | `~/.claude/commands/awawa-pr-review.md` and `awawa-plan-wave-start.md` edits, tool-specific, no entity; the rule itself exists (`abde632`) |
| 6 | After a rebase that meets a conflict in a file the base moved or split, `git range-diff <backup>..<new>` is read before the push and every `!` line is accounted for | The two hunks lost on the `DOCS48` rebase | `~/.ai` pull request to open on `procedures/awawa-pr-review-round.md` § 2 (the 2026-09-21 rebases already did it: `agent-aef0c1aa` « git range-diff shows commits 1 and 3 identical ») |
| 7 | A task pull request whose usage report lands on a sibling branch says so in its readiness comment, and the merge waits for that branch | #101 merged at 10:40:48Z with its report on #102's branch, the second time in two reports a report is not in the pull request it measures | none new — remediation 1 of `2026-09-18-pr-100.md` (the readiness comment names what remains), still open, counted below |

### Entities proposed to the owner

| Entity | Type | What it records | Ruling expected |
|--------|------|-----------------|-----------------|
| `@TASK.CHORE60` | `TASK`, `draft` | `agents/worktree.md` names the acceptance commands a task branch runs before its pull request opens, `bun run audit:quality` included | Ratify to `todo`, or reject if the wave prompt alone is to carry the list; its number may move at #102's rebase |
| `@TASK.CHORE61` | `TASK`, `draft` | The review of the three provenance gates of `docs/_schema.awawa` against the cases of #101–#103, `@TASK.CHORE57` carrying the `PULL_REQUEST` type | Ratify to `todo`, or fold into `@TASK.CHORE57` at its refinement; its number may move at #102's rebase |
| `@PULL_REQUEST.PCST101`, `@PULL_REQUEST.PCST103` | `PULL_REQUEST` | Pull requests #101 and #103 of `@PROJECT.PCST`, deleted by their own review rounds (`2c9e2b0`, `65b03fe`) and re-created because `EVALUATES` names nothing else | Accept as the entities the report cites, until `@TASK.CHORE57` |
| `@USAGE_REPORT.PR101_102_103` | `USAGE_REPORT` | This report, anchored on its first-line tag, `EVALUATES` the three pull requests; the `SOURCE` of the two tasks | Accept as the source entity |

## Part B — The tool

### What went well

| Promise | Kept | Evidence |
|---------|------|----------|
| L004 refuses the deletion of an entity something cites | yes | Probe on a copy of #102's head tree with `PULL_REQUEST PCST102` removed: `L004 unresolved reference @PULL_REQUEST.PCST102` at `decisions.awawa:2559` and `tasks.awawa:626`, exit 1 — what reply 4060952577 claimed |
| `WHEN STATUS active` with `INCOMING <converse>` makes an orphan an error, under a `CONVERSE` declared on a nested `REF` | yes | `_schema.awawa` at `8d3496e`: `CONVERSE SOURCE_OF` under `SOURCE.REF` of `SCHEMA *`, `INCOMING SOURCE_OF` under `WHEN STATUS active` of `GAME_RELEASE` and `URL`; probe appending `GAME_RELEASE 9.999`: `L026 nothing refers to GAME_RELEASE 9.999 under SOURCE_OF`, exit 1; the two uncited releases of `master` were caught the same way after the rebase |
| `diff base head` renders a schema change and a promotion at field level | yes | `corpus-diff.txt` of #103: 18 entities, `@SCHEMA.*` `+ FIELD.FIELD.CONVERSE SOURCE_OF`, `@SCHEMA.GAME_RELEASE` `+ WHEN.INCOMING SOURCE_OF`, `@TASK.CHORE52` `STATUS todo -> implemented` with 17 `+ SPEC.IMPL`, 2 decisions `STATUS (none) -> archived`; #101: 6 entities; #102: 5; 0.0 s each |
| `status --where DELIVERED_BY==N` finds the delivered task | yes | `corpus-tasks-of-pr.json`: `101` → `@TASK.CHORE51`, `102` → `@TASK.FIX45`, `103` → `@TASK.CHORE52`, exit 0 |
| `lint --strict` from the root validates every new anchor | yes | 6 `IMPL` on `@TASK.CHORE51`, 17 on `@TASK.CHORE52`, 3 on `@TASK.FIX45`, 4 `IMPL`/`ENFORCED_BY` on the two decisions of #102, 10 `TABLE`/`JSON_SCHEMA` on the five `DATATABLE`, 8 repointed anchors on #103: `corpus-lint-strict.txt` exit 0 on the three heads |
| `refs` says what points at an entity, including through a nested `SOURCE.REF` | yes | `@PULL_REQUEST.PCST102`: 2 incoming (`SOURCE.REF` of a decision and of a task); `PCST35`, `PCST55`, `PCST60`: 1 each, as reply 4060952577 states |
| `--overlay` lets a gate be tested without writing the corpus | yes | `agent-aa2411a6` hand-back: « I tested the orphan-source gate by overlay: an orphan URL and an orphan GAME_RELEASE each raise L026 » |
| `new TYPE Name` prints a schema-correct skeleton | yes | 1 `new` in each of `agent-aa2411a6`, `agent-a311ea`, `agent-a71e2b62`; no lint error on a skeleton in the timelines |
| Every `awawa` command runs in 0.0 s on a 289-entity corpus | yes | `index.md` of the three collections: `status`, `show`, `lint --closure`, `diff`, `fmt --check`, `lint --strict` all at 0.0 s |

### What went wrong

| Defect | Observation | Checked against |
|--------|-------------|-----------------|
| awawa cannot run in CI today: it is a local binary with no install source the project can name | `~/.local/bin/awawa`: ELF x86-64, 1.6 MB, dated 2026-09-11, `awawa --version` 2.7.0; the manual (`~/Web/awawa/manual.md` § 2 Install) names `scripts/install.sh`, `cargo install --path .` and `scripts/package.sh` — all inside a source checkout, and `~/Web/awawa` is an unpacked archive, not a repository (`git remote -v`: not a git repository); no published release the runner could fetch. Consequences on this journey: `scripts/check-corpus-anchors.ts` re-parses the corpus text (255 lines plus a 253-line spec) instead of calling `awawa show --json`, with the reason as its line 5 comment; no job of `.github/workflows/quality.yml` runs `awawa fmt --check` or `awawa lint --strict` (`grep -n awawa .github/workflows/*.yml`: 0 lines), so the corpus is checked only on its author's machine; `grep -rl awawa scripts packages .github package.json`: the guard and its spec alone. Recorded by the review as `@TASK.CHORE58`, cited here on the owner's request (4060968303) | run, manual |
| L016 tests an anchor's path on the disk and never consults git | `@TASK.CHORE51` `RATIONALE`: « L016 tests existence on disk and never consults git, so an anchor on an ignored path is clean for its author and an error in a fresh clone »; the whole guard of #101 (508 lines) exists to close that gap; the guard's spec pins the case « a file on disk that git does not track » | run (the guard's spec), manual |
| L025 treats the fragment of an anchor as prose | Probe on a copy of #103's head: `IMPL "docs/save-format.md::@DATATABLE.LegacyTerrainLayerProperties"`, whose fragment exists at `docs/save-format.md:360` → `L025 @DATATABLE.LegacyTerrainLayerProperties is mentioned in prose and indexed by nothing; nest a field carrying … under IMPL`; `agent-a4791676` met it on 2026-09-18 and « anchored on nearby plain text instead » (hand-back 13:51:44Z) | run, transcript |
| `lint --closure` has no `--skip`, so the closure of a delivered task carries the provenance hub | `corpus-closure-TASK-CHORE51.txt`: 7 entities, footer `PULL_REQUESTS (rest, 46)` and `referenced by (rest, 73)`; `CHORE52`: `SOURCE_OF (rest, 71)`, `PULL_REQUESTS (rest, 46)`; `FIX45`: `GOVERNED_BY (rest, 32)`, `PULL_REQUESTS (rest, 44)`, `referenced by (rest, 65)` — through `SOURCE → @PROJECT.PCST` in every case (raised in `2026-09-18-pr-100.md`) | run |
| `status` counts an entity at the `DEFAULT` value of `STATUS` as `(none)` | `corpus-status.txt` of the three heads: `DECISION (none) 105` or `106`, `PULL_REQUEST (none) 48` or `49`, while `SCHEMA *` declares `DEFAULT active` (raised in `2026-09-17-pr-95.md` and `2026-09-18-pr-100.md`) | run |
| `fmt` wraps strings at about 95 columns with no option, and a text edit that does not expect the wrap fails | `agent-aa2411a6` 13:47:32Z: `fix_anchors.py` `AssertionError` on a `REJECTED` line the script expected whole and the file held wrapped after `awawa fmt`; one answer lost, the edit redone; `git-diff-corpus.patch` of #103: 41 `+ "` continuation lines among the 118 added; this report's own `FILE` anchor (99 characters) came back from `awawa fmt .` on two `+` lines, L016 still resolving it (raised in the two previous reports) | run, transcript |

Sent back to part A: the three treatments of the `PULL_REQUEST` instruction and the L026 block of #103 come from declarations of our own schema (`SOURCE.REF REQUIRED`, `EVALUATES @PULL_REQUEST`, the `INCOMING` gate) and from a prompt line, not from the binary, which did what those declarations say.

### Remediations

| Proposal | What it would have changed on this PR |
|----------|---------------------------------------|
| A published release of the binary (the `awawa-<version>-<os>-<arch>.zip` the manual's `scripts/package.sh` builds) at an address a GitHub Actions step can fetch, or an install action | `check:anchors` calls `awawa show --json` instead of carrying a 255-line parser; `quality.yml` runs `awawa fmt --check` and `awawa lint --strict` on every pull request; `@TASK.CHORE58` has an answer to its first `SPEC` |
| L016 consults `git ls-files` when the workspace root is inside a repository, or a `--tracked` flag on `lint` does | `@TASK.CHORE51` is not needed; its 508 lines are 0 |
| L025 skips the fragment of an anchor field (everything after `::`) | `agent-a4791676` anchors the two docs `IMPL` on the `@DATATABLE` mention it wanted, not on a neighbouring sentence |
| `--skip CATEGORY` on `lint --closure`, as on `context` | The closure footers of the three tasks list 1 to 4 names instead of 117 to 121 |
| `status` reports the `DEFAULT` value as the entity's `STATUS` | `corpus-status.txt` reads `DECISION active 105` |
| `--width N` or `--no-wrap` on `fmt`, or the width in `fmt --help` | The `fix_anchors.py` assertion holds |

## Recurring points

### Recurring pain points

| Point | Times seen | First raised in | Seen again here | Remediation proposed then | Done? |
|-------|------------|-----------------|-----------------|---------------------------|-------|
| Commands refused by the worktree-isolation check — **flagged, 3 reports** | 3 | `2026-09-17-pr-95.md`, part A | 24 refusals in 238 answers of the 6 isolated agents; 0 in the 9 un-isolated ones | The rule in `commands.md` (delairec/.ai#34 `abde632`, merged 2026-09-17), then (`pr-100`) the launch prompt quotes the line | rule merged, not working; the prompt edit not done (`grep` of `awawa-plan-wave-start.md` for the line: 0) — remediation 5 above adds the un-isolated launch that the evidence shows works |
| `status` counts the `DEFAULT` `STATUS` as `(none)` — **flagged, 3 reports** | 3 | `2026-09-17-pr-95.md`, part B | `DECISION (none) 105` on the three heads | `status` reports the `DEFAULT` value | no (tool) |
| `fmt` wraps long strings with no option — **flagged, 3 reports** | 3 | `2026-09-17-pr-95.md`, part B | `fix_anchors.py` assertion on a wrapped `REJECTED`, 13:47:32Z; 41 continuation lines in #103's corpus diff | `--width N` or `--no-wrap` | no (tool) |
| `lint --closure` has no `--skip` | 2 | `2026-09-18-pr-100.md`, part B | Footers of 117 to 121 names on the three delivered tasks | `--skip CATEGORY` on `lint --closure` | no (tool) |
| A schema change landing mid-wave ripples to the open pull requests | 2 | `2026-09-18-pr-100.md`, part A | The 3 rebase agents (64 answers, 3.49 M) and the `PULL_REQUEST` churn that followed the `DELIVERED_BY` change | After a merge changing `_schema.awawa`, the review command lists the open task PRs whose diff touches the changed fields | no (`grep _schema.awawa` in `awawa-pr-review-round.md` and `awawa-pr-review.md`: 0 lines) |
| The readiness comment reads as an authorisation to merge now, and the report lands elsewhere | 2 | `2026-09-18-pr-100.md`, part A | #101 merged at 10:40:48Z while this report, which measures it, was being written for #102's branch — arranged this time, but the report of #101 is still not in #101 | The readiness comment names what remains (`awawa-pr-review-round.md` line 169) | no (line 169 unchanged: « Ready to be merged — review addressed, corpus updated, checks green. Merging is yours. ») |
| The modelling of a delivery overturned by review (`PULL_REQUEST` per PR) | 2 | `2026-09-18-pr-100.md`, part A | The owner asked three times for the entities' removal; `@TASK.CHORE57` opened by #101 to remove the type | none then (the review had settled `DELIVERED_BY`) | partly — `@TASK.CHORE57` draft; remediation 2 above |

Raised before and not seen again, closed by observation: 93 answers in one context (longest here: 62, `agent-a2b5dd95`, noted above as a PROC-4 miss); three `DECISION` entities written then struck (0 written in five rounds, 1 deleted); five undecided points in the PR body (`corpus-open-questions-live.txt`: 0 `OPEN_QUESTION`, no open point in the three bodies); the carried decisions contradicted by code and `PROCESS` without `APPLIES_TO_*` (`@TASK.DOCS50` still `draft`, not this wave's subject); the owner's hand commits introducing gaps (the two uncited releases are the closest case, counted under the L026 row of part A); a launch prompt asserting a setup that did not exist (the three round prompts of 2026-09-21 say « Do not assume the per-worktree setup »); the pre-commit hook blocked on its PATH (the hook ran and refused the L026 commits, as intended); commits without `Co-Authored-By` (0 of 18); the owner's answer lost by the CLI (1 `AskUserQuestion`, answered in 46 s); the review's claim wider than its check (the L004 claim of #102 named its perimeter and holds); the nested `REF` declared 8 times, `diff` pairing by name, `fmt --help` printing the global options (not exercised).

### Recurring strengths

| Mechanism | Times seen | First noted in | Held again here |
|-----------|------------|----------------|-----------------|
| Every commit lints clean from the repository root — **established** | 3 | `2026-09-17-pr-95.md`, part A | 18 of 18 commits, `lint --strict` and `fmt --check` exit 0 |
| Questions to the owner batched in one call — **established** | 3 | `2026-09-17-pr-95.md`, part A | 1 `AskUserQuestion` on the review day, 3 options and a recommendation |
| Work parallelised in agents with a written brief — **established** | 3 | `2026-09-17-pr-95.md`, part A | 3 delivery agents launched in 41 s with ~7 k-character briefs; 3 review agents launched in 64 s |
| A review round addresses its threads in minutes — **established** | 3 | `2026-09-17-pr-95.md`, part A | 4 min 56 s to 6 min 17 s per round, 12 threads and 3 comments over 5 rounds |
| Incoming references checked before a deletion — **established** | 3 | `2026-09-17-pr-95.md`, part A | 15 `refs` calls in the review agents; 0 orphan `PULL_REQUEST` at #102's head |
| The PR body kept describing the branch head — **established** | 3 | `2026-09-17-pr-95.md`, part A | Rewritten in #101 round 2 and 3, #103 round 2, #102 rebase |
| Checks green at head — **established** | 3 | `2026-09-17-pr-95.md`, part A | 8 CI jobs pass on each of the 3 heads; local `bun test`, `lint:types`, `audit:quality` exit 0 on #102 and #103 |
| The recording threshold applied before a `DECISION` is written | 2 | `2026-09-18-pr-100.md`, part A | 0 `DECISION` in five rounds; 1 deleted below the threshold with the reason in the thread |
| `lint --strict` validates every anchor — **established** | 3 | `2026-09-17-pr-95.md`, part B | 48 new or repointed anchors resolve |
| `refs` says what points at an entity — **established** | 3 | `2026-09-17-pr-95.md`, part B | `PCST102` 2 incoming, `PCST101` 1 at `9e23ad9`, `1.618` 0 at base |
| `status --where FIELD==VALUE` finds an entity without a file read — **established** | 3 | `2026-09-17-pr-95.md`, part B | `DELIVERED_BY==101`, `102`, `103` |
| `diff base head` gives an entity-level review — **established** | 3 | `2026-09-17-pr-95.md`, part B | 4, 6 and 18 entities at field level, schema included |
| `WHEN <field> <value>` gates a required field or an incoming edge — **established** | 3 | `2026-09-17-pr-95.md`, part B | `WHEN STATUS implemented` required 17 `IMPL` on `CHORE52`; `WHEN STATUS active` + `INCOMING SOURCE_OF` caught 2 orphans |
| `new TYPE Name` skeletons | 2 | `2026-09-17-pr-95.md`, part B | 3 calls, 0 skeleton lint errors |
| The 50-step hand-over of PROC-4 | 1 | this report | `agent-aa2411a6` stopped at 50, `agent-a4791676` finished in 17 answers (`2026-09-18-pr-100.md` saw it in its follow-up, outside its perimeter) |
| A wave ruling relayed to the peer it concerns | 1 | this report | `SendMessage` at 13:46:31Z on awawa-in-CI |

Noted before and not seen holding here: the hosted `claude[bot]` review finding gaps — « No issues found » on the three pull requests while the owner opened 12 threads; the pilot sending an unbacked claim back before relaying it (no such case: the three delivery hand-backs were relayed as received); one `SCHEMA *` per walk forcing a commit sequence, the deleted design files classified, a held sentence turned into a general rule the same afternoon (not exercised).

## Follow-up on the previous report's remediations

| Remediation | Done? | Evidence |
|-------------|-------|----------|
| 1 — The readiness comment of a task PR names what remains (report, then merge) | no | `~/.ai/procedures/awawa-pr-review-round.md` line 169 unchanged; the five readiness comments of this journey carry the old sentence; #101 merged before its report landed, by arrangement |
| 2 — After a merge changing `docs/_schema.awawa`, the review command lists the open task PRs to rebase | no | `grep -n _schema.awawa` in `awawa-pr-review-round.md` and `awawa-pr-review.md`: 0 lines; no schema change merged since, so not exercised either |
| 3 — The wave launch prompt quotes the refusal rule above its base check | no | `grep` of `awawa-plan-wave-start.md` for the line: 0; 24 refusals here, 5 of them on the base check (`git merge-base … ; echo anc=$?`) the prompt's own example chains |
| 4 — A claim about what lint refuses names the gate it holds under | yes | Reply 4060952577: « I checked on a copy of the corpus: removing the entity makes awawa lint --strict fail with L004 … at decisions.awawa:2559 » — perimeter named, probe reproduced |
| `@PULL_REQUEST.PCST100` and `@USAGE_REPORT.PR100` accepted | yes | Both at `master` `c8039f8` (`sources.awawa:263`, `:268`); `@USAGE_REPORT.PR100` has 0 incoming, `PR95` 2 (`awawa status USAGE_REPORT .`) |
| `@TASK.DOCS50` (from `pr-95`, restated in `pr-100`) | no | `awawa status TASK .`: `draft`, `tasks.awawa:510` |

## Appendix

### Measurements

Active time counts gaps under ten minutes only. Figures from `measure.py` (`transcripts-measure.txt` of the three collections; `agent-a71e2b62` measured by hand). Tool calls exclude `SubagentHandback`. The three pilots marked shared also drove PR #100 or the launch of this report and are not summed.

| Session | Active time | User turns | Tool calls | awawa calls by subcommand | Tokens read from cache | Tokens written |
|---------|-------------|------------|------------|---------------------------|------------------------|----------------|
| wave pilot `51c18626` (shared with #100), 33 answers | 17 min 06 s | 16 | 28 | status 4, context 1, lint 2, show 2 | 3,161,528 | 25,713 |
| `agent-a29933ea` CHORE51 delivery, 40 answers | 6 min 46 s | 3 | 56 | fmt 10, lint 9, show 4, context 1, status 1 | 3,816,861 | 26,936 |
| `agent-a1ccac75` GREEN of CHORE51, 8 answers | 1 min 39 s | 2 | 9 | — | 320,671 | 11,419 |
| `agent-ac59e89a` FIX45 delivery, 34 answers | 8 min 48 s | 4 | 41 | lint 7, fmt 6, refs 3, show 2, context 1 | 3,636,218 | 24,930 |
| `agent-af2e08bd` GREEN of FIX45, 32 answers | 4 min 14 s | 2 | 34 | — | 2,068,597 | 18,626 |
| `agent-aa2411a6` CHORE52 delivery, 50 answers (budget stop) | 9 min 21 s | 4 | 68 | lint 9, fmt 5, show 4, status 3, context 1, new 1, refs 1 | 6,387,127 | 52,852 |
| `agent-a367d94a` GREEN of CHORE52, 6 answers | 32 s | 2 | 5 | — | 215,792 | 2,279 |
| `agent-a4791676` CHORE52 hand-over, 17 answers | 2 min 11 s | 3 | 17 | lint 5, fmt 4, show 3, context 1, status 1 | 984,819 | 10,150 |
| #100 review pilot `d9c08253` (shared, up to 14:11:30Z), 30 answers | 16 min 59 s | 10 | 27 | status 7 | 1,819,074 | 19,803 |
| `agent-ab415d3f` rebase #101 after #100, 26 answers | 4 min 44 s | 5 | 24 | fmt 5, lint 5, show 2, refs 1 | 1,527,371 | 9,255 |
| `agent-a559648d` rebase #102 after #100, 21 answers | 4 min 21 s | 4 | 21 | lint 3, fmt 3, show 2, refs 1 | 1,112,845 | 9,176 |
| `agent-a84a7e10` rebase #103 after #100, 17 answers | 3 min 57 s | 4 | 16 | show 4, fmt 3, lint 3, refs 1 | 845,296 | 6,401 |
| `agent-a72fd73e` fallow fix #101, 22 answers | 4 min 22 s | 4 | 21 | lint 3, fmt 1 | 1,299,517 | 9,855 |
| interactive `7c8c9316` fallow fix #102 (Sonnet 5), 21 answers | 2 min 14 s | 1 | 20 | — | 1,413,143 | 7,549 |
| review pilot `a756b5f6` (#101–#103), 19 answers | 20 min 41 s | 9 | 14 | — | 1,197,463 | 11,944 |
| `agent-a0aee5fb` #101 round 2, 44 answers | 6 min 17 s | 2 | 61 | lint 9, fmt 8, status 7, show 5, refs 2 | 3,156,996 | 20,143 |
| `agent-a2b5dd95` #103 round 1, 62 answers | 5 min 42 s | 2 | 69 | show 9, lint 8, fmt 4, refs 4, status 3 | 4,481,942 | 27,386 |
| `agent-a7abe9f0` #103 round 2, 35 answers | 5 min 19 s | 2 | 34 | show 7, lint 6, fmt 4, refs 1 | 2,354,485 | 19,684 |
| `agent-aef0c1aa` #102 rebase, 22 answers | 9 min 05 s | 2 | 20 | fmt 3, lint 3 | 1,101,747 | 7,832 |
| review pilot `3373c56c` (#102), 8 answers | 6 min 15 s | 6 | 5 | — | 391,279 | 2,887 |
| `agent-a311ea` #102 round, 26 answers | 5 min 31 s | 2 | 24 | show 9, refs 8, status 6, lint 4, fmt 2, new 1 | 1,425,051 | 12,724 |
| `agent-a71e2b62` #101 round 3, 25 answers | 4 min 56 s | 2 | 23 | show 6, fmt 5, lint 5, status 4, new 1 | 1,562,345 | 13,532 |
| **Dedicated total (19 rows, the 3 shared pilots excluded)** | 1 h 56 min 55 s | 61 | 578 | 225 (lint 87, fmt 73, show 55, status 25, refs 20, context 5, new 3) | 39,299,565 | 305,560 |

Per pull request, dedicated rows only: #101 165 answers, 11,683,761 cache-read, 91,140 written (delivery 48, rebase 26, fallow fix 22, round 2 44, round 3 25); #102 164 answers, 11,152,289 cache-read, 83,616 written (delivery 66, rebase 21, fallow fix 21, rebase 22, review 34); #103 187 answers, 15,266,461 cache-read, 118,860 written (delivery 73, rebase 17, round 1 62, round 2 35); the review pilot `a756b5f6` (19 answers) served the three.

Journey: pull requests opened 2026-09-18 13:45:04Z (#101), 13:48:27Z (#102), 13:48:45Z (#103); first owner review 2026-09-20 13:16:07Z (#103), 13:31:41Z (#101), 2026-09-21 09:38:33Z (#102); readiness 2026-09-21 09:36:49Z (#103), 09:49:44Z (#102), 10:33Z (#101, comment edited); #101 merged 10:40:48Z.

| Pull request | Corpus lines changed | PR lines changed | Share |
|--------------|----------------------|------------------|-------|
| #101 (`c8039f8...029b884`) | 69 (+68 −1) | 584 (+580 −4) | 11.8 % |
| #102 (`c8039f8...37f2ca3`) | 58 (+56 −2) | 183 (+174 −9) | 31.7 % |
| #103 (`c8039f8...8d3496e`) | 138 (+118 −20) | 2,219 (+1,248 −971) | 6.2 % |
| Three together | 265 | 2,986 | 8.9 % |

`git diff --numstat` on each range, `.awawa` files against all files.

### Claims checked

| Claim | Check | Verdict |
|-------|-------|---------|
| #102 body: `bun test` 818 pass, 0 fail; `lint:types`, `check:guards`, `awawa fmt --check`, `awawa lint --strict` exit 0 | `checks-bun-test.txt` 818 pass 0 fail; `checks-lint-types.txt` exit 0; `corpus-fmt-check.txt`, `corpus-lint-strict.txt` exit 0 on `trees/head` | confirmed |
| #102 reply 4060952577: removing `@PULL_REQUEST.PCST102` makes `awawa lint --strict` fail with L004 at `decisions.awawa:2559` | Probe on a copy of `trees/head`: 2 × L004 (`decisions.awawa:2559:7`, `tasks.awawa:626:7`), exit 1 | confirmed |
| #102 reply 4060952577: `PCST35`, `PCST55`, `PCST60` are each cited by one decision's `SOURCE.REF` alone | `awawa refs` on `trees/head`: 1 incoming each, all `SOURCE.REF` of a `DECISION` | confirmed |
| #102 body: `@TASK.FIX45` promoted to `implemented`, `DELIVERED_BY 102`; `@DECISION.AnEmptyMetadataSectionIsRefusedByValidation` `IMPL` repointed, `ENFORCED_BY` added | `corpus-diff.txt`: `STATUS todo -> implemented`, `+ DELIVERED_BY 102`; 1 `~ SPEC.IMPL`, 2 `+ SPEC.IMPL`, 3 `+ SPEC.ENFORCED_BY` | confirmed |
| #102 rebase comment: `git range-diff` shows the three commits unchanged apart from the conflict context | `git range-diff 6a1b54d..20cbb3b c8039f8..01aa2ec` in this worktree: commit 1 `=`, commit 2 `!` on `sources.awawa` context lines only (the third line was cut by this report's `head -12`) | confirmed for commits 1 and 2, the third unchecked |
| #103 body: `awawa lint --strict .` exits 0 at each commit of the branch | `lint-each-commit.py`: 7 of 7 commits, lint 0 and fmt 0 | confirmed |
| #103 body: `master` added `@GAME_RELEASE.1.618` and `2.008` with no citation, and the new gate rejects them | `awawa refs` on `trees/base` (`c8039f8`): 0 incoming each (`2.103`: 1); probe of an orphan release on `trees/head`: L026, exit 1 | confirmed |
| #103 body: `bun test` 821 pass; `validate:tables` not wired into `check:guards` or CI | `checks-bun-test.txt` 821 pass 0 fail; `package.json:30` `check:guards` chain has no `validate:tables`; `grep validate .github/workflows/quality.yml`: 0 | confirmed |
| #103 round 2 hand-back: the final tree is identical to the one before the reorder | The pre-reorder tree was not pushed and is not in the collection | unchecked |
| #101 body: `bun test` 825 pass, 8 guards, `awawa` checks green | Checks skipped for #101 (`--skip-checks`, its worktree being another agent's); CI `test` and `guards` jobs pass on `029b884` (`gh pr checks 101`); `corpus-lint-strict.txt` and `corpus-fmt-check.txt` exit 0 on `trees/head` | confirmed by CI, not run locally |
| #101 reply 4061284213: only `scripts/check-corpus-anchors.ts` and its spec mention awawa outside `docs/` | `grep -rl awawa scripts packages .github package.json` on `trees/head`: the two files | confirmed |
| #101 reply 4061284213: no job of `quality.yml` runs `awawa fmt --check` or `awawa lint --strict` | `grep -n awawa .github/workflows/*.yml`: 0 lines | confirmed |
| #101 reply 4060809253: `@PULL_REQUEST.PCST101` was cited only by `@DECISION.TheAnchorGuardReadsTheCorpusTextItself` | `awawa refs @PULL_REQUEST.PCST101 .` on the extracted tree of `9e23ad9` (the commit before the deletion): 1 incoming, that decision's `SOURCE.REF` | confirmed |
| #101 round 3 hand-back: the highest task number in use across `master`, #102 and #103 was 57 | `awawa status TASK .` on `master` (`FEAT55` highest), `trees/head` of #102 (`REFACTOR56`), of #101 at `8e971cb` (`CHORE57`); #103 adds no task | confirmed |
| #101 body: `@TASK.CHORE51` `implemented` with an `IMPL` per `SPEC` and `DELIVERED_BY 101` | `corpus-show-TASK-CHORE51.txt`: 4 `SPEC`, 6 `IMPL`, `DELIVERED_BY 101`; `corpus-closure-TASK-CHORE51.txt` exit 0 | confirmed |
| `2026-09-18-pr-100.md`: the three rebase agents cost 47 answers and 2.36 M cache-read tokens | Full runs: 64 answers, 3,485,512 cache-read; the report's figure was taken up to its own cut at 14:11:30Z, which it states | confirmed within its perimeter |
| `2026-09-18-pr-100.md` remediation 3: the wave prompt quotes the refusal rule | `grep` of `~/.claude/commands/awawa-plan-wave-start.md`: 0 matching lines | wrong (not done) |
| Owner-directed: awawa cannot run in CI today, binary local only, no install source | `~/.local/bin/awawa` ELF x86-64; `~/Web/awawa` not a git repository; manual § 2 names install scripts of a source checkout; `.github/workflows/*.yml` run no awawa | confirmed |
