{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "energyLevelsOfRelease.schema.json",
  "title": "Energy levels of a release",
  "description": "Base power, in kW, of the world objects whose value in one earlier release differs from the next newer table of a release, or from the energy levels table, which holds the values of the last release, for the newest; the table applies to its release and every earlier one; one row per world object name.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["worldObjectName", "role", "kilowatts", "source"],
    "additionalProperties": false,
    "properties": {
      "worldObjectName": {"type": "string", "valueOfTable": {"table": "packages/core-mapping/src/domain/energyLevels.json", "column": "worldObjectName"}},
      "role": {"enum": ["production", "consumption"]},
      "kilowatts": {"type": "number", "exclusiveMinimum": 0},
      "source": {"type": "string", "pattern": "^https://"}
    }
  }
}
